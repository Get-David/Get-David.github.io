import{_ as t,o,c as i,a2 as e}from"./chunks/framework.BWuWLRhz.js";const a="/assets/24-hook-lifecycle@2x.D0sxhcfu.png",n="/assets/24-lifecycle-hooks@2x.DhSGjjPp.png",E=JSON.parse('{"title":"24 · 规则与钩子（Rules & Hooks）：给 Codex 装上「卡点」和「扳机」","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"codex/24-hooks.md","filePath":"codex/24-hooks.md"}'),d={name:"codex/24-hooks.md"};function p(l,s,h,r,c,k){return o(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="_24-·-规则与钩子-rules-hooks-给-codex-装上「卡点」和「扳机」" tabindex="-1">24 · 规则与钩子（Rules &amp; Hooks）：给 Codex 装上「卡点」和「扳机」 <a class="header-anchor" href="#_24-·-规则与钩子-rules-hooks-给-codex-装上「卡点」和「扳机」" aria-label="Permalink to &quot;24 · 规则与钩子（Rules &amp; Hooks）：给 Codex 装上「卡点」和「扳机」&quot;">​</a></h1><blockquote><p>📚 <strong>系列导航</strong>：上一篇〔<a href="./23-plugins">23 插件（Plugins）</a>〕教你把别人打包好的能力一键装进 Codex。这一篇聊两件更底层、也更「你说了算」的事——<strong>规则（Rules）管「哪些命令能在沙箱外跑」，钩子（Hooks）管「在干活流程的某个固定点自动替你跑一段脚本」</strong>。一个是闸门，一个是扳机，配好了，很多「每次都得盯着、每次都得手敲」的破事就再也不用你操心了。下一篇〔<a href="./25-worktrees">25 Worktrees 并行隔离</a>〕再讲怎么让多个 Codex 各干各的、互不打架。</p></blockquote><p>先还原一段我上个月真实发生的对话。我跟同事在排查一个 CI 老挂的问题：</p><blockquote><p>我：「这周我让 Codex 改完代码，手动补敲 <code>ruff format</code> 的次数，少说也有十几回了。」 同事：「你 AGENTS.md 里不是写了『改完记得格式化』吗？」 我：「写了啊。可它三次里总有一次想不起来——那是<strong>请求</strong>，又不是<strong>保证</strong>。漏一次，CI 的格式检查就把我打回来重跑一遍。」 同事：「那你挂个钩子啊。事件一触发，脚本必跑，跟它记不记得没关系。」</p></blockquote><p>就这一句点醒我。回去配了一个 <code>PostToolUse</code> 钩子，从那以后 Codex 每改完一个文件，格式化自动跑，<strong>再没手敲过一次，也再没被 CI 打回来过</strong>。这一篇，就把规则和钩子这俩东西，从「是什么」一路讲到「怎么配、怎么调」。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>一句话说清规则和钩子各管什么、为啥它们能做到 AGENTS.md 做不到的「确定性」</li><li>规则（<code>.rules</code> 文件 + <code>prefix_rule</code>）怎么写，让某条命令「永远放行」或「直接禁掉」，怎么用 <code>codex execpolicy check</code> 先验后用</li><li>钩子配在哪几个文件、生命周期里有哪些「时机」（<code>PreToolUse</code> / <code>PostToolUse</code> / <code>Stop</code> / <code>SessionStart</code> 等）能挂</li><li>Codex 钩子特有的<strong>信任机制</strong>——为什么新钩子默认不跑，得你在 <code>/hooks</code> 里先「过目签字」</li><li>钩子和 Codex 怎么对话（stdin 的 JSON、退出码、stdout 的 JSON），以及和 Claude Code 几个<strong>反直觉的差异</strong></li><li>两个能直接抄走的真实例子：改完自动格式化、拦危险命令；外加钩子不触发时怎么查</li></ul><blockquote><p>⚠️ 下文凡涉及具体命令、配置项、默认值，都以 Codex 官方文档（<a href="https://developers.openai.com/codex/hooks" target="_blank" rel="noreferrer">Hooks</a> / <a href="https://developers.openai.com/codex/rules" target="_blank" rel="noreferrer">Rules</a>）为准。<strong>规则（Rules）官方标注为「实验性，可能变化」</strong>，配置项名字和行为随版本可能调整，看到时以你本地实际为准。</p></blockquote><hr><h2 id="_01-先分清-规则管「闸门」-钩子管「扳机」" tabindex="-1">01 先分清：规则管「闸门」，钩子管「扳机」 <a class="header-anchor" href="#_01-先分清-规则管「闸门」-钩子管「扳机」" aria-label="Permalink to &quot;01 先分清：规则管「闸门」，钩子管「扳机」&quot;">​</a></h2><p>开篇先把这俩的分工钉死，因为名字都挺抽象，新手最容易糊在一起。</p><ul><li><strong>规则（Rules）</strong> 解决的是「<strong>这条命令到底准不准在沙箱外跑</strong>」——它是一道<strong>闸门</strong>，盯着你想执行的命令，按你写死的规矩判「放行 / 先问 / 直接禁」。</li><li><strong>钩子（Hooks）</strong> 解决的是「<strong>干活流程走到某个点，自动替我跑段脚本</strong>」——它是一个<strong>扳机</strong>，挂在生命周期的固定时机上，事件一到就开火，跟 Codex 自己想不想干没关系。</li></ul><p><strong>类比：小区门口的两套东西——门禁卡规则，和自动感应灯。</strong> 门禁系统里存着一张「谁能进、谁要登记、谁直接拦」的名单，这就是<strong>规则</strong>：每次有人刷卡，它对着名单判一下。而楼道里那盏「人一走过就亮」的感应灯，是<strong>钩子</strong>：它不关心你是谁、要去哪，只要「有人经过」这个事件发生，灯就必然亮。一个在<strong>判断准入</strong>，一个在<strong>自动响应</strong>——两件事，别混。</p><p>那它们和你已经会写的 AGENTS.md（项目说明书，第 <a href="./11-agents-md">11</a> 篇讲过）差在哪？差在一个<strong>根本点</strong>：</p><blockquote><p>写进 AGENTS.md 的，是<strong>请求</strong>；配成规则或钩子的，是<strong>保证</strong>。</p></blockquote><p>说白了：</p><ul><li><strong>AGENTS.md 里写「改完记得格式化」「别碰生产库」</strong>——这是你<strong>拜托</strong> Codex 做的事，它大概率照办，但每一步都在「自己判断」，<strong>可能漏、可能忘</strong>。</li><li><strong>配成钩子的格式化、配成规则的禁令</strong>——只要那个条件触发，动作<strong>一定</strong>执行（或一定被拦），跟 Codex 想不想、记不记得没半点关系。</li></ul><p>这就是开头那段对话的核心：「改完跑格式化」写在 AGENTS.md 里是请求，三次漏一次；配成钩子是保证，一次不漏。</p><p>几个你大概率会遇到、值得「上保证」的场景，先感受一下：</p><ul><li><strong>「<code>gh pr view</code> 这条命令我天天用，别每次都弹审批问我」</strong>——用规则给它开个「永远放行」的口子</li><li><strong>「<code>rm -rf</code>、<code>git push --force</code> 这种，谁来都给我硬拦死」</strong>——用规则或钩子确定地拦住，不靠提示</li><li><strong>「每次改完文件，自动跑格式化 / lint」</strong>——用钩子，别再手敲，也别指望它自觉</li></ul><blockquote><p>💡 一句话总结：规则是「准不准跑某条命令」的闸门，钩子是「到某个时机自动跑脚本」的扳机；它俩的共同价值，是把 AGENTS.md 里的<strong>请求</strong>升级成必然兑现的<strong>保证</strong>。</p></blockquote><hr><h2 id="_02-规则-rules-给单条命令立规矩" tabindex="-1">02 规则（Rules）：给单条命令立规矩 <a class="header-anchor" href="#_02-规则-rules-给单条命令立规矩" aria-label="Permalink to &quot;02 规则（Rules）：给单条命令立规矩&quot;">​</a></h2><blockquote><p>⚠️ <strong>实验性，可能变化。</strong> 规则是 Codex 官方明确标注的实验功能，下面的字段和行为以官方为准。</p></blockquote><p>先说规则解决的痛点。第 <a href="./15-permissions">15</a> 篇讲过，沙箱是「按圈划」的<strong>粗</strong>边界——圈里随便动、出圈就问。但有时候你想要的是<strong>按命令</strong>的精细控制：「<code>gh pr view</code> 哪怕出圈也直接放行，别烦我」「<code>grep</code> 一律禁掉，逼我自己用 <code>rg</code>」。这种需求，扩大整个沙箱太粗暴，写一条规则才对症。</p><p><strong>类比：给保安一张「特批 / 拉黑」名单。</strong> 沙箱是保安的默认守则（「陌生人一律先登记」），规则是你额外塞给他的两张小条子：一张写「这几个老熟人放进来，别拦」，一张写「这几个人来了直接轰走」。保安照着名单办，比每次都临时问你省事，也比「把大门拆了」安全。</p><p>它真实会用在哪：</p><ul><li>团队里 <code>gh</code>、<code>make test</code> 这类高频只读命令，开个白名单口子，省掉每天几十次审批点击</li><li>把 <code>grep</code>、<code>find</code> 这类有更好替代品的命令标成禁用，强制大家走 <code>rg</code>、<code>fd</code></li><li>公司层面把 <code>rm -rf /</code>、改 <code>~/.ssh</code> 这类高危前缀直接 <code>forbidden</code>，焊死团队红线</li></ul><h3 id="规则文件长什么样、放哪" tabindex="-1">规则文件长什么样、放哪 <a class="header-anchor" href="#规则文件长什么样、放哪" aria-label="Permalink to &quot;规则文件长什么样、放哪&quot;">​</a></h3><p>规则写在 <strong><code>.rules</code> 文件</strong>里，这文件要放在某个<strong>生效配置层旁边的 <code>rules/</code> 文件夹</strong>下，最常用的是用户级 <code>~/.codex/rules/default.rules</code> 。语法看着像 Python，<strong>实际是 Starlark</strong>（一种「能安全执行、不会乱碰你文件系统」的配置语言）。</p><p>一条最简单的规则长这样——「<code>gh pr view</code> 出沙箱前先问我一声」：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 运行带 \`gh pr view\` 前缀的命令前，先弹审批问我。</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">prefix_rule(</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 要匹配的命令前缀（按参数逐个写）。</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    pattern</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pr&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;view&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 匹配到时的动作：allow（直接放行）/ prompt（先问）/ forbidden（直接拦死）。</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    decision</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;prompt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 可选：写清这条规则为什么存在，审批弹窗里可能给你看。</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    justification</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;查看 PR 允许，但要我点头&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 可选的「内联单元测试」：举例哪些命令该命中、哪些不该，加载时 Codex 会替你验。</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    match</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;gh pr view 7888&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;gh pr view --repo openai/codex&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    not_match</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # 不命中：pattern 必须是「精确前缀」，这条把 view 挪后面了。</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;gh pr --repo openai/codex view 7888&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>prefix_rule</code> 里几个字段，挑最该懂的说：</p><table tabindex="0"><thead><tr><th>字段</th><th>作用</th><th>备注</th></tr></thead><tbody><tr><td><code>pattern</code>（必填）</td><td>要匹配的命令<strong>前缀</strong>，按参数逐个列</td><td>元素可以是字面量 <code>&quot;pr&quot;</code>，也可以是「或」<code>[&quot;view&quot;, &quot;list&quot;]</code></td></tr><tr><td><code>decision</code>（默认 <code>allow</code>）</td><td>命中后怎么办</td><td><code>allow</code> / <code>prompt</code> / <code>forbidden</code>，<strong>最严的赢</strong></td></tr><tr><td><code>justification</code>（可选）</td><td>这条规则的理由</td><td>审批 / 拒绝时可能显示给你</td></tr><tr><td><code>match</code> / <code>not_match</code>（可选）</td><td>举例验证规则对不对</td><td>加载时 Codex 替你跑，提前抓写错</td></tr></tbody></table><p><code>decision</code> 三选一，优先级官方写死——<strong>最严的赢</strong>（<code>forbidden</code> &gt; <code>prompt</code> &gt; <code>allow</code>）：</p><table tabindex="0"><thead><tr><th>decision</th><th>效果</th></tr></thead><tbody><tr><td><code>allow</code></td><td>出沙箱直接跑，不问</td></tr><tr><td><code>prompt</code></td><td>每次匹配都先问你</td></tr><tr><td><code>forbidden</code></td><td>直接拦死，不问也不跑</td></tr></tbody></table><p>写完 <code>.rules</code> 别忘了<strong>重启 Codex</strong> 才生效。还有个你会自然碰到的点：当你在 TUI 里手动把某条命令「加进允许列表」时，Codex 会自动帮你写进 <code>~/.codex/rules/default.rules</code> ——所以这个文件常常不是你纯手写的，而是用着用着自己长出来的。</p><h3 id="一个救命的安全设计-复合命令会被拆开判" tabindex="-1">一个救命的安全设计：复合命令会被拆开判 <a class="header-anchor" href="#一个救命的安全设计-复合命令会被拆开判" aria-label="Permalink to &quot;一个救命的安全设计：复合命令会被拆开判&quot;">​</a></h3><p>这条特别重要，第 <a href="./15-permissions">15</a> 篇点过，这里展开。碰到 <code>git add . &amp;&amp; rm -rf /</code> 这种<strong>把多条命令塞进一行</strong>的，Codex 在安全的前提下会用 tree-sitter <strong>把它拆成单条逐个判</strong>，再取「最严的结果」：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[&quot;bash&quot;, &quot;-lc&quot;, &quot;git add . &amp;&amp; rm -rf /&quot;]</span></span></code></pre></div><p>会被拆成两条单独评估：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[&quot;git&quot;, &quot;add&quot;, &quot;.&quot;]</span></span>
<span class="line"><span>[&quot;rm&quot;, &quot;-rf&quot;, &quot;/&quot;]</span></span></code></pre></div><p><strong>关键就在这</strong>：哪怕你 <code>allow</code> 了 <code>git add</code>，那个 <code>rm -rf /</code> 也会被<strong>单独拦下</strong>，整条命令不会被「夹带过关」。这堵死了「把危险命令藏在安全命令屁股后面偷渡」的路子。</p><p>但要留个心眼——<strong>只有「干净的线性命令链」才会被拆</strong>。如果脚本里用了重定向（<code>&gt;</code>）、变量替换（<code>$(...)</code>）、环境变量赋值（<code>FOO=bar</code>）、通配符（<code>*</code>）这些高级货，Codex <strong>不会</strong>去拆它，而是把整段当成一条 <code>bash -lc &quot;&lt;整个脚本&gt;&quot;</code> 来判。所以别指望规则能看穿任意复杂 shell——它在「安全可拆」时帮你细查，「拆不动」时保守对待。</p><h3 id="改完规则-先用-execpolicy-check-验一把" tabindex="-1">改完规则，先用 <code>execpolicy check</code> 验一把 <a class="header-anchor" href="#改完规则-先用-execpolicy-check-验一把" aria-label="Permalink to &quot;改完规则，先用 \`execpolicy check\` 验一把&quot;">​</a></h3><p>别上来就实战。改完规则文件，用 <code>codex execpolicy check</code> 拿一条命令试试它会怎么判：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> execpolicy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --pretty</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --rules</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.codex/rules/default.rules</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> view</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 7888</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> title,body,comments</span></span></code></pre></div><p>它会输出一段 JSON，告诉你<strong>最严的判决是什么、命中了哪几条规则</strong>（连 <code>justification</code> 一起带出来）。我自己写 <code>forbidden</code> 规则时养成了个习惯：<strong>先用 <code>execpolicy check</code> 把「该拦的拦住、不该拦的别误伤」验一遍，再重启 Codex</strong>。有次我图省事直接上，结果一条写宽了的 <code>forbidden</code> 把团队常用的 <code>git log</code> 也连带拦了，被同事吐槽半天——<code>execpolicy check</code> 跑一下本可以提前发现。</p><blockquote><p>💡 一句话总结：规则用 <code>.rules</code> 文件里的 <code>prefix_rule</code> 给命令前缀立规矩（<code>allow</code>/<code>prompt</code>/<code>forbidden</code>，<strong>最严的赢</strong>）；复合命令会被安全地拆开逐条判、防夹带；改完先 <code>codex execpolicy check</code> 验、再重启生效。</p></blockquote><hr><h2 id="_03-钩子-hooks-有哪些「时机」能挂" tabindex="-1">03 钩子（Hooks）：有哪些「时机」能挂 <a class="header-anchor" href="#_03-钩子-hooks-有哪些「时机」能挂" aria-label="Permalink to &quot;03 钩子（Hooks）：有哪些「时机」能挂&quot;">​</a></h2><p>规则讲完，转到钩子。钩子不是随便什么时候都能挂，它得挂在 Codex 干活流程里<strong>特定的「时机」<strong>上——官方叫</strong>事件（event）</strong>。想用好钩子，第一步就是认清「我想让这事在什么时候发生」。</p><p>回想第 <a href="./02-core-concepts">02</a> 篇讲的「代理循环」——Codex 干活是「想 → 做 → 看」转圈。这些事件，正好散布在这个循环的前前后后。先画张图，看几个最常用的事件卡在哪儿：</p><p><img src="`+a+'" alt="Codex 钩子生命周期：SessionStart → UserPromptSubmit → 要不要用工具？要用走 Pre/Post 循环；不用 Stop 收尾"></p><p>这张图把「想 → 做 → 看」摊开了：一进会话是 <code>SessionStart</code>，你说话是 <code>UserPromptSubmit</code>，然后进入「要不要用工具」的循环——每次动工具，前面有 <code>PreToolUse</code>、后面有 <code>PostToolUse</code>，这一轮答完就是 <code>Stop</code>。<strong>你想让动作在哪一步发生，就挂对应的那个事件。</strong></p><p>Codex 支持的事件不止这几个（还有压缩前后的 <code>PreCompact</code>/<code>PostCompact</code>、子代理起停的 <code>SubagentStart</code>/<code>SubagentStop</code>、审批请求时的 <code>PermissionRequest</code> 等），但<strong>对小白来说，先把下面这四个吃透，能覆盖九成场景</strong>：</p><table tabindex="0"><thead><tr><th>事件</th><th>什么时候触发</th><th>最典型的用法</th></tr></thead><tbody><tr><td><strong><code>PreToolUse</code></strong></td><td>某个工具<strong>执行前</strong></td><td>拦危险命令、改写命令（<strong>能阻止操作</strong>）</td></tr><tr><td><strong><code>PostToolUse</code></strong></td><td>某个工具产出结果<strong>之后</strong></td><td>改完文件自动格式化 / 跑 lint</td></tr><tr><td><strong><code>Stop</code></strong></td><td>Codex 答完这一轮</td><td>让它「再多跑一趟」（比如把没过的测试再修一遍）</td></tr><tr><td><strong><code>SessionStart</code></strong></td><td>会话开始 / 恢复时</td><td>往上下文里注入项目状态（如最近的提交）</td></tr></tbody></table><p>第五个值得知道的是 <strong><code>PermissionRequest</code></strong>（将要弹审批弹窗时触发），能自动放行或拒绝——配合第 02 节讲的规则一起用，比单独靠规则更灵活，尤其适合「高频命令免审批」这种场景。</p><p>记这张表有个窍门：<strong>看名字里的 <code>Pre</code> 和 <code>Post</code></strong>——<code>Pre</code> 是「之前」，所以只有它能在动作发生前<strong>拦住</strong>；<code>Post</code> 是「之后」，工具都跑完了，它撤不回已经产生的副作用，只能「事后补一刀」（格式化、记日志、把结果退回让模型重看）。</p><p><img src="'+n+`" alt="钩子挂在生命周期固定点"></p><p>这张图把上面四个事件摊在 Codex 一次会话的生命周期上：<strong>会话开始 / 工具调用前 / 工具调用后 / 会话结束</strong>自上而下串成主轴，每个固定点旁边各挂一个 hook 脚本——流程一走到那个点，挂着的脚本就被「到点自动跑」拉起来，跟 Codex 记不记得没关系。这就是钩子「扳机」的本质：你只管挂上，开火由时机决定。</p><p>这里有个<strong>易错点</strong>得专门说：和 Claude Code 不同，Codex 这边 <code>Stop</code> 事件里 <code>decision: &quot;block&quot;</code> <strong>不是「拒绝这一轮」</strong>，而是反过来——它告诉 Codex「<strong>别停，再来一轮</strong>」，并把你给的 <code>reason</code> 当成下一条用户提示。<code>PostToolUse</code> 的 <code>decision: &quot;block&quot;</code> 也不撤销已经跑完的命令，而是把反馈塞回去让模型接着往下想。<strong>Codex 的 <code>block</code> 在这两个事件上更像「续一轮 / 退回重看」，不是「否决」</strong>，别按 Claude Code 的直觉理解。</p><blockquote><p>💡 一句话总结：钩子挂在 Codex 生命周期的<strong>特定事件</strong>上；新手先吃透 <code>PreToolUse</code>（前，能拦）、<code>PostToolUse</code>（后，补刀）、<code>Stop</code>（答完，能续一轮）、<code>SessionStart</code>（开场）这四个；注意 Codex 的 <code>block</code> 在 <code>Stop</code>/<code>PostToolUse</code> 上是「续 / 退回」而非「否决」。</p></blockquote><hr><h2 id="_04-钩子配在哪、matcher-怎么把它收窄" tabindex="-1">04 钩子配在哪、<code>matcher</code> 怎么把它收窄 <a class="header-anchor" href="#_04-钩子配在哪、matcher-怎么把它收窄" aria-label="Permalink to &quot;04 钩子配在哪、\`matcher\` 怎么把它收窄&quot;">​</a></h2><p>知道了能挂哪些事件，来看怎么写、写哪。</p><h3 id="钩子放在哪几个文件" tabindex="-1">钩子放在哪几个文件 <a class="header-anchor" href="#钩子放在哪几个文件" aria-label="Permalink to &quot;钩子放在哪几个文件&quot;">​</a></h3><p>Codex 找钩子的地方有两种形态：<strong>独立的 <code>hooks.json</code></strong>，或 <strong><code>config.toml</code> 里内联的 <code>[hooks]</code> 表</strong>。官方说最常用的四个位置是：</p><table tabindex="0"><thead><tr><th>配置文件</th><th>生效范围</th><th>能共享给团队吗</th></tr></thead><tbody><tr><td><code>~/.codex/hooks.json</code></td><td><strong>你所有项目</strong></td><td>否，只在你这台机器</td></tr><tr><td><code>~/.codex/config.toml</code></td><td><strong>你所有项目</strong></td><td>否</td></tr><tr><td><code>&lt;repo&gt;/.codex/hooks.json</code></td><td><strong>仅当前项目</strong></td><td><strong>是</strong>，可提交进 git</td></tr><tr><td><code>&lt;repo&gt;/.codex/config.toml</code></td><td>仅当前项目</td><td><strong>是</strong></td></tr></tbody></table><p>逻辑和第 <a href="./15-permissions">15</a> 篇讲配置时一样：<strong>全队都该有的钩子（比如「改完一律格式化」）写进项目的 <code>.codex/</code> 提交进 git；只是你自己想要的写进 <code>~/.codex/</code></strong>。两个细节记一下：</p><ul><li><strong>多个来源的钩子会全部加载、一起跑</strong>——高优先级的配置层<strong>不会替换</strong>低优先级的钩子，是叠加关系。</li><li><strong>同一层里别同时写 <code>hooks.json</code> 和内联 <code>[hooks]</code></strong>，Codex 会合并但会在启动时警告你，官方建议一层只用一种写法。</li><li><strong>项目级钩子只在这个 <code>.codex/</code> 层被「信任」时才加载</strong>（信任是什么，下一节讲）；不信任的项目里，Codex 仍会加载你的用户级和系统级钩子。</li></ul><h3 id="钩子配置长什么样" tabindex="-1">钩子配置长什么样 <a class="header-anchor" href="#钩子配置长什么样" aria-label="Permalink to &quot;钩子配置长什么样&quot;">​</a></h3><p>先看一个最小的完整例子——<strong>「每次 Codex 用 Bash 跑完命令，触发一个脚本」</strong>，写进项目根的 <code>.codex/hooks.json</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/usr/bin/python3 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(git rev-parse --show-toplevel)/.codex/hooks/post_tool_use.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;timeout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;statusMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Reviewing Bash output&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>别被嵌套吓到，它就三层，对着拆一下你立刻懂：</p><ol><li><strong><code>&quot;PostToolUse&quot;</code></strong>——挂哪个<strong>事件</strong>（这里：工具产出结果后）。</li><li><strong><code>&quot;matcher&quot;: &quot;Bash&quot;</code></strong>——<strong>收窄到哪个工具</strong>才触发（这里：只在 <code>Bash</code> 之后）。</li><li><strong>里层的 <code>hooks</code> 数组</strong>——真正要跑的<strong>动作</strong>：<code>&quot;type&quot;: &quot;command&quot;</code> 表示跑一条 shell 命令，<code>&quot;command&quot;</code> 就是那条命令。</li></ol><p>这里有几个 Codex 特有的、<strong>最容易想当然写错的默认值</strong>，挑出来钉死：</p><ul><li><strong><code>timeout</code> 的单位是「秒」，不是毫秒</strong>；省略不写时，默认是 <strong>600 秒</strong>。</li><li><strong>目前只有 <code>type: &quot;command&quot;</code> 的处理器真会跑</strong>；<code>prompt</code> 和 <code>agent</code> 类型「能被解析但会被跳过」；<code>async: true</code> 的也会被跳过（异步钩子还没支持）。</li><li><strong>同一事件上有多个匹配钩子时，它们是并发启动的</strong>——你的拦截钩子不会阻止另一个同事件匹配钩子同时跑。这是 Codex 和 Claude Code 的又一差异点：写 <code>PreToolUse</code> 拦截逻辑时，别以为它能「先跑完再决定其他钩子跑不跑」。</li><li><strong>命令的工作目录是会话的 <code>cwd</code></strong>。所以仓库内的钩子，官方建议<strong>别用 <code>.codex/hooks/...</code> 这种相对路径</strong>，因为 Codex 可能从子目录启动——用 <code>$(git rev-parse --show-toplevel)</code> 从 git 根算绝对路径才稳（上面例子就是这么写的）。</li><li><strong>Windows 想用不同命令</strong>，加一个可选的 <code>command_windows</code>（TOML 里也可写 <code>commandWindows</code>）字段覆盖。</li></ul><blockquote><p>⚠️ 这里要把和 Claude Code 的几个<strong>差异</strong>点透，免得你拿那边的笔记照抄踩坑：Codex 钩子的 <code>timeout</code> 是<strong>秒</strong>（Claude Code 是毫秒），没有 <code>$CLAUDE_PROJECT_DIR</code> 这个变量（用 git 根代替），也<strong>没有 <code>disableAllHooks</code> 这种一键全关开关</strong>（要关就改 <code>[features] hooks = false</code>）。同名不同义，别混。</p></blockquote><p>写成 <code>config.toml</code> 内联 TOML 是等价的，长这样：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PostToolUse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">matcher = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^Bash$&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PostToolUse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">command = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/usr/bin/python3 &quot;$(git rev-parse --show-toplevel)/.codex/hooks/post_tool_use.py&quot;&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">timeout = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">statusMessage = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Reviewing Bash output&quot;</span></span></code></pre></div><h3 id="matcher-让钩子「只在该触发的时候触发」" tabindex="-1"><code>matcher</code>：让钩子「只在该触发的时候触发」 <a class="header-anchor" href="#matcher-让钩子「只在该触发的时候触发」" aria-label="Permalink to &quot;\`matcher\`：让钩子「只在该触发的时候触发」&quot;">​</a></h3><p><code>matcher</code> 是钩子配置里<strong>最该搞懂的一个字段</strong>。一句话：<strong>没有它，钩子会在那个事件的「每一次」都触发；有了它，你能把范围收窄</strong>。</p><p><strong>类比：还是楼道那盏感应灯——你得圈定它的感应范围。</strong> 灯如果整层楼都感应，那隔壁单元有人走动它也亮，纯浪费电。你得把感应区圈在「<strong>只有自家门口</strong>那段过道」。<code>matcher</code> 干的就是这个「圈定范围」的活。</p><p>Codex 的 <code>matcher</code> 和 Claude Code 有个关键区别：<strong>它是一个正则表达式（regex）字符串</strong>。对工具类事件（<code>PreToolUse</code>/<code>PostToolUse</code>），它匹配的是<strong>工具名</strong>。写法看这张表：</p><table tabindex="0"><thead><tr><th>你写的 matcher</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td><code>&quot;Bash&quot;</code></td><td>匹配 Bash 工具</td><td>只在跑 Bash 命令时触发</td></tr><tr><td><code>&quot;^apply_patch$&quot;</code></td><td>用正则精确匹配</td><td>只在改文件时触发</td></tr><tr><td><code>&quot;Edit|Write&quot;</code></td><td>改文件的别名（<code>|</code> 是正则的「或」）</td><td>只在编辑 / 写文件之后触发</td></tr><tr><td><code>&quot;mcp__filesystem__.*&quot;</code></td><td>正则匹配一批 MCP 工具</td><td>这个 MCP server 的所有工具</td></tr><tr><td><code>&quot;*&quot;</code> / <code>&quot;&quot;</code> / 省略</td><td><strong>匹配所有</strong></td><td>该事件每次发生都跑</td></tr></tbody></table><p>几个<strong>新手最容易栽的坑</strong>：</p><ul><li><strong>改文件的工具名是 <code>apply_patch</code>，不是 <code>Edit</code>/<code>Write</code></strong>。Codex 改文件走的是 <code>apply_patch</code> 机制——你在 <code>matcher</code> 里可以用 <code>Edit</code>、<code>Write</code>、<code>apply_patch</code> 任意一个来匹配它（互为别名），但钩子收到的 stdin 里 <code>tool_name</code> 始终报的是 <code>&quot;apply_patch&quot;</code>。想在脚本里判断工具名，记得认 <code>apply_patch</code>。</li><li><strong>不是所有事件都认 <code>matcher</code></strong>。<code>UserPromptSubmit</code> 和 <code>Stop</code> 这俩<strong>无视 <code>matcher</code></strong>，你写了也被忽略，因为它们没有「工具名」这种东西可筛，总是每次都触发。<code>SubagentStop</code> 则不同，它的 <code>matcher</code> 作用于 <code>agent_type</code>，是支持的——别把 <code>SubagentStop</code> 和 <code>Stop</code> 混在一起理解。<code>SessionStart</code> 的 <code>matcher</code> 匹配的不是工具名，而是「会话怎么起来的」（<code>startup</code>/<code>resume</code>/<code>clear</code>/<code>compact</code>）。</li><li><strong><code>PreToolUse</code> 拦不住所有命令</strong>。官方说得很直白：它是「护栏，不是密不透风的强制边界」——目前只拦得住简单的 shell 调用、<code>apply_patch</code> 和 MCP 工具，更复杂的 shell（走 <code>unified_exec</code> 的）、<code>WebSearch</code> 这类它拦不到。所以<strong>别拿 <code>PreToolUse</code> 当唯一的安全防线</strong>，它是「多一道闸」，不是「万能墙」。</li></ul><blockquote><p>💡 一句话总结：钩子写进 <code>~/.codex/</code> 或项目 <code>.codex/</code> 的 <code>hooks.json</code> / <code>config.toml</code>，配置就三层——<strong>事件、matcher、动作</strong>；记死 Codex 特有的几条：<code>timeout</code> 单位是<strong>秒</strong>（默认 600）、改文件工具名是 <strong><code>apply_patch</code></strong>、<code>matcher</code> 是<strong>正则</strong>、用 git 根拼路径。</p></blockquote><hr><h2 id="_05-信任机制-为什么你的新钩子「默认不跑」" tabindex="-1">05 信任机制：为什么你的新钩子「默认不跑」 <a class="header-anchor" href="#_05-信任机制-为什么你的新钩子「默认不跑」" aria-label="Permalink to &quot;05 信任机制：为什么你的新钩子「默认不跑」&quot;">​</a></h2><p>这一节是 Codex 钩子区别于 Claude Code 的<strong>最大不同</strong>，也是新手第一次配钩子最容易懵的地方：<strong>你明明把钩子写好了，它却没跑——因为 Codex 默认不信任它。</strong></p><p>先说为什么有这道关。钩子是<strong>用你完整用户权限跑的 shell 脚本</strong>，能删你能删的任何文件、能联网把东西发出去。要是从网上抄一段插件、或者克隆一个仓库，里头夹带的钩子<strong>自动就跑</strong>，那等于谁都能在你机器上偷偷执行代码。所以 Codex 加了一道「<strong>过目签字</strong>」的关卡。</p><p><strong>类比：新装的 App 第一次要权限，得你亲手点「允许」。</strong> 你装个新 App，它想用相机、想发通知，系统不会替你默认放开，而是弹一下「是否允许」——你不点，它就用不了。Codex 的钩子信任就是这个机制：<strong>非托管的命令钩子，必须你先「审一眼、再信任」，否则它一直被跳过、不会跑。</strong></p><p>官方把规则定得很清楚：</p><ul><li>Codex 按钩子定义的<strong>当前哈希</strong>记录信任。所以<strong>新加的、或改动过的钩子</strong>都会被标成「待审」，<strong>在你信任之前一律跳过</strong>。改一个字符，哈希变了，就得重新信任。</li><li>用 <strong><code>/hooks</code></strong> 这个命令来查看钩子来源、审阅新增 / 变动的钩子、信任它们、或禁用某个非托管钩子。</li><li>如果启动时有钩子待审，Codex 会打印一条警告，提示你去开 <code>/hooks</code>。</li></ul><p>所以<strong>第一次配完钩子的标准动作</strong>是：配好 → 启动 Codex（可能看到「有钩子待审」的警告）→ 敲 <code>/hooks</code> → 找到你那条、确认命令没问题 → 信任它。之后它才会真正触发。我第一次配格式化钩子时，就是没注意这茬，改完文件死活没格式化，还以为 <code>matcher</code> 写错了，查了半天才反应过来——<strong>根本没信任，它压根没跑</strong>。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/hooks</span></span></code></pre></div><p><strong>预期</strong>：弹出钩子浏览器，列出所有来源的钩子，标明哪些是「待审（review）」、哪些已信任、哪些是「托管（managed）」。托管钩子（来自系统、MDM、企业 <code>requirements.toml</code> 等）是「按策略信任」的，<strong>你在这个界面里禁不掉</strong>——那是管理员焊死的。</p><p>还有个一次性的出口：如果你在 CI 这种「钩子来源本来就在 Codex 之外审过了」的场景，可以用 <code>--dangerously-bypass-hook-trust</code> 启动，跳过这次的信任校验。名字里那个 <code>dangerously</code> 就是提醒你——<strong>别在日常本机随手用，它等于把这道安全关卡关了</strong>。</p><blockquote><p>⚠️ 回到安全主线（第 <a href="./16-security">16</a> 篇）：<strong>加任何钩子前先在 <code>/hooks</code> 里把它的命令逐行审一遍</strong>，尤其别从来路不明的插件或仓库里抄整段钩子就信任。信任机制是 Codex 替你设的最后一道防线，但点「信任」那一下的判断，还是你自己的。</p></blockquote><blockquote><p>💡 一句话总结：Codex 钩子有<strong>信任机制</strong>——非托管钩子默认不跑，新增 / 改动后都要你在 <code>/hooks</code> 里「审一眼再信任」（按哈希记录，改一字符就要重审）；这是它和 Claude Code 最大的不同，也是新手「钩子不触发」的头号原因。</p></blockquote><hr><h2 id="_06-钩子和-codex-怎么对话-stdin、退出码、stdout" tabindex="-1">06 钩子和 Codex 怎么对话：stdin、退出码、stdout <a class="header-anchor" href="#_06-钩子和-codex-怎么对话-stdin、退出码、stdout" aria-label="Permalink to &quot;06 钩子和 Codex 怎么对话：stdin、退出码、stdout&quot;">​</a></h2><p>这一节是<strong>看懂一切的钥匙</strong>。前面的脚本怎么拿到命令内容？钩子怎么「拦」住一条命令？答案全在这套「对话机制」里，和第 <a href="./16-security">16</a> 篇讲的输入输出是一脉相承的。</p><p>机制本身极简单，三条管道：<strong>Codex 把事件数据从 <code>stdin</code> 喂给你的脚本 → 脚本干活 → 脚本用「退出码 + stdout」告诉 Codex 接下来怎么办</strong>。</p><h3 id="输入-codex-从-stdin-递给你一坨-json" tabindex="-1">输入：Codex 从 stdin 递给你一坨 JSON <a class="header-anchor" href="#输入-codex-从-stdin-递给你一坨-json" aria-label="Permalink to &quot;输入：Codex 从 stdin 递给你一坨 JSON&quot;">​</a></h3><p>事件一触发，Codex 会把<strong>这个事件的相关数据</strong>作为一段 JSON 从标准输入（stdin）塞给你的命令。每个钩子都会拿到一些共有字段：</p><table tabindex="0"><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>session_id</code></td><td>当前会话 id</td></tr><tr><td><code>cwd</code></td><td>会话的工作目录</td></tr><tr><td><code>hook_event_name</code></td><td>当前事件名</td></tr><tr><td><code>transcript_path</code></td><td>会话记录文件路径（可能为 null）</td></tr><tr><td><code>model</code></td><td>当前活跃的模型 slug（Codex 特有扩展，需要按模型做差异处理时用）</td></tr><tr><td><code>permission_mode</code></td><td>当前权限模式（多数事件都有，部分特殊事件可能缺省）</td></tr></tbody></table><p>工具类事件还会多带 <code>tool_name</code>（比如 <code>&quot;Bash&quot;</code>、<code>&quot;apply_patch&quot;</code>）、<code>tool_input</code>（工具的具体输入，<code>Bash</code> 和 <code>apply_patch</code> 用 <code>tool_input.command</code> 装命令）。比如 Codex 要跑 Bash 命令时，<code>PreToolUse</code> 钩子收到的大概长这样：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;session_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;abc123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cwd&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/Users/sarah/myproject&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hook_event_name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tool_name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tool_input&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rm -rf /tmp/x&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>看到了吧——<strong>Codex 要干什么、用哪个工具、参数是什么，全在里头</strong>。你的脚本从这坨 JSON 里把 <code>tool_input.command</code> 抠出来，就能判断这条命令该不该拦。</p><h3 id="输出-用「退出码」给-codex-下指令" tabindex="-1">输出：用「退出码」给 Codex 下指令 <a class="header-anchor" href="#输出-用「退出码」给-codex-下指令" aria-label="Permalink to &quot;输出：用「退出码」给 Codex 下指令&quot;">​</a></h3><p>脚本干完活，靠<strong>退出码</strong>（exit code）给 Codex 下最基础的指令：</p><table tabindex="0"><thead><tr><th>退出码</th><th>含义</th><th>效果</th></tr></thead><tbody><tr><td><strong><code>0</code></strong></td><td>没意见，正常走</td><td>操作继续；没有任何输出也当成功</td></tr><tr><td><strong><code>2</code></strong></td><td><strong>特殊信号</strong></td><td>阻止 / 续轮等（不同事件含义不同，见下）</td></tr><tr><td>其他</td><td>官方未明确定义</td><td>以实测为准</td></tr></tbody></table><p><strong>重点是 <code>exit 2</code></strong>，但它在不同事件上含义不一样，这点 Codex 和 Claude Code 不太一样，务必对清楚：</p><ul><li>在 <strong><code>PreToolUse</code> / <code>UserPromptSubmit</code></strong> 上：<code>exit 2</code> + 把理由写到 <strong>stderr</strong> = <strong>拦住</strong>这次操作 / 这条提示。</li><li>在 <strong><code>PostToolUse</code> / <code>Stop</code> / <code>SubagentStop</code></strong> 上：<code>exit 2</code> + stderr 不是「拦」（工具早跑完了），而是把那段理由作为反馈塞回去——<code>PostToolUse</code> 用它替换工具结果让模型重看，<code>Stop</code> 用它让 Codex 再续一轮（详细行为见第 03 节）。</li></ul><p>换句话说，<strong>「拦得住」的只有 <code>Pre</code> 类事件</strong>；<code>Post</code>/<code>Stop</code> 收到 <code>exit 2</code> 是「退回 / 续轮」，撤不回已经发生的事。</p><h3 id="进阶-用-stdout-返回-json-做更细的控制" tabindex="-1">进阶：用 stdout 返回 JSON，做更细的控制 <a class="header-anchor" href="#进阶-用-stdout-返回-json-做更细的控制" aria-label="Permalink to &quot;进阶：用 stdout 返回 JSON，做更细的控制&quot;">​</a></h3><p>退出码只能表达粗信号。想要更细的控制（拦的同时说明原因、往上下文注入信息），就 <code>exit 0</code> 然后往 <strong>stdout</strong> 打印一段 JSON。举两个最常用的：</p><p><strong>① <code>PreToolUse</code> 想拦截、并说明理由</strong>——返回 <code>permissionDecision: &quot;deny&quot;</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hookSpecificOutput&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;hookEventName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;permissionDecision&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;permissionDecisionReason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;这条命令会动生产库，已拦截&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p>Codex 也接受一种更老的写法 <code>{&quot;decision&quot;: &quot;block&quot;, &quot;reason&quot;: &quot;...&quot;}</code>，效果一样。两种都行，新写法语义更清楚。</p></blockquote><p><strong>② <code>SessionStart</code> 想往上下文里塞点信息</strong>——返回 <code>additionalContext</code>，那段文字会作为「额外的开发者上下文」喂给模型：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hookSpecificOutput&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;hookEventName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SessionStart&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;additionalContext&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;编辑前先读一遍本仓库的代码规范。&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>这里有几个 Codex 的<strong>输出约定坑</strong>得提醒：</p><ul><li><strong><code>PreToolUse</code> 往 stdout 打纯文本会被忽略</strong>——想注入上下文得用 JSON 里的 <code>additionalContext</code>，想拦得用 <code>permissionDecision</code> 或 <code>exit 2</code>，光 <code>echo</code> 一句没用。</li><li><strong><code>SessionStart</code>、<code>UserPromptSubmit</code> 的 stdout 纯文本会被当成上下文</strong>喂进去（这俩特殊）；但 <strong><code>Stop</code>、<code>SubagentStop</code> 必须输出 JSON</strong>，纯文本对它们是非法的。</li><li><strong><code>PreToolUse</code> 不支持 <code>continue</code>/<code>stopReason</code> 这些字段</strong>，你硬返回，Codex 会把这次钩子标记为失败、报错、然后<strong>照样继续</strong>那次工具调用——所以别在 <code>PreToolUse</code> 里指望用 <code>continue: false</code> 来阻止。</li></ul><blockquote><p>💡 一句话总结：钩子和 Codex 靠三条管道对话——<strong>stdin 喂 JSON、退出码下指令、stdout 的 JSON 做精细控制</strong>；记死 Codex 特有的：<code>exit 2</code> 在 <code>Pre</code> 类是「拦」、在 <code>Post</code>/<code>Stop</code> 是「退回 / 续轮」；改文件认 <code>apply_patch</code>；<code>PreToolUse</code> 的纯文本 stdout 会被忽略。</p></blockquote><hr><h2 id="_07-两个能直接抄走的真实例子" tabindex="-1">07 两个能直接抄走的真实例子 <a class="header-anchor" href="#_07-两个能直接抄走的真实例子" aria-label="Permalink to &quot;07 两个能直接抄走的真实例子&quot;">​</a></h2><p>理论够了，上两个最常用、你也能直接抄的钩子。每个都标清楚「挂哪个事件、收窄到哪、配在哪个文件」。</p><h3 id="例子一-改完文件自动格式化-posttooluse" tabindex="-1">例子一：改完文件自动格式化（<code>PostToolUse</code>） <a class="header-anchor" href="#例子一-改完文件自动格式化-posttooluse" aria-label="Permalink to &quot;例子一：改完文件自动格式化（\`PostToolUse\`）&quot;">​</a></h3><p>就是治好开头那「漏敲格式化」毛病的那个，最实用、零风险，<strong>强烈建议每个项目都配上</strong>。</p><p><strong>第一步</strong>，把格式化逻辑写进脚本 <code>.codex/hooks/format.py</code>（从 stdin 读 JSON、抠出改的文件路径、丢给格式化工具）。这里给个最小骨架：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/usr/bin/env python3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json, subprocess, sys</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json.load(sys.stdin)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># apply_patch 的 tool_input.command 里是补丁内容；实际项目里</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 通常直接对整个工作区跑格式化，简单稳妥：</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subprocess.run([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ruff&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;format&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span></code></pre></div><p><strong>第二步</strong>，在项目根的 <code>.codex/hooks.json</code> 里注册它，挂到 <code>apply_patch</code> 的 <code>PostToolUse</code> 上（<code>matcher</code> 用 <code>Edit|Write</code> 或 <code>apply_patch</code> 都行，它们是别名）：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Edit|Write&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/usr/bin/python3 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(git rev-parse --show-toplevel)/.codex/hooks/format.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;timeout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;statusMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;格式化改动文件&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>第三步</strong>（关键，别漏），启动 Codex 后敲 <code>/hooks</code> <strong>信任这个钩子</strong>——没这一步它不跑（第 05 节讲过）。从此 Codex 每改完文件，格式化自动跑。把 <code>ruff format</code> 换成 <code>prettier --write .</code>、<code>gofmt -w .</code>、<code>black .</code> 都是一个套路。</p><h3 id="例子二-拦掉危险命令-pretooluse-脚本" tabindex="-1">例子二：拦掉危险命令（<code>PreToolUse</code> + 脚本） <a class="header-anchor" href="#例子二-拦掉危险命令-pretooluse-脚本" aria-label="Permalink to &quot;例子二：拦掉危险命令（\`PreToolUse\` + 脚本）&quot;">​</a></h3><p>这个用上了「<code>exit 2</code> 拦截」。命令复杂时，把逻辑写进<strong>单独脚本</strong>比硬塞进 JSON 清爽得多。</p><p><strong>第一步</strong>，把脚本存到 <code>.codex/hooks/block-dangerous.py</code>：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/usr/bin/env python3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json, sys</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json.load(sys.stdin)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">command </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data.get(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_input&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {}).get(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;rm -rf&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> command:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Blocked: 检测到 rm -rf，已拦截&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sys.stderr)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 写 stderr，反馈给 Codex</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sys.exit(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                                                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># exit 2 = 拦住这次调用</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sys.exit(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 其余命令放行，走正常审批流程</span></span></code></pre></div><p><strong>第二步</strong>，在 <code>.codex/hooks.json</code> 里注册，挂到 <code>Bash</code> 的 <code>PreToolUse</code> 上：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/usr/bin/python3 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(git rev-parse --show-toplevel)/.codex/hooks/block-dangerous.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;statusMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;检查 Bash 命令&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>第三步</strong>，<code>/hooks</code> 信任它。之后 Codex 一旦想跑带 <code>rm -rf</code> 的 Bash 命令，这个钩子就 <code>exit 2</code> 把它拦下，并把理由反馈给模型。</p><blockquote><p>⚠️ 拦危险命令，<strong>规则和钩子其实都能干</strong>——<code>forbidden</code> 规则更轻量（一行声明，无需写脚本），钩子更灵活（能跑任意逻辑、读完整 JSON 再决定）。简单的命令前缀禁令，优先用规则；要按命令内容做复杂判断（比如「只拦删特定目录的 <code>rm</code>」），才上钩子。<strong>别两边都配同一条，容易自己绕晕。</strong></p></blockquote><p>这两个例子对比着记：</p><table tabindex="0"><thead><tr><th>维度</th><th>例子一：自动格式化</th><th>例子二：拦危险命令</th></tr></thead><tbody><tr><td>挂的事件</td><td><code>PostToolUse</code>（事后）</td><td><code>PreToolUse</code>（事前）</td></tr><tr><td>matcher</td><td><code>Edit|Write</code>（= <code>apply_patch</code>）</td><td><code>Bash</code></td></tr><tr><td>核心机制</td><td>跑完就格式化，不阻止</td><td><code>exit 2</code> 拦住</td></tr><tr><td>风险</td><td>零风险，人人可配</td><td>改错可能误拦正常命令</td></tr><tr><td>也能用规则做吗</td><td>不能（规则只管命令准入）</td><td><strong>能</strong>，简单禁令优先用规则</td></tr></tbody></table><blockquote><p>💡 一句话总结：两个钩子按风险递增——<strong>格式化（<code>PostToolUse</code>，零风险，建议人人配）、拦命令（<code>PreToolUse</code> + 脚本 + <code>exit 2</code>）</strong>；脚本路径用 <code>$(git rev-parse --show-toplevel)</code> 拼最稳，<strong>配完都要 <code>/hooks</code> 信任</strong>才生效；简单命令禁令优先用规则而非钩子。</p></blockquote><hr><h2 id="_08-动手-5-分钟配一个钩子并亲眼看它触发" tabindex="-1">08 动手：5 分钟配一个钩子并亲眼看它触发 <a class="header-anchor" href="#_08-动手-5-分钟配一个钩子并亲眼看它触发" aria-label="Permalink to &quot;08 动手：5 分钟配一个钩子并亲眼看它触发&quot;">​</a></h2><p>光看不练记不住。下面带你配一个<strong>最安全、最容易看到效果</strong>的钩子——<strong>每次 Codex 跑完 Bash 命令，就把这条命令记进一个日志文件</strong>。全程不动你任何代码，纯加一段配置，跑完能亲眼验证。</p><blockquote><p>⚠️ 平台说明：脚本用 Python 写，Mac / Linux / Windows 都能跑（Windows 上把 <code>python3</code> 换成 <code>py -3</code>，或用 <code>command_windows</code> 字段）。下面以 Mac / Linux 为例。</p></blockquote><p><strong>第一步：找个练手目录，建钩子脚本和配置</strong></p><p>随便找个空目录（别在重要项目里练），在里头建两个文件。脚本 <code>.codex/hooks/log-bash.py</code> ：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/usr/bin/env python3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json, sys, os</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> json.load(sys.stdin)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">command </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data.get(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_input&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {}).get(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">log </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> os.path.expanduser(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/codex-bash-log.txt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> open</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(log, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    f.write(command </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>配置 <code>.codex/hooks.json</code> ：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/usr/bin/python3 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(git rev-parse --show-toplevel)/.codex/hooks/log-bash.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;statusMessage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;记录 Bash 命令&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p>注意：<code>command</code> 里用了 <code>git rev-parse --show-toplevel</code> 拼路径，<strong>这个练手目录得先 <code>git init</code> 一下</strong>，不然这条命令找不到 git 根。嫌麻烦的话，把它换成脚本的绝对路径也行。</p></blockquote><p><strong>第二步：进这个目录启动 Codex，信任钩子</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span></span></code></pre></div><p>进去后敲 <code>/hooks</code> ：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/hooks</span></span></code></pre></div><p><strong>预期</strong>：弹出钩子浏览器，你会看到刚配的 <code>PostToolUse</code> 钩子，<strong>大概率标着「待审 / review」</strong>。选中它、确认命令无误后<strong>信任它</strong>（第 05 节讲的关键一步）。看到它从「待审」变成已信任 = 这钩子现在会真正触发了。</p><p><strong>第三步：让 Codex 跑条 Bash 命令，触发钩子</strong></p><p>回到对话，让它跑个无害的命令：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>帮我用 ls 看一下当前目录有哪些文件</span></span></code></pre></div><p>它会调用 <code>Bash</code> 工具跑 <code>ls</code>。<strong>这一跑，<code>PostToolUse</code> 钩子就该触发了。</strong>（钩子成功执行时通常是「静默」的，终端不会特意提示——这是正常的。）</p><p><strong>第四步：验证钩子真跑了——查日志文件</strong></p><p>新开一个终端，看日志：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/codex-bash-log.txt</span></span></code></pre></div><p><strong>预期</strong>：文件里出现了刚才那条 <code>ls</code> 命令（以及这个会话里 Codex 跑过的其他 Bash 命令）。<strong>看到命令被记下来了 = 钩子真的在每次 Bash 后自动触发了</strong>，你这条「自动化规则」立起来了。</p><p><strong>第五步：清理（可选）</strong></p><p>练完拆掉很简单：如果这个练习目录里只有这一条钩子，直接删掉 <code>.codex/hooks.json</code>，再用 <code>rm ~/codex-bash-log.txt</code> 删掉日志文件。若你还有别的钩子，就不要删整个文件，只删这条练习钩子对应的 <code>PostToolUse</code> / <code>matcher: &quot;Bash&quot;</code> 配置块，并确保剩下的 <code>hooks.json</code> 仍是合法 JSON。不要把文件内容清空；空文件会被当成非法 JSON 解析，至少要保留 <code>{ &quot;hooks&quot;: {} }</code> 这种合法空配置。</p><p>跑通这五步，你就把「<strong>写配置 → <code>/hooks</code> 信任 → 触发事件 → 验证副作用</strong>」这条完整链路亲手走了一遍。<strong>以后配任何钩子，本质都是这套流程</strong>，无非换个事件、换个 matcher、换个脚本——但「<code>/hooks</code> 信任」这一步，Codex 这边千万别漏。</p><blockquote><p>💡 一句话总结：练手就配「记录 Bash 命令」这种零风险钩子最稳——<strong>写进 <code>.codex/hooks.json</code>、<code>/hooks</code> 信任它、让 Codex 跑命令触发、查日志验证</strong>；Codex 比 Claude Code 多了「信任」这关，亲眼走一遍才不会以后栽在它上面。</p></blockquote><hr><h2 id="_09-钩子不触发-报错了-怎么查" tabindex="-1">09 钩子不触发 / 报错了，怎么查 <a class="header-anchor" href="#_09-钩子不触发-报错了-怎么查" aria-label="Permalink to &quot;09 钩子不触发 / 报错了，怎么查&quot;">​</a></h2><p>钩子配好了不灵，是新手最常见的卡点。别瞎猜，按下面这个顺序查，基本都能定位。我把它整理成「症状 → 怎么查」：</p><table tabindex="0"><thead><tr><th>症状</th><th>最可能的原因 / 怎么查</th></tr></thead><tbody><tr><td><strong>钩子压根不触发</strong></td><td>① <strong>是不是没在 <code>/hooks</code> 里信任</strong>？这是 Codex 头号坑（第 05 节）；② 改过钩子后哈希变了、又得重新信任；③ 事件 / matcher 选错了</td></tr><tr><td><strong><code>/hooks</code> 里根本没有我配的钩子</strong></td><td>① JSON 格式错了（<strong>不允许尾逗号、不允许注释</strong>）；② 文件位置 / 文件名错了（<code>hooks.json</code> 或 <code>config.toml</code> 内联 <code>[hooks]</code>）；③ 同一层同时写了两种、被合并并警告；④ 改完重启一次</td></tr><tr><td><strong>报 <code>command not found</code> / 脚本找不到</strong></td><td>路径不对。命令工作目录是会话 <code>cwd</code>，<strong>用 <code>$(git rev-parse --show-toplevel)</code> 拼绝对路径</strong>，别用 <code>.codex/hooks/...</code> 相对路径</td></tr><tr><td><strong>想拦却没拦住</strong></td><td>① 八成挂错事件了（拦操作要用 <code>PreToolUse</code>，<code>PostToolUse</code> 拦不住）；② <code>PreToolUse</code> 本就拦不住复杂 shell / <code>WebSearch</code>（第 04 节）；③ 拦截要 <code>exit 2</code> 或 <code>permissionDecision: &quot;deny&quot;</code>，光打印文本没用</td></tr><tr><td><strong>钩子超时被杀</strong></td><td><code>timeout</code> <strong>单位是秒</strong>（默认 600），别按毫秒理解；长任务适当调大</td></tr></tbody></table><p>两个<strong>最实用的排查手段</strong>单独拎出来：</p><p><strong>① 手动喂假数据测脚本</strong>。不用真在 Codex 里触发，自己造一段 JSON 管道喂给脚本，看它退出码对不对：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;tool_name&quot;:&quot;Bash&quot;,&quot;tool_input&quot;:{&quot;command&quot;:&quot;rm -rf /tmp/x&quot;}}&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .codex/hooks/block-dangerous.py</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> $?</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 看退出码：拦截脚本这里应该输出 2</span></span></code></pre></div><p>这是调拦截钩子的标准动作——<strong>先把脚本单独喂熟了，再挂到 Codex 上</strong>，省得在真实会话里反复试。</p><p><strong>② 用 <code>codex execpolicy check</code> 验规则</strong>（规则那边的排查利器，第 02 节讲过）。规则没按预期放行 / 拦截时，拿命令试一下它到底怎么判，别靠猜。</p><p>还有一条值得知道：<strong>想临时全关钩子</strong>，Codex 没有 Claude Code 那种 <code>disableAllHooks</code> 开关，而是改 <code>config.toml</code>：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">features</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hooks = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><blockquote><p>💡 一句话总结：钩子不灵，<strong>第一件事先查「是不是没在 <code>/hooks</code> 信任」</strong>（Codex 独有的头号坑），再查 matcher / 事件选对没、用 git 根拼路径没；想拦没拦住先看是不是挂错成 <code>PostToolUse</code>、或没用 <code>exit 2</code>；规则不灵用 <code>execpolicy check</code> 验。</p></blockquote><hr><h2 id="_10-小结" tabindex="-1">10 小结 <a class="header-anchor" href="#_10-小结" aria-label="Permalink to &quot;10 小结&quot;">​</a></h2><p>这一篇把规则和钩子从「是什么」一路讲到「怎么配、怎么调」——<strong>它俩是给 Codex 装的「闸门」和「扳机」：规则管哪些命令能在沙箱外跑，钩子在生命周期固定点自动替你开火，把 AGENTS.md 里的请求升级成必然兑现的保证。</strong></p><p>把核心串起来回顾：</p><table tabindex="0"><thead><tr><th>你想干的事</th><th>用什么</th><th>关键点</th></tr></thead><tbody><tr><td>让某条命令永远放行 / 禁掉</td><td>规则 <code>prefix_rule</code></td><td><code>allow</code>/<code>prompt</code>/<code>forbidden</code>，<strong>最严的赢</strong>；改完 <code>execpolicy check</code> 验</td></tr><tr><td>选对钩子挂载时机</td><td>认生命周期事件</td><td><code>Pre</code> 能拦、<code>Post</code> 补刀、<code>Stop</code> 能续轮、<code>SessionStart</code> 开场</td></tr><tr><td>写一个钩子</td><td>配进 <code>hooks.json</code> / <code>config.toml</code></td><td>三层：事件 + <code>matcher</code>（正则）+ 动作；<code>timeout</code> 单位<strong>秒</strong></td></tr><tr><td>让新钩子真的跑起来</td><td><code>/hooks</code> 信任</td><td><strong>Codex 独有</strong>：非托管钩子默认不跑，改一字符要重审</td></tr><tr><td>让钩子和 Codex 对话</td><td>stdin / 退出码 / stdout</td><td><code>exit 2</code> 在 <code>Pre</code> 是拦、在 <code>Post</code>/<code>Stop</code> 是退回 / 续轮</td></tr><tr><td>钩子不灵了</td><td>按顺序排查</td><td><strong>先查信任</strong>、再查 matcher / 事件 / 路径</td></tr></tbody></table><p><strong>你现在应该能：</strong> 说清规则和钩子各管什么、跟「写进 AGENTS.md 的请求」差在哪个根本点（保证 vs 请求）；写一条 <code>prefix_rule</code> 给命令立规矩并用 <code>execpolicy check</code> 验；知道 <code>PreToolUse</code>/<code>PostToolUse</code>/<code>Stop</code>/<code>SessionStart</code> 各挂在 Codex 干活流程的哪一步；照模板往 <code>hooks.json</code> 写一个带 <code>matcher</code> 的钩子，并记得<strong>去 <code>/hooks</code> 信任它</strong>；看懂钩子靠 stdin / 退出码 / stdout 跟 Codex 对话，且分得清 Codex 和 Claude Code 那几个反直觉差异（<code>timeout</code> 秒、<code>apply_patch</code>、<code>block</code> 是续轮、信任机制）。<strong>开头那「每周漏敲十几次格式化」的苦差，到这儿你已经有能力用一个钩子永久解决了。</strong></p><blockquote><p>💡 一句话总结：规则是「闸门」、钩子是「扳机」，两者配合把 AGENTS.md 的请求升级成必然兑现的保证；Codex 独有的信任机制、并发钩子、秒制 timeout 和 <code>apply_patch</code> 命名，是和 Claude Code 最容易踩坑的几个差异点。</p></blockquote><hr><p>下一篇 <strong>〔<a href="./25-worktrees">25 Worktrees 并行隔离</a>〕</strong>——这一路你都是「一个 Codex 干一件事」。但真到了开发节奏快起来的时候，你会想：<strong>能不能让一个 Codex 修 bug、另一个同时做新功能，俩互不踩脚？</strong> 直接开两个会话改同一个仓库，分分钟把彼此的改动覆盖掉。下一篇聊 Git worktree 这个老牌但极好用的招——给每条任务一个独立工作区，让多个 Codex 真正并行、各扫门前雪。想想看：你现在要同时推进两件事，是不是只能干完一件再开下一件？</p>`,197)])])}const u=t(d,[["render",p]]);export{E as __pageData,u as default};
