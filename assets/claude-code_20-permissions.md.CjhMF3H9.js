import{_ as t,o as e,c as o,a2 as d}from"./chunks/framework.BWuWLRhz.js";const a="/assets/20-permission-spectrum@2x.DJ-u_mDB.png",k=JSON.parse('{"title":"20 · 权限配置：放多松、收多紧，你说了算","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"claude-code/20-permissions.md","filePath":"claude-code/20-permissions.md"}'),i={name:"claude-code/20-permissions.md"};function n(p,s,c,l,h,r){return e(),o("div",null,[...s[0]||(s[0]=[d('<h1 id="_20-·-权限配置-放多松、收多紧-你说了算" tabindex="-1">20 · 权限配置：放多松、收多紧，你说了算 <a class="header-anchor" href="#_20-·-权限配置-放多松、收多紧-你说了算" aria-label="Permalink to &quot;20 · 权限配置：放多松、收多紧，你说了算&quot;">​</a></h1><p>📚 <strong>系列导航</strong>：上一篇 <a href="./19-context-management">19 上下文管理</a> 教你怎么管好 Claude 的「工作台」、别让它把记忆塞爆。这一篇换个维度——管的不是「它记得多少」，而是「它敢动多少」：从一行命令都要问你，到全自动放飞，权限这根缰绳到底攥多紧，你说了算。</p><p>「你怎么敢开 <code>--dangerously-skip-permissions</code>？这玩意儿名字里都写着 dangerous 了。」</p><p>「我在沙箱里，怕啥。它就算把整个目录 <code>rm -rf</code> 了，删的也是个一次性容器，重建一个就有了。」</p><p>这段对话我自己两头都站过：在云端的隔离容器里跑批量重构时，我全程开着最松的危险模式，删了重来毫无心理负担；可一回到本机那个装着团队真实代码的目录，我连 <code>acceptEdits</code> 都得掂量两秒。说白了，<strong>权限这事没有「对错」，只有「你在哪用」</strong>。同一个开到最松的危险模式，在隔离容器里是「提速神器」，在你装着公司生产代码的机器上就是「定时炸弹」。</p><p>前面第 07 篇我们浅尝过一次——Claude 改文件前会停下来问你。那只是冰山一角。今天把整座冰山掀开：<strong>Claude Code 有哪几档权限模式、怎么一键切、怎么用配置文件精确规定「这个能干、那个绝对不许」</strong>。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>六种权限模式各是什么、分别该在什么场景用，一张表看明白</li><li><code>Shift+Tab</code> 在模式间切换的肌肉记忆，以及启动时怎么直接指定模式</li><li>在 <code>settings.json</code> 里写 <code>allow</code> / <code>ask</code> / <code>deny</code> 规则的语法，按工具、按命令精确控权</li><li>「玩具项目放松、生产项目收紧」的两套配置模板，复制即用</li><li><code>--dangerously-skip-permissions</code> 到底什么时候才敢开，红线在哪</li></ul><hr><h2 id="_01-先搞懂-权限系统在管什么" tabindex="-1">01 先搞懂：权限系统在管什么 <a class="header-anchor" href="#_01-先搞懂-权限系统在管什么" aria-label="Permalink to &quot;01 先搞懂：权限系统在管什么&quot;">​</a></h2><p>先给结论：<strong>Claude Code 默认是个「先问后动」的实习生，权限配置就是你给这个实习生定的「行为守则」</strong>。</p><p>它把所有操作分成三类，默认待遇完全不同。官方文档里这张分类表，是理解后面一切的地基：</p><table tabindex="0"><thead><tr><th>工具类型</th><th>例子</th><th>默认要不要批准</th></tr></thead><tbody><tr><td><strong>只读</strong></td><td>读文件、<code>Grep</code> 搜索</td><td>不要，直接放行</td></tr><tr><td><strong>Bash 命令</strong></td><td>执行 shell 命令</td><td>要</td></tr><tr><td><strong>文件修改</strong></td><td><code>Edit</code> / <code>Write</code> 改文件</td><td>要</td></tr></tbody></table><p><strong>类比：实习生动手前问不问你。</strong> 一个靠谱的实习生，让他「看看这段代码」他随手就看了（只读，零风险）；但他要「改生产配置」「跑个删除命令」，正常都会先抬头问你一句「头儿，这个我能动吗？」。Claude Code 默认就是这种人——<strong>读随便读，动手前先报备</strong>。</p><p>这里有个关键认知，我自己就在这儿栽过一跤：<strong>权限是 Claude Code 这个程序强制执行的，不是靠「拜托模型自觉」</strong>。</p><p>我那会儿在 <code>CLAUDE.md</code> 里专门写了句「不要执行 git push」，以为这就锁死了，结果有一次它该 push 还是利索地 push 了——因为 <code>CLAUDE.md</code> 只是「影响它想干啥」的软提示，<strong>真正的硬约束得写在权限规则里</strong>。后来我把这条搬进 <code>deny</code>，它才老实。官方说得很直白：</p><blockquote><p>权限规则由 Claude Code 强制执行，而不是由模型强制执行。您的提示或 <code>CLAUDE.md</code> 中的说明会影响 Claude 尝试执行的操作，但它们不会改变 Claude Code 允许的操作。</p></blockquote><p>记住这条，你才知道「防线」该建在哪。</p><blockquote><p>💡 一句话总结：只读随便放、动手要批准是默认守则；<strong>真要拦死某个操作，得写权限规则，光在 <code>CLAUDE.md</code> 里嘱咐没用</strong>。</p></blockquote><hr><h2 id="_02-六种权限模式-从「步步问」到「全放开」" tabindex="-1">02 六种权限模式：从「步步问」到「全放开」 <a class="header-anchor" href="#_02-六种权限模式-从「步步问」到「全放开」" aria-label="Permalink to &quot;02 六种权限模式：从「步步问」到「全放开」&quot;">​</a></h2><p>权限模式（permission mode）控制的是一件事：<strong>Claude 在动手前暂停问你的「频率」</strong>。从「每一步都停下来等你点头」，到「啥都不问直接干」，是一条连续的光谱。</p><p><strong>类比：还是那个实习生，模式就是你给他的「自主权等级」。</strong> 新来第一天，每件事都要请示（<code>default</code>）；熟了之后，改代码不用问、但删库还是得喊你（<code>acceptEdits</code>）；你出差了完全信任他，让他自己看着办（<code>bypassPermissions</code>）。</p><p>官方一共给了六种模式。把它们的「会不会先问你」和「适用场景」整理成一张表——<strong>这是本篇最该记住的一张</strong>：</p><table tabindex="0"><thead><tr><th>模式</th><th>无需询问就能干的事</th><th>会不会先问你</th><th>最适合的场景</th></tr></thead><tbody><tr><td><code>default</code></td><td>仅只读</td><td>改文件、跑命令<strong>都问</strong></td><td>入门、敏感工作</td></tr><tr><td><code>acceptEdits</code></td><td>只读 + 文件编辑 + 常见文件系统命令（<code>mkdir</code>、<code>mv</code>、<code>cp</code> 等）</td><td>文件编辑和上述文件系统命令<strong>不问</strong>，其他 Bash 命令仍问</td><td>迭代你正在审查的代码</td></tr><tr><td><code>plan</code></td><td>仅只读（只研究、只出方案，<strong>不碰你的源码</strong>）</td><td>跟 <code>default</code> 一样的提示规则</td><td>动手改之前先探索、出计划</td></tr><tr><td><code>auto</code></td><td>所有操作，带后台分类器安全检查</td><td><strong>基本不问</strong>，越界的由分类器拦</td><td>长任务、减少打断（研究预览版）</td></tr><tr><td><code>dontAsk</code></td><td>仅预先批准的工具</td><td>不问也不停，没批准的<strong>直接拒</strong></td><td>锁死的 CI、脚本</td></tr><tr><td><code>bypassPermissions</code></td><td>所有操作，<strong>跳过一切检查</strong></td><td><strong>完全不问</strong></td><td>仅隔离容器 / VM</td></tr></tbody></table><p>几个新手最容易搞混的点，挑出来说清楚：</p><p><strong><code>plan</code>（Plan Mode，计划模式）不是「放松」，恰恰是最克制的。</strong> 它让 Claude 只读文件、只跑只读命令去摸清状况，然后给你写一份「我打算这么改」的计划，<strong>但一个字都不会动你的源码</strong>。接手任何陌生项目，第一步都建议先切 <code>plan</code> 让它通读一遍再说，比一上来就让它瞎改稳得多。</p><p><strong><code>acceptEdits</code> 是日常开发的甜点区。</strong> 它自动批准你工作目录里的文件编辑和几个常见文件系统命令（<code>mkdir</code>、<code>touch</code>、<code>rm</code>、<code>rmdir</code>、<code>mv</code>、<code>cp</code>、<code>sed</code>），但<strong>别的 shell 命令、超出工作目录的写入，照样停下来问你</strong>。等于「改代码不用一次次点同意，但危险动作还留着闸」。</p><p><strong><code>auto</code> 和 <code>bypassPermissions</code> 看着都「不问」，但安全性天差地别。</strong> <code>auto</code> 是研究预览版，背后有个独立的分类器模型在每个操作前审一遍，越界的（比如 <code>curl | bash</code>、推 <code>main</code>、删云存储）会被拦下；<code>bypassPermissions</code> 是<strong>真·裸奔</strong>，连提示注入都不防。官方写得明明白白：</p><blockquote><p><code>bypassPermissions</code> 不提供针对提示注入或意外操作的保护。对于没有提示的后台安全检查，请改为使用 auto mode。</p></blockquote><p>所以想「省心又有底线」，优先 <code>auto</code>，别一上来就 <code>bypassPermissions</code>。</p><blockquote><p>💡 一句话总结：<code>default</code> 步步问、<code>acceptEdits</code> 改码免问、<code>plan</code> 只看不动、<code>auto</code> 有分类器兜底、<code>bypassPermissions</code> 完全裸奔——<strong>记住这条从严到松的光谱，对号入座就行</strong>。</p></blockquote><p><img src="'+a+`" alt="权限模式松紧光谱：从步步问到全裸奔"></p><p>这张图把六种模式排成一条「从最严到最松」的光谱：左边绿区是 <code>plan</code> / <code>default</code>（只读、步步问，最安全），往右经过 <code>acceptEdits</code>（改码免问）、<code>auto</code>（分类器兜底），一直到右边红区的 <code>bypassPermissions</code>（完全裸奔）——颜色越往右越红，提醒你「越松越要看清自己在哪用」。</p><hr><h2 id="_03-切换模式-shift-tab-一键循环" tabindex="-1">03 切换模式：Shift+Tab 一键循环 <a class="header-anchor" href="#_03-切换模式-shift-tab-一键循环" aria-label="Permalink to &quot;03 切换模式：Shift+Tab 一键循环&quot;">​</a></h2><p>模式知道了，怎么切？<strong>最常用的就一个快捷键：<code>Shift+Tab</code></strong>。</p><p>在会话里随手按 <code>Shift+Tab</code>，会在三种模式间循环：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>default → acceptEdits → plan → （再按回到 default）</span></span></code></pre></div><p>当前是哪个模式，<strong>看状态栏</strong>就知道。比如切到 <code>acceptEdits</code> 时，状态栏会显示 <code>⏵⏵ accept edits on</code>。</p><p>注意一个官方细节，免得你按半天找不到某个模式：<strong>默认循环里只有 <code>default</code> / <code>acceptEdits</code> / <code>plan</code> 这三个</strong>。其余三个进入方式各不同：<code>auto</code> 在账户满足条件后会自动出现在循环中；<code>bypassPermissions</code> 要用 <code>--permission-mode bypassPermissions</code> 等标志启动才进入循环；<code>dontAsk</code> 永远不出现在循环里，只能用 <code>--permission-mode dontAsk</code> 启动参数设置（下面讲）。</p><p><strong>类比：手机的「响铃 / 震动 / 静音」三段切换。</strong> 你按音量键边上那个拨杆，就在这三档之间转。<code>Shift+Tab</code> 就是 Claude Code 的那个拨杆，转一圈是「步步问 / 改码免问 / 只看不动」。</p><p>如果你不想每次进来都手动切，有两个办法把模式「钉死」：</p><p><strong>办法一：启动时用参数指定</strong>（只管这一次会话）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --permission-mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plan</span></span></code></pre></div><p>把 <code>plan</code> 换成 <code>acceptEdits</code>、<code>dontAsk</code> 等任意模式名都行。<code>bypassPermissions</code> 比较特殊——<code>--permission-mode bypassPermissions</code> 和 <code>--dangerously-skip-permissions</code> 两种写法等价，但日常都用后者那个「自带警告」的名字，第 05 节细说。</p><p><strong>办法二：写进 <code>settings.json</code> 当默认</strong>（每次启动都生效）。在你项目的 <code>.claude/settings.json</code> 里：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;defaultMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acceptEdits&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p>⚠️ 一个官方明确的限制：<code>defaultMode</code> 设成 <code>&quot;auto&quot;</code> 时，<strong>项目和本地设置里的会被忽略</strong>（防止某个仓库偷偷给自己开自动模式），要默认 <code>auto</code> 得写到用户级的 <code>~/.claude/settings.json</code> 里。</p></blockquote><p>一个推荐的习惯：<strong>不在项目里钉死模式，全靠 <code>Shift+Tab</code> 手动切</strong>。因为同一个项目，有时你想让它放手干（切 <code>acceptEdits</code>），有时只想让它出个方案（切 <code>plan</code>），写死反而别扭。<code>defaultMode</code> 一般只在「这个项目就是要从严」时才设成 <code>default</code> 兜底。</p><blockquote><p>💡 一句话总结：会话里 <code>Shift+Tab</code> 在「步步问 / 改码免问 / 只看不动」三档循环，状态栏看当前档；<strong>想钉死就用 <code>--permission-mode</code> 启动参数或 <code>settings.json</code> 的 <code>defaultMode</code></strong>。</p></blockquote><hr><h2 id="_04-精细控权-allow-ask-deny-三种规则" tabindex="-1">04 精细控权：allow / ask / deny 三种规则 <a class="header-anchor" href="#_04-精细控权-allow-ask-deny-三种规则" aria-label="Permalink to &quot;04 精细控权：allow / ask / deny 三种规则&quot;">​</a></h2><p>模式是「粗调」，定个大基调。<strong>真正精确到「这条命令能跑、那条绝对不许」的「细调」，靠 <code>settings.json</code> 里的三种规则</strong>。</p><p>每条规则最终都落到三个动作之一：</p><table tabindex="0"><thead><tr><th>动作</th><th>效果</th><th>典型用途</th></tr></thead><tbody><tr><td><code>allow</code></td><td>无需审批，自动放行</td><td>低风险高频操作，如 <code>git status</code>、<code>npm run build</code></td></tr><tr><td><code>ask</code></td><td>弹提示，由你拍板</td><td>有点风险想留个确认，如 <code>git push</code></td></tr><tr><td><code>deny</code></td><td>直接拦死，不执行也不提示</td><td>明确禁止的危险操作，如 <code>rm -rf</code>、读 <code>.env</code></td></tr></tbody></table><p><strong>优先级是铁律：<code>deny → ask → allow</code>，第一个匹配的规则赢。</strong> 所以 <code>deny</code> 永远压过其他俩——你既写了 <code>allow</code> 又写了 <code>deny</code>，<code>deny</code> 说了算。这个设计很合理：<strong>「禁止」就该比「允许」更有分量</strong>。</p><p>规则的写法是 <code>工具名</code> 或 <code>工具名(说明符)</code>。看几个例子就懂了：</p><table tabindex="0"><thead><tr><th>规则</th><th>匹配什么</th></tr></thead><tbody><tr><td><code>Bash</code></td><td>所有 Bash 命令</td></tr><tr><td><code>Bash(npm run build)</code></td><td>只匹配 <code>npm run build</code> 这条确切命令</td></tr><tr><td><code>Bash(npm run *)</code></td><td>匹配 <code>npm run</code> 开头的命令（<code>build</code>、<code>test</code>…）</td></tr><tr><td><code>Read(./.env)</code></td><td>读当前目录的 <code>.env</code> 文件</td></tr><tr><td><code>WebFetch(domain:github.com)</code></td><td>抓取 github.com 的网络请求</td></tr></tbody></table><p>通配符 <code>*</code> 有个<strong>新手必踩的空格坑</strong>，官方专门强调过：</p><blockquote><p><code>Bash(ls *)</code> 匹配 <code>ls -la</code> 但不匹配 <code>lsof</code>，而 <code>Bash(ls*)</code> 匹配两者。</p></blockquote><p>差一个空格，含义就变了。<code>ls *</code>（带空格）要求 <code>ls</code> 后面必须跟空格，所以 <code>lsof</code> 漏网；<code>ls*</code>（不带空格）连 <code>lsof</code> 一起匹配。<strong>想精确，就带空格</strong>。</p><p>一段完整的配置长这样——允许 npm 和 git commit，但拦死 git push：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;allow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(npm run *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git commit *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git push *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>最后一个<strong>安全要点必须划重点</strong>：<code>Read</code> / <code>Edit</code> 的 <code>deny</code> 规则，<strong>拦不住 Bash 子进程里的「绕道读写」</strong>。</p><p>啥意思？你写了 <code>deny: Read(./.env)</code> 挡住 Claude 直接读 <code>.env</code>，但如果它跑一段 Python 脚本 <code>open(&#39;.env&#39;).read()</code>，这个 <code>deny</code> 就管不着了——因为那是子进程在读，不走 Claude 的内置文件工具。官方提醒：</p><blockquote><p>它们不适用于间接读取或写入文件的任意子进程，如打开文件本身的 Python 或 Node 脚本。为了获得阻止所有进程访问路径的 OS 级别强制执行，请启用沙箱。</p></blockquote><p>这点很容易让人意外——以为 <code>deny .env</code> 就万无一失了。所以<strong>真要锁死敏感文件，权限规则 + 沙箱（Sandbox）一起上</strong>才叫深度防御（沙箱是 OS 级隔离，下一篇「安全」会展开）。</p><blockquote><p>💡 一句话总结：<code>deny → ask → allow</code> 按这个优先级匹配、<code>deny</code> 永远最大；规则带不带空格含义不同；<strong>但 <code>deny</code> 挡不住脚本绕道读写，敏感文件得配沙箱</strong>。</p></blockquote><hr><h2 id="_05-玩具放松、生产收紧-两套模板-「危险模式」红线" tabindex="-1">05 玩具放松、生产收紧：两套模板 +「危险模式」红线 <a class="header-anchor" href="#_05-玩具放松、生产收紧-两套模板-「危险模式」红线" aria-label="Permalink to &quot;05 玩具放松、生产收紧：两套模板 +「危险模式」红线&quot;">​</a></h2><p>讲了一堆机制，落到实处就一句话：<strong>项目越「玩具」越能放松，越「生产」越要收紧</strong>。</p><p>这里给两套常备配置，按项目性质二选一。</p><p><strong>模板一：玩具 / 个人小项目（放松提速）。</strong> 改砸了重建就行，没必要一步一停。设成 <code>acceptEdits</code> 让它改码免问，只兜住几个真危险的：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;defaultMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acceptEdits&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(rm -rf *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git push *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>模板二：生产 / 公司项目（收紧把关）。</strong> 默认每步都问，读操作放开方便它查代码，写文件和危险命令必须经你手，敏感文件直接拦死：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;defaultMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;allow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git status *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git diff *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(npm run *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(rm -rf *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git push *)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Read(./.env)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Read(./.env.*)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Read(./secrets/**)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>写这俩 deny 时记得：上一节说过，挡 <code>.env</code> 的 <code>deny</code> 防不住脚本绕道，生产环境真要稳，<strong>这层之外还得叠沙箱</strong>。别把单层 <code>deny</code> 当成铁壁。</p><p>最后是那道<strong>红线</strong>，也是开头对话的主角——<code>--dangerously-skip-permissions</code>（等价于 <code>bypassPermissions</code> 模式）。</p><p>这玩意儿<strong>跳过一切权限检查和安全检查</strong>，工具调用立即执行。它名字里的 <code>dangerously</code>（危险地）不是吓唬人。官方给它划了使用边界：</p><blockquote><p>仅在隔离环境（如容器、VM 或没有互联网访问的 dev containers）中使用此模式，其中 Claude Code 无法对您的主机系统造成损害。</p></blockquote><p>下面这套铁规矩可以直接照搬：</p><table tabindex="0"><thead><tr><th>场景</th><th>敢不敢开 <code>--dangerously-skip-permissions</code></th></tr></thead><tbody><tr><td>隔离容器 / VM / dev container</td><td>✅ 敢，删的也是一次性环境</td></tr><tr><td>CI 里跑一次性任务</td><td>✅ 敢，但配合 <code>deny</code> 再兜一层</td></tr><tr><td>自己日常开发的本机</td><td>❌ 不开，宁可慢点也要留闸</td></tr><tr><td>装着公司生产代码的机器</td><td>❌ 绝对不开，这是炸弹</td></tr></tbody></table><p>两个官方设计的「保险」让你稍微安心：一是即便在这个模式下，<strong><code>rm -rf /</code> 和 <code>rm -rf ~</code> 这种删根目录 / 主目录的操作仍会提示</strong>，当作防手滑的断路器；二是在 Linux / macOS 上<strong>以 root 或 <code>sudo</code> 身份是直接拒绝启动</strong>这个模式的。但别指望保险——<strong>核心还是「只在删了也不心疼的隔离环境里开」</strong>。</p><blockquote><p>💡 一句话总结：玩具项目 <code>acceptEdits</code> 提速、生产项目 <code>default</code> 把关，两套模板复制即用；<code>--dangerously-skip-permissions</code> <strong>只在隔离容器里开，本机和生产机一律免谈</strong>。</p></blockquote><hr><h2 id="_06-动手-5-分钟配出你的第一套权限规则" tabindex="-1">06 动手：5 分钟配出你的第一套权限规则 <a class="header-anchor" href="#_06-动手-5-分钟配出你的第一套权限规则" aria-label="Permalink to &quot;06 动手：5 分钟配出你的第一套权限规则&quot;">​</a></h2><p>光看不练假把式。下面带你给一个玩具项目配一套权限规则，<strong>亲眼看到 <code>allow</code> 和 <code>deny</code> 是怎么生效的</strong>。全程不依赖任何复杂环境。</p><p><strong>第一步：建玩具项目和配置文件</strong>（Mac / Linux）</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perm-demo</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perm-demo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .claude</span></span></code></pre></div><p><strong>预期</strong>：<code>perm-demo</code> 文件夹里有一个空的 <code>.claude</code> 目录。敲 <code>ls -a</code> 能看到 <code>.claude</code> 在。</p><p><strong>第二步：写一份 <code>settings.json</code></strong></p><p>用你顺手的编辑器，在 <code>perm-demo/.claude/settings.json</code> 里贴入：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permissions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;defaultMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;allow&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git status *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;Bash(git push *)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>这套规则的意思：默认每步都问，但 <code>git status</code> 放行不用问，<code>git push</code> 直接拦死。</p><p><strong>第三步：启动 Claude 并核对规则</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span></span></code></pre></div><p>进去后敲：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/permissions</span></span></code></pre></div><p><strong>预期</strong>：弹出权限管理界面，能看到你刚写的规则——<code>git status *</code> 在 Allow 列表里、<code>git push *</code> 在 Deny 列表里，并标着它们来自哪个 <code>settings.json</code> 文件。<strong>看到这两条 = 配置已被正确加载</strong>。</p><p><strong>第四步：验证 <code>deny</code> 真的拦得住</strong></p><p>在输入框里让它干一件被禁的事：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>帮我执行 git push origin main</span></span></code></pre></div><p><strong>预期</strong>：Claude <strong>不会</strong>执行，也不会弹「要不要批准」的提示——它会直接告诉你这个操作被权限规则拒绝了。<strong>这就是 <code>deny</code> 的威力：不执行、不提示，一拦到底。</strong></p><p><strong>第五步：对比 <code>allow</code> 的顺滑</strong></p><p>再让它干件被放行的事：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>帮我看一下 git status</span></span></code></pre></div><p><strong>预期</strong>：因为命中了 <code>allow: Bash(git status *)</code>，它<strong>不弹批准提示</strong>就直接跑了（这目录还没 <code>git init</code>，命令本身会报「不是 git 仓库」，但那是 git 的事——关键是<strong>它没停下来问你</strong>，说明 <code>allow</code> 生效了）。</p><p>跑通这五步，你就把「写规则 → 加载 → <code>deny</code> 拦死 → <code>allow</code> 放行」这条完整链路亲手验证了一遍。<strong>以后任何权限配置，本质都是在这套机制上加规则。</strong></p><blockquote><p>💡 一句话总结：建 <code>.claude/settings.json</code> 写规则、<code>/permissions</code> 看是否加载、再让 Claude 各跑一条被禁和被放行的命令——<strong>亲手跑通这条链路，比记十条语法都管用</strong>。</p></blockquote><hr><h2 id="_07-小结" tabindex="-1">07 小结 <a class="header-anchor" href="#_07-小结" aria-label="Permalink to &quot;07 小结&quot;">​</a></h2><p>这一篇我们把 Claude Code 的「权限缰绳」从头捋到尾——<strong>它能放多松、收多紧，全在你的一念之间和几行配置里</strong>。</p><p>把核心要点串起来回顾：</p><table tabindex="0"><thead><tr><th>你要做的事</th><th>用什么</th><th>关键点</th></tr></thead><tbody><tr><td>调整「问你的频率」</td><td>六种权限模式</td><td>从 <code>default</code>（步步问）到 <code>bypassPermissions</code>（裸奔）一条光谱</td></tr><tr><td>会话里快速切模式</td><td><code>Shift+Tab</code></td><td>在 <code>default</code> / <code>acceptEdits</code> / <code>plan</code> 三档循环</td></tr><tr><td>钉死默认模式</td><td><code>defaultMode</code></td><td>写进 <code>settings.json</code>；<code>auto</code> 得放用户级</td></tr><tr><td>精确控制单条操作</td><td><code>allow</code> / <code>ask</code> / <code>deny</code></td><td>优先级 <code>deny → ask → allow</code>，deny 最大</td></tr><tr><td>锁死敏感文件</td><td><code>deny</code> + 沙箱</td><td>光 <code>deny</code> 防不住脚本绕道</td></tr></tbody></table><p><strong>你现在应该能：</strong> 看懂六种权限模式分别在什么场景用、用 <code>Shift+Tab</code> 随手切换、在 <code>settings.json</code> 里写出按工具和按命令的 <code>allow</code> / <code>deny</code> 规则，给玩具项目和生产项目分别配出松紧合适的权限，并且清楚 <code>--dangerously-skip-permissions</code> 这道红线只能在隔离环境里碰。<strong>这套「松紧自如」的控权能力，是你敢放手让 Claude 干活、又不至于翻车的底气。</strong></p><hr><p>下一篇 <strong>21「安全与风险边界」</strong>——这一篇教你「权限怎么配」，但配置只是工具，更上层的问题是：<strong>到底该不该信任 AI 去碰你的代码和系统？哪些场景是真正的高危区？提示注入、敏感数据泄露这些坑长什么样？</strong> 缰绳攥在手里了，下一篇聊聊「什么时候该收、什么时候能放」的判断力。</p>`,118)])])}const u=t(i,[["render",n]]);export{k as __pageData,u as default};
