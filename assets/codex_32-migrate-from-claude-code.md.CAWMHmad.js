import{_ as e,o,c as t,a2 as s}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"32 · 从 Claude Code 迁移：旧地图换个工具，照样能找到家","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"codex/32-migrate-from-claude-code.md","filePath":"codex/32-migrate-from-claude-code.md"}'),a={name:"codex/32-migrate-from-claude-code.md"};function i(n,d,c,l,r,p){return o(),t("div",null,[...d[0]||(d[0]=[s(`<h1 id="_32-·-从-claude-code-迁移-旧地图换个工具-照样能找到家" tabindex="-1">32 · 从 Claude Code 迁移：旧地图换个工具，照样能找到家 <a class="header-anchor" href="#_32-·-从-claude-code-迁移-旧地图换个工具-照样能找到家" aria-label="Permalink to &quot;32 · 从 Claude Code 迁移：旧地图换个工具，照样能找到家&quot;">​</a></h1><blockquote><p>📚 <strong>系列导航</strong>：上一篇〔<a href="./31-speed">31 进阶技巧与提速</a> 〕讲怎么把整条工作流跑得更快、更省、更少返工。这一篇换个角度——专门写给<strong>从 Claude Code 转过来的你</strong>：你脑子里那套用了大半年的心智模型，哪些能直接搬、哪些得改个名、哪些是 Codex 独有的新东西。下一篇〔<a href="./33-windows">33 Windows 使用要点</a> 〕再聊 Windows 上那些专属的坑。</p></blockquote><p>先还原一段真实对话。上个月一个一直用 Claude Code 的朋友装好 Codex，开口就是一连串问号：</p><blockquote><p>他：「Codex 的 <code>CLAUDE.md</code> 放哪？我项目根目录那份它怎么不读？」 我：「Codex 不认 <code>CLAUDE.md</code> 这个名，它读 <code>AGENTS.md</code>，内容你几乎能原样搬过去。」 他：「那我 <code>settings.json</code> 里那套 <code>allow</code> / <code>deny</code> 权限规则呢？」 我：「也得换——Codex 用 <code>~/.codex/config.toml</code>，TOML 格式，权限那套思路是『沙箱 + 审批』，不是 <code>allowedTools</code>。」 他：「<code>claude -p</code> 跑脚本那套呢？还有 <code>/compact</code>、<code>/clear</code> 这些命令在不在？」 我：「<code>claude -p</code> 对应 <code>codex exec</code>，斜杠命令大半都在、名字基本一样。说白了你 90% 的肌肉记忆能直接用。」</p></blockquote><p>他听完那口气一下松了——<strong>原来不是从头学一个新工具，是同一张地图换了套地名</strong>。这一篇就把他问的、和他没来得及问的，一次性给你对齐：哪些概念一一对应、哪些差异照搬会栽跟头、最后带你把一份真实的 <code>CLAUDE.md</code> 手动改写成 <code>AGENTS.md</code>。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>一颗定心丸：你 90% 的 Claude Code 心智模型能直接搬到 Codex，迁移成本比想象低</li><li>一张「Claude Code 概念 → Codex 对应」的完整对照表，逐项讲清关键差异</li><li>项目说明书、配置文件、权限模型三处「名字像但脾气不同」的地方，照搬必踩的坑</li><li>哪些 Claude Code 的东西在 Codex 里<strong>没有、或不一样</strong>，别想当然</li><li>一套照着做就能把 <code>CLAUDE.md</code> 迁成 <code>AGENTS.md</code> 的动手流程</li></ul><blockquote><p>⚠️ 下文凡涉及具体命令、配置键、默认行为，都以 Codex <a href="https://developers.openai.com/codex" target="_blank" rel="noreferrer">官方文档</a> 为准；模型名、版本号这类随版本变的东西，以你本地 <code>/model</code> 面板和 <code>codex --help</code> 实际显示为准，别背名字。两边工具都在快速迭代，对照表讲的是「概念怎么映射」，不是逐字保证。</p></blockquote><hr><h2 id="_01-先给定心丸-你的心智模型-90-能直接搬" tabindex="-1">01 先给定心丸：你的心智模型 90% 能直接搬 <a class="header-anchor" href="#_01-先给定心丸-你的心智模型-90-能直接搬" aria-label="Permalink to &quot;01 先给定心丸：你的心智模型 90% 能直接搬&quot;">​</a></h2><p>先把最容易焦虑的事说死：<strong>从 Claude Code 转 Codex，你不是从零学一个新工具，是把同一套底层逻辑换个壳子继续用。</strong></p><p>为什么这么有底气？因为这俩在最根本的层面是<strong>同一类东西</strong>——都是终端里的 AI 编程 CLI，都跑「代理循环（agentic loop）」，都能直接读写你真实的代码库。这三点你在 Claude Code 篇里反复见过，到 Codex 这边一个字都不用改。</p><p><strong>类比：从安卓换到另一个牌子的安卓机。</strong> 不是从安卓换苹果那种推倒重来——你拨号、发消息、装应用、滑动返回的肌肉记忆全在，只是图标挪了位置、设置菜单换了层级、个别 App 名字不一样。你需要的不是重新学「怎么用手机」，是花十分钟摸清「这台机器把东西放哪了」。Codex 之于 Claude Code，就是这种关系。</p><p>具体哪些能直接搬？我自己从 Claude Code 转过来时，下面这些<strong>一次都没卡过</strong>：</p><ul><li><strong>「想 → 做 → 看」的代理循环</strong>：你描述需求、它制定计划、动手改、再回看结果，这套节奏完全一致。</li><li><strong>「先让它读、看懂方案再放手」的习惯</strong>：接手陌生项目先只读通读、出方案、确认后才动手，两边都该这么干。</li><li><strong>「项目规矩写进一份文件、每轮开工自动读」的思路</strong>：只是文件名和加载细节有别（下一节细说）。</li><li><strong>斜杠命令当「会话控制台」</strong>：切模型、清上下文、看状态，入口都是一个 <code>/</code>。</li></ul><p>我第一次正式拿 Codex 干活，是去年把一个 FastAPI 项目从 Claude Code 切过来。我几乎是凭着旧习惯瞎按的——<code>/status</code> 看配置、<code>/model</code> 切模型、改之前先让它出方案——<strong>结果八成操作直接命中</strong>，剩下两成是文件名和权限设法不一样，翻了下官方文档十分钟就对齐了。那种「咦这我会」的熟悉感，比我预想的强太多。</p><blockquote><p>💡 一句话总结：Codex 和 Claude Code 是同一类工具（终端 CLI + 代理循环 + 读写真实代码库），<strong>你 90% 的心智模型能直接搬</strong>，要重新认的只是「东西放哪、叫什么名」。</p></blockquote><hr><h2 id="_02-一张大对照表-旧地名-→-新地名" tabindex="-1">02 一张大对照表：旧地名 → 新地名 <a class="header-anchor" href="#_02-一张大对照表-旧地名-→-新地名" aria-label="Permalink to &quot;02 一张大对照表：旧地名 → 新地名&quot;">​</a></h2><p>这一节是全篇的骨架——把你在 Claude Code 篇学过的每个核心概念，对到 Codex 这边的名字和位置。<strong>先看表建立全局印象，后面几节再把差异最大的几行单独拆开讲。</strong></p><table tabindex="0"><thead><tr><th>Claude Code</th><th>Codex</th><th>关系</th><th>一句话差异</th></tr></thead><tbody><tr><td>项目说明书 <code>CLAUDE.md</code></td><td><code>AGENTS.md</code></td><td>换名</td><td>概念一致，发现链 / 覆写规则不同（见 03）</td></tr><tr><td>配置文件 <code>~/.claude/settings.json</code>（JSON）</td><td><code>~/.codex/config.toml</code>（TOML）</td><td>换格式</td><td>JSON → TOML，键名和结构全不一样（见 04）</td></tr><tr><td>权限模式 + <code>allow</code>/<code>ask</code>/<code>deny</code> 规则</td><td>沙箱 <code>sandbox</code> + 审批 <code>approval</code></td><td>换思路</td><td>从「按工具列白名单」变「圈地 + 出圈才问」（见 05）</td></tr><tr><td>无头模式 <code>claude -p</code></td><td>非交互命令 <code>codex exec</code></td><td>换名</td><td>都是「不进交互界面、跑完就走」</td></tr><tr><td><code>CLAUDE.md</code> 分层（用户 / 项目 / 子目录）</td><td><code>AGENTS.md</code> 分层（全局 / 项目逐级）</td><td>对应</td><td>思路一致，Codex 多了 <code>AGENTS.override.md</code></td></tr><tr><td>外部工具 MCP</td><td>MCP</td><td>几乎通用</td><td>同一套协议，配置写法各自不同</td></tr><tr><td>子代理 Subagents</td><td>子代理 Subagents</td><td>对应</td><td>两边都有，配置位置不同</td></tr><tr><td>技能 Skills</td><td>技能 Skills</td><td>对应</td><td>两边都有，本地目录组织略有差异</td></tr><tr><td>斜杠命令（<code>/model</code>、<code>/compact</code>…）</td><td>斜杠命令（<code>/model</code>、<code>/compact</code>…）</td><td>大半同名</td><td>命令名基本一致，个别有出入（见 06）</td></tr><tr><td>自动记忆 memory（默认开）</td><td>Memories / Chronicle</td><td>对应但脾气不同</td><td>Codex 记忆<strong>默认关</strong>、有地区限制、异步生成</td></tr><tr><td>模型 Opus / Sonnet / Haiku</td><td>GPT-5.x 系列</td><td>换型号</td><td>旗舰 <code>gpt-5.5</code>、轻量 <code>gpt-5.4-mini</code> 等</td></tr></tbody></table><p>这张表你不用背，<strong>记住一个总规律就行</strong>：</p><p><strong>凡是「概念」层面的东西（项目说明书、权限、记忆、子代理、技能、MCP），两边都有，迁移就是「换名 + 调写法」；真正会让你栽跟头的，是那几个「名字像、脾气不同」的——项目说明书、配置文件、权限模型。</strong> 下面三节专治这三个。</p><p>至于模型对照，一句话带过：你在 Claude Code 那边按「Opus 啃硬活、Sonnet 跑日常、Haiku 跑杂活」选型号，到 Codex 这边换成「<code>gpt-5.5</code> 啃硬活、<code>gpt-5.4-mini</code> 跑杂活」，选型逻辑（拿任务难度匹配算力）一模一样，详见〔<a href="./30-models">30 怎么选模型</a> 〕。注意别把 <code>gpt-5.4</code> 当旗舰——旗舰是 <code>gpt-5.5</code>，<code>gpt-5.4-mini</code> 是它的轻量款。</p><blockquote><p>💡 一句话总结：概念层面两边几乎一一对应，迁移多是「换名 + 调写法」；<strong>真正的坑集中在项目说明书、配置文件、权限模型这三处「名字像但脾气不同」的地方</strong>，下面逐个拆。</p></blockquote><hr><h2 id="_03-项目说明书-claude-md-→-agents-md" tabindex="-1">03 项目说明书：<code>CLAUDE.md</code> → <code>AGENTS.md</code> <a class="header-anchor" href="#_03-项目说明书-claude-md-→-agents-md" aria-label="Permalink to &quot;03 项目说明书：\`CLAUDE.md\` → \`AGENTS.md\`&quot;">​</a></h2><p>这是你迁移时第一个会撞的问题，也是我朋友开口第一问：<strong>Codex 不读 <code>CLAUDE.md</code>。</strong> 你项目根目录那份它视而不见，因为它认的文件名叫 <code>AGENTS.md</code>。</p><p>好消息是：<strong>内容你几乎能原样搬。</strong> 项目概述、技术栈、常用命令、代码约定、禁改清单——这五类东西在两边都是同样的写法、同样的目的。你在 Claude Code 篇 [18] 学的「写每轮该记住的事实、删一切看代码就能自证的东西」，到 <code>AGENTS.md</code> 这边一字不改照样成立。</p><p><strong>类比：换公司后重写一份交接文档。</strong> 你跳槽了，旧东家那份《项目说明》里的核心信息——技术栈、怎么跑测试、哪些雷区别碰——大半能直接复制到新东家的模板里。但新公司文档归档的<strong>文件夹结构和命名规范</strong>不一样，你得按它的来。<code>AGENTS.md</code> 就是这种「内容能搬、归档规则得换」的情况。</p><p>具体到「归档规则」的差异，有几处照搬会出问题，列清楚：</p><table tabindex="0"><thead><tr><th>维度</th><th>Claude Code（<code>CLAUDE.md</code>）</th><th>Codex（<code>AGENTS.md</code>）</th></tr></thead><tbody><tr><td>用户级放哪</td><td><code>~/.claude/CLAUDE.md</code></td><td><code>~/.codex/AGENTS.md</code></td></tr><tr><td>项目级放哪</td><td><code>./CLAUDE.md</code> 或 <code>./.claude/CLAUDE.md</code></td><td><code>./AGENTS.md</code>（项目根）</td></tr><tr><td>子目录级</td><td>任意子目录 <code>CLAUDE.md</code>，读到才加载</td><td>从 Git 根逐级到当前目录，每目录挑一个</td></tr><tr><td>临时覆写</td><td>本地变体 <code>CLAUDE.local.md</code>（不进 git）</td><td><strong><code>AGENTS.override.md</code></strong>（那一层跳过同级 <code>AGENTS.md</code>）</td></tr><tr><td>大小红线</td><td>按<strong>行数</strong>（建议 200 行内）</td><td>按<strong>字节</strong>（合并后默认 32 KiB，<code>project_doc_max_bytes</code>）</td></tr></tbody></table><p>三个最该记住的差异：</p><p><strong>第一，临时覆写机制不一样。</strong> Claude Code 用 <code>CLAUDE.local.md</code> 放「只你自己看、不进 git」的本地偏好。Codex 那套是 <code>AGENTS.override.md</code> ——它存在的那一层，同级的 <code>AGENTS.md</code> 被整个跳过。这俩<strong>不是一回事</strong>：前者是「附加个人内容」，后者是「这一层用我、别用旁边那份」。详见 [11] 。</p><p><strong>第二，大小红线从「数行」变「数字节」。</strong> Claude Code 劝你 <code>CLAUDE.md</code> 别超 200 行；Codex 是硬指标——合并后超过默认 32 KiB 就开始截断、甚至整份文件被拦在外面。我迁移那份 FastAPI 项目的说明书时就吃过一记：原来 <code>CLAUDE.md</code> 写得偏肥，搬过来一开始没在意，后来发现子目录那份指令没生效，一查才知道是合并体积撞了上限。所以<strong>搬之前顺手瘦个身，删掉那些 Codex 看代码就能自证的内容</strong>。</p><p><strong>第三，发现链是「拼接」不是「覆盖」。</strong> 全局那份和项目那份<strong>同时生效</strong>，冲突时离当前目录越近的越优先。这点和 Claude Code 的层级加载思路一致，但 Codex 的合并顺序（从根到叶拼接、越近越晚拼、优先级越高）官方写得更明确，照 [11] 那套理解就行。</p><blockquote><p>💡 一句话总结：<code>CLAUDE.md</code> 的<strong>内容</strong>几乎能原样搬进 <code>AGENTS.md</code>，但<strong>文件名、临时覆写机制（<code>CLAUDE.local.md</code> → <code>AGENTS.override.md</code>）、大小红线（行数 → 字节）</strong> 三处不同，搬之前顺手瘦身。</p></blockquote><hr><h2 id="_04-配置文件-settings-json-json-→-config-toml-toml" tabindex="-1">04 配置文件：<code>settings.json</code>（JSON）→ <code>config.toml</code>（TOML） <a class="header-anchor" href="#_04-配置文件-settings-json-json-→-config-toml-toml" aria-label="Permalink to &quot;04 配置文件：\`settings.json\`（JSON）→ \`config.toml\`（TOML）&quot;">​</a></h2><p>第二个坑：你 <code>~/.claude/settings.json</code> 里那套配置，<strong>不能直接复制到 Codex</strong>。两边连文件格式都不一样。</p><ul><li>Claude Code：<code>~/.claude/settings.json</code> ，<strong>JSON 格式</strong>，权限、环境变量、Hook、默认模型都塞这一个文件。</li><li>Codex：<code>~/.codex/config.toml</code> ，<strong>TOML 格式</strong>，模型、沙箱、审批、MCP 等行为旋钮都在这儿。</li></ul><p><strong>类比：同样是装修配电图，一个用公制一个用英制。</strong> 图纸要表达的东西（哪条线管哪个开关）是一回事，但标注的单位和符号体系是另一套。你不能拿英制图纸直接套到公制施工队手里——得逐项换算重画。JSON 配 → TOML 配，就是这种「意图一致、写法重画」的活儿。</p><p>格式差异最直观的几处，对照感受一下：</p><table tabindex="0"><thead><tr><th>你想配的事</th><th>Claude Code（<code>settings.json</code>，JSON）</th><th>Codex（<code>config.toml</code>，TOML）</th></tr></thead><tbody><tr><td>默认模型</td><td><code>&quot;model&quot;: &quot;claude-...&quot;</code></td><td><code>model = &quot;gpt-5.5&quot;</code></td></tr><tr><td>权限 / 安全</td><td><code>&quot;permissions&quot;: { &quot;deny&quot;: [...] }</code></td><td><code>sandbox_mode = &quot;...&quot;</code> + <code>approval_policy = &quot;...&quot;</code></td></tr><tr><td>嵌套结构</td><td>大括号 <code>{ }</code> + 逗号</td><td>节表头 <code>[section]</code> + 等号</td></tr><tr><td>字符串引号</td><td>双引号必带</td><td>双引号</td></tr></tbody></table><p>举个最小的对照。Claude Code 里设默认模型（JSON）：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;model&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;claude-sonnet-4&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Codex 里设默认模型 + 推理强度（TOML）：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.codex/config.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gpt-5.5&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model_reasoning_effort = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;medium&quot;</span></span></code></pre></div><p>注意 TOML 的几个习惯，从 JSON 转过来最容易写错：<strong>键值对用 <code>=</code> 不用 <code>:</code> ；不用大括号包整个对象，靠 <code>[section]</code> 这种节表头分组；行尾不加逗号。</strong> 我刚转过来时手贱在 <code>config.toml</code> 里给每行末尾加了逗号（JSON 肌肉记忆），Codex 直接报配置解析错——这是 JSON 转 TOML 最高频的低级失误，提前告诉你，省得你也踩。</p><p><code>config.toml</code> 里那一大堆旋钮（模型、沙箱、审批、推理强度、服务层级、MCP……）的完整讲解在〔<a href="./18-config">18 config.toml 配置详解</a> 〕，这里你只需建立一个认知：<strong>别想着「翻译」整个 <code>settings.json</code> ，而是照着 [18] 那张键表，把你真正在用的那几条重新配一遍。</strong> 大多数人实际改的也就模型、沙箱、审批这三五行。</p><blockquote><p>💡 一句话总结：<code>settings.json</code>（JSON）→ <code>config.toml</code>（TOML）是<strong>换格式重写</strong>，不是复制粘贴；TOML 用 <code>=</code>、用 <code>[section]</code>、行尾不加逗号——别把 JSON 的逗号习惯带过来；照 [18] 那张键表重配你真在用的那几条即可。</p></blockquote><hr><h2 id="_05-权限模型-权限模式-→-沙箱-审批" tabindex="-1">05 权限模型：权限模式 → 沙箱 + 审批 <a class="header-anchor" href="#_05-权限模型-权限模式-→-沙箱-审批" aria-label="Permalink to &quot;05 权限模型：权限模式 → 沙箱 + 审批&quot;">​</a></h2><p>这是迁移时<strong>心智差异最大</strong>的一处，也是最值得花时间重建认知的地方。照搬 Claude Code 那套「列白名单」的思路过来，你会找不着北。</p><p>Claude Code 的权限模型，核心是<strong>两件事</strong>：</p><ul><li><strong>权限模式（permission mode）</strong>：六档，从 <code>default</code>（步步问）到 <code>bypassPermissions</code>（全放开），用 <code>Shift+Tab</code> 在模式间切。</li><li><strong>规则白名单</strong>：在 <code>settings.json</code> 里写 <code>allow</code> / <code>ask</code> / <code>deny</code>，按工具、按命令精确控权（比如 <code>deny</code> 掉 <code>rm -rf</code>）。</li></ul><p>Codex 不是这套。它把「能动多大」和「问不问你」<strong>拆成两个独立旋钮</strong>：</p><ul><li><strong>沙箱（sandbox）</strong>：管「能动多大」——<code>read-only</code> / <code>workspace-write</code> / <code>danger-full-access</code> 三档。</li><li><strong>审批（approval）</strong>：管「问不问你」——<code>untrusted</code> / <code>on-request</code> / <code>never</code> 三档。</li></ul><p><strong>类比：从「逐项审批的报销制度」换成「划定预算 + 超支才报批」。</strong> Claude Code 像老式报销——每一笔（每个工具、每条命令）你都预先规定准不准、要不要问，列得很细。Codex 像现代预算制——先给你圈一块「预算范围」（沙箱），范围内你随便花不打扰（审批不触发），<strong>只有要超出这块范围时才停下来报批</strong>。前者按「项」管，后者按「圈」管，这是思路上的根本切换。</p><p>两边映射关系，我整理成一张迁移对照表：</p><table tabindex="0"><thead><tr><th>你在 Claude Code 想要的效果</th><th>Claude Code 怎么设</th><th>Codex 怎么设</th></tr></thead><tbody><tr><td>只让它读、别动我东西</td><td><code>default</code> 模式 / 只读</td><td>沙箱 <code>read-only</code></td></tr><tr><td>项目里放手改、出项目要问</td><td><code>acceptEdits</code> 一类</td><td>沙箱 <code>workspace-write</code> + 审批 <code>on-request</code>（日常黄金组合）</td></tr><tr><td>全自动、啥都别问</td><td><code>bypassPermissions</code></td><td>沙箱 <code>danger-full-access</code> + 审批 <code>never</code>（<code>--yolo</code> 更彻底：完全绕开沙箱）</td></tr><tr><td>拦死某条危险命令</td><td><code>permissions.deny</code> 写规则</td><td>rules（实验性）用 <code>prefix_rule()</code> 匹配命令前缀，<code>decision = &quot;forbidden&quot;</code> 即拦死</td></tr><tr><td>切换松紧</td><td><code>Shift+Tab</code></td><td>会话里 <code>/permissions</code> 或启动加 <code>-s</code> / <code>-a</code></td></tr></tbody></table><p>几个搬过来必须重建的认知：</p><p><strong>第一，「不问」≠「放权」。</strong> Claude Code 那套里模式越往后越松；Codex 这俩旋钮是独立的——你完全可以「只读 + 不问我」（<code>read-only</code> + <code>never</code>），意思是「随便读、但读的时候一句都别打扰」。<code>never</code> 是「不弹审批」，不是「放开权限」。</p><p><strong>第二，默认档跟「有没有 Git」挂钩。</strong> Codex 启动时会智能选档：有 Git 管的目录给你 <code>workspace-write</code> + <code>on-request</code>（Auto 档），没 Git 的目录默认 <code>read-only</code> 。这是 Claude Code 没有的贴心设计，也是道安全网——别在没 Git 的临时目录里手贱开完全访问（我在 [15] 讲过自己怎么因为这个后背发凉）。</p><p><strong>第三，<code>workspace-write</code> 下网络默认是关的、<code>.git</code> 是只读保护的。</strong> 这俩默认值反直觉，照搬 Claude Code 经验最容易踩。想联网得自己开 <code>network_access</code> 。</p><p>权限这块的完整玩法（三档怎么组合、rules 怎么写、<code>--yolo</code> 红线在哪）在〔<a href="./15-permissions">15 权限、沙箱与审批</a> 〕，迁移时重点是先把「两个独立旋钮」这个新心智建立起来，别拿「列白名单」的旧框架硬套。</p><blockquote><p>💡 一句话总结：Claude Code 的「权限模式 + <code>allow</code>/<code>deny</code> 白名单」到 Codex 变成「沙箱（能动多大）+ 审批（问不问你）两个独立旋钮」；记住「不问 ≠ 放权」、默认档看有没有 Git、<code>workspace-write</code> 网络默认关——这三点照搬必栽。</p></blockquote><hr><h2 id="_06-交互习惯-斜杠命令、会话管理的异同" tabindex="-1">06 交互习惯：斜杠命令、会话管理的异同 <a class="header-anchor" href="#_06-交互习惯-斜杠命令、会话管理的异同" aria-label="Permalink to &quot;06 交互习惯：斜杠命令、会话管理的异同&quot;">​</a></h2><p>好消息回来了：<strong>进了会话之后，你的肌肉记忆大半能直接用。</strong> 斜杠命令两边重叠度极高，常用的那批几乎同名。</p><table tabindex="0"><thead><tr><th>你想干的事</th><th>Claude Code</th><th>Codex</th><th>是否同名</th></tr></thead><tbody><tr><td>切模型</td><td><code>/model</code></td><td><code>/model</code></td><td>✅</td></tr><tr><td>压缩上下文</td><td><code>/compact</code></td><td><code>/compact</code></td><td>✅</td></tr><tr><td>清屏开新对话</td><td><code>/clear</code></td><td><code>/clear</code></td><td>✅</td></tr><tr><td>看状态 / 配置</td><td><code>/status</code>（或 <code>/config</code>）</td><td><code>/status</code></td><td>基本对应</td></tr><tr><td>生成项目说明书</td><td><code>/init</code></td><td><code>/init</code></td><td>✅（一个生成 <code>CLAUDE.md</code>、一个生成 <code>AGENTS.md</code>）</td></tr><tr><td>看改动 diff</td><td><code>/diff</code></td><td><code>/diff</code></td><td>✅</td></tr><tr><td>让它审一遍改动</td><td><code>/review</code></td><td><code>/review</code></td><td>✅</td></tr><tr><td>调权限松紧</td><td><code>Shift+Tab</code> 切模式</td><td><code>/permissions</code></td><td>❌ 入口不同</td></tr></tbody></table><p>差异主要在两处：</p><p><strong>第一，调权限的入口不同。</strong> Claude Code 靠 <code>Shift+Tab</code> 这个快捷键在权限模式间循环切；Codex 是敲斜杠命令 <code>/permissions</code> 弹选择器挑档。功能等价，但手感不一样——从快捷键变成了菜单选择。</p><p><strong>第二，<code>/clear</code> 和 <code>/new</code> 在 Codex 这边分得更细。</strong> Codex 里 <code>/clear</code> 是「清屏 + 开新对话」，<code>/new</code> 是「开新对话但不清屏」。Claude Code 那边换任务清空主要就 <code>/clear</code> 一个（旧对话还能 <code>/resume</code> 找回）。细微差别，知道有就行。</p><p>我自己迁移过来后第一周的真实体感：<strong>敲命令基本不用查</strong>——<code>/model</code> 切模型、<code>/status</code> 看状态、<code>/compact</code> 压上下文全是顺手就来。唯一卡过一下的是想调权限，习惯性按 <code>Shift+Tab</code> 没反应，才想起来 Codex 得敲 <code>/permissions</code> 。完整的斜杠命令速查在〔<a href="./12-slash-commands">12 斜杠命令与快捷键</a> 〕，但凭旧习惯先用着、卡了再查，基本够用。</p><blockquote><p>💡 一句话总结：斜杠命令两边重叠度极高（<code>/model</code>、<code>/compact</code>、<code>/clear</code>、<code>/diff</code>、<code>/review</code> 全同名），凭旧习惯先用、卡了再查；最大差异是调权限——Claude Code 按 <code>Shift+Tab</code>，Codex 敲 <code>/permissions</code> 。</p></blockquote><hr><h2 id="_07-别想当然-这些在-codex-里没有或不一样" tabindex="-1">07 别想当然：这些在 Codex 里没有或不一样 <a class="header-anchor" href="#_07-别想当然-这些在-codex-里没有或不一样" aria-label="Permalink to &quot;07 别想当然：这些在 Codex 里没有或不一样&quot;">​</a></h2><p>迁移最危险的不是「不会用」，是「<strong>以为和 Claude Code 一样、结果不一样</strong>」。这一节专门点出几个容易想当然的地方。</p><p><strong>第一，记忆系统默认状态相反。</strong> Claude Code 的自动记忆默认<strong>开着</strong>，按 git 仓库分目录、开局自动读回。Codex 的 Memories <strong>默认关着</strong>、有地区限制、走后台异步生成，存放路径和控制方式都另一套。我那个朋友刚转过来时，以为「我跟它说一句它就记住了」——结果转头又用错包管理器，就是把 Claude Code 那套「默认有记忆」的预期带过来了。<strong>必须每次生效的死规矩，两边都该写进项目说明书（<code>AGENTS.md</code>），别赌记忆</strong>，详见 [19] 。</p><p><strong>第二，Codex 有 Claude Code 没有的东西。</strong> 别假设功能只是「换名」，有些是新增的：</p><ul><li><strong><code>AGENTS.override.md</code></strong>：那一层临时盖章用的覆写文件，Claude Code 无对应物（[11] ）。</li><li><strong>Chronicle</strong>：用屏幕内容喂记忆的能力，官方目前标为「研究预览版（research preview）」、需手动开启且有地区限制，Codex 独有（[19] ）。</li><li><strong>基于 Git 的默认档选择</strong>：启动时按「有没有 Git」自动给你选沙箱档（[15] ）。</li></ul><p><strong>第三，反过来，Claude Code 的个别东西在 Codex 里形态不同。</strong> 比如 Claude Code 那套 <code>CLAUDE.local.md</code> 本地变体，Codex 用 <code>AGENTS.override.md</code> 顶替但语义不同；权限白名单那套精确控命令，Codex 对应的是实验性的 rules（写法是 Starlark，不是 JSON 数组）。</p><p>下面这张「别想当然」清单，迁移前过一遍：</p><table tabindex="0"><thead><tr><th>你可能的想当然</th><th>实际情况</th></tr></thead><tbody><tr><td>❌ Codex 也读 <code>CLAUDE.md</code></td><td>✅ 它只读 <code>AGENTS.md</code>（除非你配 <code>project_doc_fallback_filenames</code>）</td></tr><tr><td>❌ 记忆默认开着、说一句就记</td><td>✅ Memories 默认关、异步生成、有地区限制</td></tr><tr><td>❌ <code>settings.json</code> 复制过去就行</td><td>✅ 得重写成 TOML 的 <code>config.toml</code></td></tr><tr><td>❌ 权限按工具列白名单</td><td>✅ 是沙箱 + 审批两个旋钮</td></tr><tr><td>❌ <code>Shift+Tab</code> 切权限</td><td>✅ Codex 敲 <code>/permissions</code></td></tr></tbody></table><blockquote><p>💡 一句话总结：迁移最大风险是「想当然以为一样」——记忆<strong>默认关</strong>（不是开）、Codex 有 <code>AGENTS.override.md</code> / Chronicle / 基于 Git 的默认档这些独有物、权限是旋钮不是白名单；过一遍上面那张清单再动手。</p></blockquote><hr><h2 id="_08-动手-把一份-claude-md-改写成-agents-md" tabindex="-1">08 动手：把一份 <code>CLAUDE.md</code> 改写成 <code>AGENTS.md</code> <a class="header-anchor" href="#_08-动手-把一份-claude-md-改写成-agents-md" aria-label="Permalink to &quot;08 动手：把一份 \`CLAUDE.md\` 改写成 \`AGENTS.md\`&quot;">​</a></h2><p>光看不练记不住。下面用一份真实风格的 <code>CLAUDE.md</code> ，手把手迁成 <code>AGENTS.md</code> ，并验证 Codex 真读到了。跟着敲，五分钟搞定。</p><p>平台差异先说清：下面的 <code>mkdir</code> / <code>git init</code> 在 <strong>Mac / Linux</strong> 直接用；<strong>Windows</strong> 建议在 Git Bash 或 WSL 里敲，或用资源管理器手动建。路径里 <code>~</code> 指用户主目录，Windows 对应 <code>C:\\Users\\你的用户名\\</code> 。</p><p><strong>第一步：准备一份待迁移的 <code>CLAUDE.md</code> 。</strong></p><p>建个玩具项目，放一份典型的 <code>CLAUDE.md</code>（就当是你从 Claude Code 项目里搬来的）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate-demo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate-demo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span></span></code></pre></div><p>在项目根新建 <code>CLAUDE.md</code> ，贴入这份（故意带一行该删的废话，演示瘦身）：</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># migrate-demo 项目说明</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">这是一个基于 FastAPI 的订单管理后端。本项目由订单团队在 2023 年立项，</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">最初用 Flask，后来为了异步性能迁到 FastAPI，技术选型经过三轮评审……（一大段背景）</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 技术栈</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Python 3.11 / PostgreSQL / pytest</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 常用命令</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`pytest\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> —— 运行测试</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`ruff check .\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> —— 跑 lint</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 编程约定</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 所有函数必须有类型注解</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 字符串统一用双引号</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 禁区</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 不要改动 migrations/ 里已有的迁移文件</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 新增生产依赖前先问我</span></span></code></pre></div><p><strong>预期</strong>：项目根出现 <code>CLAUDE.md</code> 。注意那段立项背景——<strong>Codex 写代码用不上，正是该在迁移时删掉的</strong>。</p><p><strong>第二步：改写成 <code>AGENTS.md</code> 。</strong></p><p>新建 <code>AGENTS.md</code> ，把内容搬过来、<strong>顺手砍掉那段立项背景</strong>（其余几乎照搬）：</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># migrate-demo — 基于 FastAPI 的订单管理后端</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 技术栈</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Python 3.11 / PostgreSQL / pytest</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 常用命令</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`pytest\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> —— 运行测试</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`ruff check .\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> —— 跑 lint</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 编程约定</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 所有函数必须有类型注解</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 字符串统一用双引号</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 禁区</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 不要改动 migrations/ 里已有的迁移文件</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 新增生产依赖前先问我</span></span></code></pre></div><p><strong>预期</strong>：项目根出现 <code>AGENTS.md</code> ，比原 <code>CLAUDE.md</code> 短了一截——<strong>砍掉的全是 Codex 看代码能自证或根本用不上的背景</strong>。这就是「内容能搬、顺手瘦身」的活样本。</p><p><strong>第三步：让 Codex 复述它读到的指令，验证迁移成功。</strong></p><p>在项目目录里跑（这里只读总结，加 <code>--ask-for-approval never</code> 是为免去审批打扰、让输出干净——这正是官方文档用来验证 <code>AGENTS.md</code> 是否被读到的标准做法，放心用）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ask-for-approval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> never</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Summarize the current instructions.&quot;</span></span></code></pre></div><p><strong>预期</strong>：Codex 会<strong>回显你刚写的那几条</strong>——技术栈、<code>pytest</code> / <code>ruff</code> 命令、类型注解、双引号、别动 migrations、别加依赖。只要它复述出来，就证明这份 <code>AGENTS.md</code> 这一轮确实被装进了上下文，<strong>迁移成功</strong>。</p><p><strong>第四步（可选）：把旧的 <code>CLAUDE.md</code> 处理掉。</strong></p><p>迁完确认无误，旧的 <code>CLAUDE.md</code> 留着也不影响（Codex 不读它），但容易让队友混淆。<strong>要不要删由你决定</strong>——这属于会改动文件的操作，自己手动处理，别让工具替你删。如果想让 Codex 顺带也认 <code>CLAUDE.md</code> 这个名（过渡期两边并存），可以在 <code>~/.codex/config.toml</code> 里加：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.codex/config.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">project_doc_fallback_filenames = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CLAUDE.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>加了这条，Codex 在每个目录的挑选顺序变成 <code>AGENTS.override.md</code> → <code>AGENTS.md</code> → <code>CLAUDE.md</code> ，取第一个存在且非空的——<strong>过渡期想让老 <code>CLAUDE.md</code> 也被读到，这是最省事的办法</strong>（改完重启 Codex 生效）。</p><blockquote><p>💡 一句话总结：迁移 = 把 <code>CLAUDE.md</code> 内容搬进 <code>AGENTS.md</code> + 顺手砍背景，用「Summarize the current instructions」验证读到没；过渡期想让老 <code>CLAUDE.md</code> 也被认，配一行 <code>project_doc_fallback_filenames</code> 即可。</p></blockquote><hr><h2 id="小结" tabindex="-1">小结 <a class="header-anchor" href="#小结" aria-label="Permalink to &quot;小结&quot;">​</a></h2><p>这一篇把「从 Claude Code 迁到 Codex」这件事从心智到落地捋了一遍：</p><table tabindex="0"><thead><tr><th>维度</th><th>关键结论</th></tr></thead><tbody><tr><td>总判断</td><td>90% 心智模型直接搬，要重学的只是「东西放哪、叫什么名」</td></tr><tr><td>项目说明书</td><td><code>CLAUDE.md</code> → <code>AGENTS.md</code>，内容能搬，<strong>临时覆写机制、大小红线（行 → 字节）</strong> 不同</td></tr><tr><td>配置文件</td><td><code>settings.json</code>（JSON）→ <code>config.toml</code>（TOML），<strong>换格式重写</strong>，别带 JSON 的逗号习惯</td></tr><tr><td>权限模型</td><td>权限模式 + 白名单 → 沙箱 + 审批<strong>两个独立旋钮</strong>，「不问 ≠ 放权」</td></tr><tr><td>交互习惯</td><td>斜杠命令大半同名，最大差异是调权限（<code>Shift+Tab</code> → <code>/permissions</code>）</td></tr><tr><td>别想当然</td><td>记忆<strong>默认关</strong>、Codex 有 <code>AGENTS.override.md</code> / Chronicle 等独有物</td></tr></tbody></table><p><strong>你现在应该能</strong>：把一份 <code>CLAUDE.md</code> 干净地迁成 <code>AGENTS.md</code> ；知道 <code>settings.json</code> 该重写成哪几行 <code>config.toml</code> ；用「沙箱 + 审批」的新框架替换掉「权限模式 + 白名单」的旧框架；并且躲开「以为和 Claude Code 一样、其实不一样」的那几个坑。<strong>一句话——你那套用熟的 Claude Code 经验没白学，换个工具，照样能找到家。</strong></p><hr><p>下一篇〔<a href="./33-windows">33 Windows 使用要点</a> 〕——前面好几处动手我都让 Windows 用户「在 Git Bash 或 WSL 里敲」，那到底为什么？Windows 上跑 Codex，沙箱、路径、终端这些地方有哪些专属的坑？留个小思考：本篇讲的沙箱机制，在一个原生没有 Linux 那套隔离能力的系统上，Codex 又是靠什么把缰绳攥住的？下一篇就把 Windows 这条线单独讲透。</p>`,116)])])}const k=e(a,[["render",i]]);export{g as __pageData,k as default};
