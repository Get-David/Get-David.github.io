import{_ as t,o as i,c as a,a2 as o}from"./chunks/framework.BWuWLRhz.js";const e="/assets/33-hook-lifecycle@2x.BpHrZjQN.png",g=JSON.parse('{"title":"33 · 钩子（Hooks）：在固定时机自动扣扳机","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"claude-code/33-hooks.md","filePath":"claude-code/33-hooks.md"}'),n={name:"claude-code/33-hooks.md"};function d(l,s,p,h,k,r){return i(),a("div",null,[...s[0]||(s[0]=[o('<h1 id="_33-·-钩子-hooks-在固定时机自动扣扳机" tabindex="-1">33 · 钩子（Hooks）：在固定时机自动扣扳机 <a class="header-anchor" href="#_33-·-钩子-hooks-在固定时机自动扣扳机" aria-label="Permalink to &quot;33 · 钩子（Hooks）：在固定时机自动扣扳机&quot;">​</a></h1><blockquote><p>📚 <strong>系列导航</strong>：上一篇 <a href="./32-output-styles">32 输出样式（Output Styles）</a> 教你怎么换一套「人格」让 Claude 用你想要的口吻干活。这一篇聊的是另一种「自动化」——不是改它怎么说话，而是<strong>在某个事件一发生的那一刻，雷打不动替你跑一段动作</strong>：改完文件自动格式化、危险命令直接拦死、它干完活给你发条通知。这就是钩子（Hooks）。</p></blockquote><p>设想这样一个数字：<strong>某一周里，让 Claude 改完代码后，手动补敲 <code>prettier --write</code> 的次数，是 23 次</strong>。</p><p>23 次。同一个动作，机械地重复了 23 遍。更离谱的是中间还漏了两回——提交上去之后被 CI 的格式检查打回来，又得重新跑一遍流程。</p><p>这时候就该想一个事：<strong>这种「每次都得做、做的内容还一模一样」的动作，凭什么要靠人记着、靠 Claude 自觉？</strong> 在 CLAUDE.md 里写过「改完文件记得跑 prettier」，可它三次里总有一次想不起来——因为那只是一句<strong>请求</strong>，不是<strong>保证</strong>。</p><p>而配一个钩子（Hook），一行配置，问题就彻底没了：<strong>从那以后，Claude 每编辑完一个文件，格式化就自动跑，再没手敲过一次 prettier，也再没被 CI 打回来过</strong>。这一篇，就把这个能「自动扣扳机」的东西，从是什么讲到怎么配、怎么调。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>一句话讲明白 Hook 是什么、它跟「写进 CLAUDE.md 的请求」差在哪个根本点上</li><li>Claude 干活的生命周期里，到底有哪些「时机」能挂钩子（<code>PreToolUse</code> / <code>PostToolUse</code> / <code>Stop</code> / <code>SessionStart</code> 等）</li><li>钩子配在哪个文件、<code>matcher</code> 怎么把它收窄到「只在改文件时触发」</li><li>三个能直接抄走的真实例子：改完自动格式化、拦危险命令、干完发通知</li><li>钩子和 Claude 之间靠什么对话（stdin 的 JSON、退出码、stdout）——这是看懂一切的钥匙</li><li>钩子不触发、报错时，怎么一步步查出来</li></ul><hr><h2 id="_01-先搞懂-hook-到底是什么、强在哪个「保证」上" tabindex="-1">01 先搞懂：Hook 到底是什么、强在哪个「保证」上 <a class="header-anchor" href="#_01-先搞懂-hook-到底是什么、强在哪个「保证」上" aria-label="Permalink to &quot;01 先搞懂：Hook 到底是什么、强在哪个「保证」上&quot;">​</a></h2><p>先给结论：<strong>Hook 是「某个事件一发生，就自动执行的一段命令或请求」——它不靠 Claude 思考决定要不要做，触发是有保证的。</strong>（最常用的是 shell 命令，此外还支持 HTTP 端点、MCP 工具、LLM 提示等形式。）</p><p>官方的定义很干脆，先放这儿：</p><blockquote><p>Hooks 是用户定义的 shell 命令，在 Claude Code 生命周期中的特定点执行。它们对 Claude Code 的行为提供确定性控制，确保某些操作始终发生，而不是依赖 LLM 选择运行它们。</p></blockquote><p>注意里头两个词：<strong>「确定性控制」「始终发生」</strong>。这就是 Hook 的命门所在。</p><p><strong>类比：家里贴的自动化规则（「当……就……」）。</strong> 你给智能家居设过那种规则吧——「<strong>当</strong>有人开门，<strong>就</strong>亮灯」「<strong>当</strong>我离家，<strong>就</strong>关掉所有插座」。条件一满足，动作必然发生，不需要谁记得。Hook 就是给 Claude Code 装的这种规则——你定义「<strong>当</strong>某个事件发生，<strong>就</strong>跑这段命令」，它自动执行，雷打不动。</p><p>这里要把一个<strong>最关键的区别</strong>钉死：<strong>写进 CLAUDE.md 的是「请求」</strong>——它大概率照办，但可能漏；<strong>配成 Hook 的是「保证」</strong>——只要那个事件触发，动作一定执行，跟 Claude 记不记得没半点关系。官方的话：</p><blockquote><p>CLAUDE.md 或 skill 中的「永远不要编辑 <code>.env</code>」之类的说明是请求，而不是保证。阻止编辑的 <code>PreToolUse</code> hook 是强制执行。</p></blockquote><p>这就是开头那 23 次的根源：「改完跑 prettier」写在 CLAUDE.md 里是请求，三次漏一次；配成 Hook 是保证，一次不漏。</p><p>几个你大概率会遇到、值得「上保证」的场景，先感受一下：</p><ul><li><strong>「每次改完文件，自动格式化 / 跑 lint」</strong>——别再手敲，也别指望它自觉</li><li><strong>「<code>rm -rf</code>、删生产库这种命令，给我硬拦死」</strong>——要确定地拦住，不能靠提示</li><li><strong>「它干完活、或者等我输入时，给我发条桌面通知」</strong>——你好切去干别的，不用盯着终端</li></ul><blockquote><p>💡 一句话总结：Hook 是「事件触发的自动动作」，核心价值是<strong>把「请求」变成「保证」</strong>——CLAUDE.md 拜托它做的事可能漏，Hook 挂上的事件一触发就必然执行。</p></blockquote><hr><h2 id="_02-有哪些「时机」能挂钩子-认识生命周期事件" tabindex="-1">02 有哪些「时机」能挂钩子：认识生命周期事件 <a class="header-anchor" href="#_02-有哪些「时机」能挂钩子-认识生命周期事件" aria-label="Permalink to &quot;02 有哪些「时机」能挂钩子：认识生命周期事件&quot;">​</a></h2><p>Hook 不是随便什么时候都能挂，它得挂在 Claude 干活流程里<strong>特定的「时机」<strong>上。这些时机，官方叫</strong>事件（event）</strong>。想用好 Hook，第一步就是认清「我想让这事在什么时候发生」。</p><p>回想第 03 篇讲的「代理循环」——Claude 干活是「想 → 做 → 看」转圈。这些事件，正好散布在这个循环的前前后后。官方把它们按<strong>触发频率</strong>分成了三档，这个分法特别好记：</p><ul><li><strong>每个会话一次</strong>：<code>SessionStart</code>（会话开始 / 恢复时）、<code>SessionEnd</code>（会话结束时）</li><li><strong>每一轮对话一次</strong>：<code>UserPromptSubmit</code>（你刚提交提示、Claude 还没开始处理时）、<code>Stop</code>（Claude 答完这一轮时）</li><li><strong>代理循环里每次工具调用</strong>：<code>PreToolUse</code>（某个工具<strong>就要执行前</strong>）、<code>PostToolUse</code>（某个工具<strong>成功执行后</strong>）</li></ul><p>光说有点抽象，画张图你一眼就懂这几个最常用的事件卡在哪儿：</p><p><img src="'+e+`" alt="Claude Code 钩子 7 个时机：SessionStart → UserPromptSubmit → Pre/Post 工具循环 → Stop → SessionEnd"></p><p>这张图把「想→做→看」的循环摊开了：一进会话是 <code>SessionStart</code>，你说话是 <code>UserPromptSubmit</code>，然后进入「要不要用工具」的循环——每次动工具，前面有 <code>PreToolUse</code>、后面有 <code>PostToolUse</code>，循环转完这一轮就是 <code>Stop</code>，整个会话收摊是 <code>SessionEnd</code>。<strong>你想让动作在哪一步发生，就挂对应的那个事件。</strong></p><p>这六个是日常用得最多的。其实官方支持的事件有三十来个（比如压缩前后的 <code>PreCompact</code>/<code>PostCompact</code>、文件落盘变动的 <code>FileChanged</code>、配置被改的 <code>ConfigChange</code>、子代理起停的 <code>SubagentStart</code>/<code>SubagentStop</code> 等），但<strong>对小白来说，先把下面这四个吃透，能覆盖九成场景</strong>：</p><table tabindex="0"><thead><tr><th>事件</th><th>什么时候触发</th><th>最典型的用法</th></tr></thead><tbody><tr><td><strong><code>PreToolUse</code></strong></td><td>某个工具<strong>执行前</strong></td><td>拦危险命令、保护敏感文件（<strong>能阻止操作</strong>）</td></tr><tr><td><strong><code>PostToolUse</code></strong></td><td>某个工具<strong>成功执行后</strong></td><td>改完文件自动格式化 / 跑 lint</td></tr><tr><td><strong><code>Stop</code></strong></td><td>Claude 答完这一轮</td><td>提醒「活还没干完，继续」、扫一遍工作区</td></tr><tr><td><strong><code>SessionStart</code></strong></td><td>会话开始或恢复时</td><td>往上下文里注入项目状态（如最近的提交）</td></tr></tbody></table><p>记这张表有个窍门：<strong>看名字里的 <code>Pre</code> 和 <code>Post</code></strong>——<code>Pre</code> 是「之前」，所以只有它能在动作发生前<strong>拦住</strong>；<code>Post</code> 是「之后」，工具都跑完了，它只能「事后补一刀」（格式化、记日志），拦不了。这个差别下一节细说。</p><blockquote><p>💡 一句话总结：Hook 挂在 Claude 生命周期的<strong>特定事件</strong>上，按频率分三档（每会话 / 每轮 / 每次工具调用）；新手先吃透 <code>PreToolUse</code>（前，能拦）、<code>PostToolUse</code>（后，补刀）、<code>Stop</code>（答完）、<code>SessionStart</code>（开场）这四个就够用。</p></blockquote><hr><h2 id="_03-钩子配在哪、matcher-怎么把它收窄" tabindex="-1">03 钩子配在哪、<code>matcher</code> 怎么把它收窄 <a class="header-anchor" href="#_03-钩子配在哪、matcher-怎么把它收窄" aria-label="Permalink to &quot;03 钩子配在哪、\`matcher\` 怎么把它收窄&quot;">​</a></h2><p>知道了能挂哪些事件，来看怎么写。Hook 写在<strong>设置文件</strong>（settings.json）里——就是第 31 篇专门讲过的那套配置文件。<strong>写在哪个文件，决定了它管多大范围</strong>：</p><table tabindex="0"><thead><tr><th>配在哪个文件</th><th>生效范围</th><th>能共享给团队吗</th></tr></thead><tbody><tr><td><code>~/.claude/settings.json</code></td><td><strong>你所有项目</strong></td><td>否，只在你这台机器</td></tr><tr><td><code>.claude/settings.json</code>（项目根）</td><td><strong>仅当前项目</strong></td><td><strong>是</strong>，可以提交进 git</td></tr><tr><td><code>.claude/settings.local.json</code>（项目根）</td><td>仅当前项目</td><td>否，被 gitignore</td></tr></tbody></table><p>这跟第 31 篇讲配置时的「项目档案柜 vs 工位抽屉」是同一套逻辑：<strong>全队都该有的 Hook（比如「改完一律格式化」）写进项目的 <code>.claude/settings.json</code> 提交进 git；只是你自己想要的（比如发通知到你的桌面）写进 <code>~/.claude/settings.json</code></strong>。</p><h3 id="hook-配置长什么样" tabindex="-1">Hook 配置长什么样 <a class="header-anchor" href="#hook-配置长什么样" aria-label="Permalink to &quot;Hook 配置长什么样&quot;">​</a></h3><p>先看一个最小的完整例子——<strong>「每次用 Edit 或 Write 改完文件，自动跑 prettier 格式化」</strong>，就是治好那 23 次毛病的那段。写进项目根目录的 <code>.claude/settings.json</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Edit|Write&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jq -r &#39;.tool_input.file_path&#39; | xargs npx prettier --write&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>别被这层嵌套吓到，它就三层，对着拆一下你立刻懂：</p><ol><li><strong><code>&quot;PostToolUse&quot;</code></strong>——挂哪个<strong>事件</strong>（这里：工具执行后）。</li><li><strong><code>&quot;matcher&quot;: &quot;Edit|Write&quot;</code></strong>——<strong>收窄到哪些工具</strong>才触发（这里：只在 <code>Edit</code> 或 <code>Write</code> 工具之后，不在 <code>Bash</code>、<code>Read</code> 之后）。</li><li><strong>里层的 <code>hooks</code> 数组</strong>——真正要跑的<strong>动作</strong>：<code>&quot;type&quot;: &quot;command&quot;</code> 表示跑一条 shell 命令，<code>&quot;command&quot;</code> 就是那条命令。</li></ol><p>这条命令里的 <code>jq</code> 是个解析 JSON 的小工具（Mac 用 <code>brew install jq</code> 装，Ubuntu 用 <code>apt-get install jq</code>）。它的作用下一节讲——简单说就是从 Claude 递来的数据里，<strong>把刚改的那个文件路径抠出来</strong>，喂给 prettier。</p><h3 id="matcher-让钩子「只在该触发的时候触发」" tabindex="-1"><code>matcher</code>：让钩子「只在该触发的时候触发」 <a class="header-anchor" href="#matcher-让钩子「只在该触发的时候触发」" aria-label="Permalink to &quot;\`matcher\`：让钩子「只在该触发的时候触发」&quot;">​</a></h3><p><code>matcher</code> 是 Hook 配置里<strong>最该搞懂的一个字段</strong>。一句话：<strong>没有它，钩子会在那个事件的「每一次」都触发；有了它，你能把范围收窄</strong>。</p><p><strong>类比：保安的进门规定——不是来送货的，不进大门。</strong> 没有 <code>matcher</code> 的钩子就像让保安「任何人来都登记」，效率很低；有了 <code>matcher</code>，就变成「<strong>只有快递员</strong>来才登记，其他人直接放行」。<code>matcher</code> 干的就是这个「划定触发范围」的活。</p><p>对工具类事件（<code>PreToolUse</code>/<code>PostToolUse</code>），<code>matcher</code> 匹配的是<strong>工具名</strong>。它的写法有三种，看这张表：</p><table tabindex="0"><thead><tr><th>你写的 matcher</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td><code>&quot;Edit|Write&quot;</code></td><td>精确匹配这几个工具（<code>|</code> 是「或」）</td><td>只在 Edit 或 Write 之后触发</td></tr><tr><td><code>&quot;Bash&quot;</code></td><td>精确匹配单个工具</td><td>只在跑 Bash 命令时触发</td></tr><tr><td><code>&quot;&quot;</code> 或省略</td><td><strong>匹配所有</strong>，每次都触发</td><td>该事件每次发生都跑</td></tr></tbody></table><p>注意：<strong><code>matcher</code> 区分大小写</strong>，写成 <code>edit</code> 是匹配不上 <code>Edit</code> 工具的——这是新手钩子不触发最常见的原因之一。</p><p>还有一点新手容易忽略：<strong>有些事件压根不支持 <code>matcher</code></strong>（比如 <code>UserPromptSubmit</code>、<code>Stop</code>），因为它们没有「工具名」这种东西可筛，总是每次都触发。给这些事件加 <code>matcher</code>，会被静默忽略。</p><blockquote><p>💡 一句话总结：Hook 写进 <code>settings.json</code>（全局放主目录、项目放 <code>.claude/</code>），配置就三层——<strong>事件、matcher、动作</strong>；<code>matcher</code> 负责把钩子收窄到「只在该触发的工具上触发」，且<strong>区分大小写</strong>。</p></blockquote><hr><h2 id="_04-钩子和-claude-怎么对话-stdin、退出码、stdout" tabindex="-1">04 钩子和 Claude 怎么对话：stdin、退出码、stdout <a class="header-anchor" href="#_04-钩子和-claude-怎么对话-stdin、退出码、stdout" aria-label="Permalink to &quot;04 钩子和 Claude 怎么对话：stdin、退出码、stdout&quot;">​</a></h2><p>这一节是<strong>看懂一切的钥匙</strong>。前面那条 <code>jq</code> 命令为什么能拿到文件路径？钩子怎么「拦」住一条命令？答案全在这套「对话机制」里。</p><p>机制本身极简单，就三条管道：<strong>Claude 把事件数据从 <code>stdin</code> 喂给你的脚本 → 脚本干活 → 脚本用「退出码 + stdout」告诉 Claude 接下来怎么办</strong>。逐个拆。</p><h3 id="输入-claude-从-stdin-递给你一坨-json" tabindex="-1">输入：Claude 从 stdin 递给你一坨 JSON <a class="header-anchor" href="#输入-claude-从-stdin-递给你一坨-json" aria-label="Permalink to &quot;输入：Claude 从 stdin 递给你一坨 JSON&quot;">​</a></h3><p>事件一触发，Claude Code 会把<strong>这个事件的相关数据</strong>，作为一段 JSON 从标准输入（stdin）塞给你的命令。比如 Claude 要跑 Bash 命令时，<code>PreToolUse</code> 钩子收到的大概长这样：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;session_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;abc123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cwd&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/Users/sarah/myproject&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hook_event_name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tool_name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tool_input&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;npm test&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>看到了吧——<strong>Claude 要干什么、用哪个工具、参数是什么，全在里头</strong>。第 03 节那条 <code>jq -r &#39;.tool_input.file_path&#39;</code>，干的就是从这坨 JSON 里把 <code>tool_input.file_path</code>（要改的文件路径）抠出来。<code>jq</code> 就是专门解析 JSON 的工具，<code>-r</code> 是让它输出纯文本（不带引号）。</p><h3 id="输出-用「退出码」告诉-claude-下一步" tabindex="-1">输出：用「退出码」告诉 Claude 下一步 <a class="header-anchor" href="#输出-用「退出码」告诉-claude-下一步" aria-label="Permalink to &quot;输出：用「退出码」告诉 Claude 下一步&quot;">​</a></h3><p>脚本干完活，靠<strong>退出码</strong>（exit code）给 Claude 下指令。这是 Hook 最核心的约定，<strong>记住三个数就行</strong>：</p><table tabindex="0"><thead><tr><th>退出码</th><th>含义</th><th>效果</th></tr></thead><tbody><tr><td><strong><code>0</code></strong></td><td>没意见，正常走</td><td>操作继续（<code>PreToolUse</code> 时<strong>不等于批准</strong>，照常走权限流程）</td></tr><tr><td><strong><code>2</code></strong></td><td><strong>拦住！</strong></td><td>操作被阻止；你写到 <strong>stderr</strong> 的内容会作为反馈递给 Claude，让它调整</td></tr><tr><td>其他（如 1）</td><td>出错了，但不拦</td><td>操作继续，终端显示一条 hook 报错提示</td></tr></tbody></table><p><strong>重点是 <code>exit 2</code></strong>——这是钩子「踩刹车」的唯一方式。注意一个反直觉的坑：</p><blockquote><p>对于大多数 hook 事件，仅退出代码 2 阻止操作。Claude Code 将退出代码 1 视为非阻止错误并继续操作，尽管 1 是传统的 Unix 失败代码。如果您的 hook 旨在强制执行策略，请使用 <code>exit 2</code>。</p></blockquote><p>翻成人话：<strong>想拦操作，必须 <code>exit 2</code>，不是 <code>exit 1</code></strong>。很多人按 Unix 习惯写了 <code>exit 1</code>，结果钩子「报了错但没拦住」，命令照样跑了。这是第一次写拦截钩子时最容易栽的跟头——脚本明明判断出了危险命令、也打印了警告，但因为顺手写成 <code>exit 1</code>，Claude 该跑还是跑了，吓人一身汗。</p><p>还有个细节关系到「能不能拦住」：<strong>只有 <code>Pre</code> 类事件能真正拦操作</strong>。<code>PostToolUse</code> 收到 <code>exit 2</code> 也拦不住——因为工具已经跑完了，木已成舟，它只能把 stderr 显示给 Claude 看。这就呼应了上一节那句「<code>Pre</code> 能拦、<code>Post</code> 只能补刀」。</p><h3 id="进阶-用-stdout-返回-json-做更细的控制" tabindex="-1">进阶：用 stdout 返回 JSON，做更细的控制 <a class="header-anchor" href="#进阶-用-stdout-返回-json-做更细的控制" aria-label="Permalink to &quot;进阶：用 stdout 返回 JSON，做更细的控制&quot;">​</a></h3><p>退出码只能「拦 / 不拦」两档。想要更细的控制（比如<strong>拦的同时告诉 Claude 具体原因</strong>、或往它上下文里<strong>注入一段信息</strong>），就改成 <code>exit 0</code> 然后往 <strong>stdout</strong> 打印一段 JSON。</p><blockquote><p>用退出码 2 配 stderr 来「阻止」，或用 JSON 配退出码 0 来做「结构化控制」。<strong>两者不要混用</strong>：你退出 2 时，Claude Code 会忽略 JSON。</p></blockquote><p>举两个最常见的 JSON 输出：</p><p><strong>① <code>PreToolUse</code> 想拦截、并说明理由</strong>——用 <code>permissionDecision</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hookSpecificOutput&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;hookEventName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;permissionDecision&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;permissionDecisionReason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;这条命令会动生产库，禁止执行&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>permissionDecision</code> 有四个值：<code>&quot;deny&quot;</code>（拦掉，把理由发给 Claude）、<code>&quot;ask&quot;</code>（照常弹权限框问你）、<code>&quot;allow&quot;</code>（跳过权限框直接放行）、<code>&quot;defer&quot;</code>（延迟执行，让工具稍后恢复，适合非交互模式下的异步审批场景）。</p><p>这里有条<strong>安全红线</strong>必须讲清楚，呼应第 20、21 篇的权限与安全：<strong>钩子返回 <code>&quot;allow&quot;</code>，不能绕过你设置里的拒绝规则</strong>。官方原话——</p><blockquote><p>返回 <code>&quot;allow&quot;</code> 跳过交互式提示但不覆盖权限规则。如果拒绝规则与工具调用匹配，即使你的 hook 返回 <code>&quot;allow&quot;</code>，调用也会被阻止。</p></blockquote><p>也就是说：<strong>Hook 只能「收紧」限制，不能「放松」到超过权限规则允许的范围</strong>。这是个很重要的安全设计——它保证了恶意钩子没法靠返回 <code>allow</code> 把你的安全护栏拆了。反过来，<code>PreToolUse</code> 钩子的拦截优先级极高：<strong>哪怕你开了 <code>--dangerously-skip-permissions</code>（跳过所有权限），一个返回 <code>deny</code> 的钩子照样能拦住</strong>。所以拿 Hook 来强制团队红线，是真·拦得死。</p><p><strong>② <code>SessionStart</code> 想往上下文里塞点信息</strong>——直接往 stdout 打印文本就行（这几个事件特殊，stdout 会被当成上下文喂给 Claude）。比如会话一开就把最近 5 条提交告诉它：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;SessionStart&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;git log --oneline -5&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p>💡 一句话总结：钩子和 Claude 靠三条管道对话——<strong>stdin 喂 JSON 进来、退出码下指令、stdout 做精细控制</strong>；记死「<code>exit 2</code> 才拦得住（不是 1）」「退出码和 JSON 别混用」「Hook 只能收紧、不能放松权限」这三条，就抓住了七成。</p></blockquote><hr><h2 id="_05-三个能直接抄走的真实例子" tabindex="-1">05 三个能直接抄走的真实例子 <a class="header-anchor" href="#_05-三个能直接抄走的真实例子" aria-label="Permalink to &quot;05 三个能直接抄走的真实例子&quot;">​</a></h2><p>理论够了，上三个常用、你也能直接抄的钩子。每个都标清楚「挂哪个事件、收窄到哪、配在哪个文件」。</p><h3 id="例子一-改完文件自动格式化-posttooluse" tabindex="-1">例子一：改完文件自动格式化（<code>PostToolUse</code>） <a class="header-anchor" href="#例子一-改完文件自动格式化-posttooluse" aria-label="Permalink to &quot;例子一：改完文件自动格式化（\`PostToolUse\`）&quot;">​</a></h3><p>就是治好那 23 次毛病的那个，最实用、零风险，<strong>强烈建议每个项目都配上</strong>。写进项目根的 <code>.claude/settings.json</code>：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Edit|Write&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jq -r &#39;.tool_input.file_path&#39; | xargs npx prettier --write&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>逻辑</strong>：Claude 每次用 <code>Edit</code>/<code>Write</code> 改完文件 → 钩子从 stdin 的 JSON 里抠出文件路径 → 丢给 <code>prettier --write</code> 格式化。从此格式永远统一，不用你操心。把 <code>prettier</code> 换成 <code>eslint --fix</code>、<code>gofmt</code>、<code>black</code> 都是一个套路。</p><h3 id="例子二-拦掉危险命令-pretooluse-脚本" tabindex="-1">例子二：拦掉危险命令（<code>PreToolUse</code> + 脚本） <a class="header-anchor" href="#例子二-拦掉危险命令-pretooluse-脚本" aria-label="Permalink to &quot;例子二：拦掉危险命令（\`PreToolUse\` + 脚本）&quot;">​</a></h3><p>这个用上了「<code>exit 2</code> 拦截」。命令复杂时，把逻辑写进一个<strong>单独的脚本</strong>比硬塞进 JSON 清爽得多。</p><p><strong>第一步</strong>，把脚本存到 <code>.claude/hooks/block-dangerous.sh</code>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/bin/bash</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># block-dangerous.sh：拦截 rm -rf 这类危险命令</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">INPUT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">COMMAND</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$INPUT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;.tool_input.command&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$COMMAND</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -q</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;rm -rf&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Blocked: 检测到 rm -rf，已拦截&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&amp;2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 写到 stderr，会反馈给 Claude</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                       # exit 2 = 阻止这次工具调用</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 其余命令放行，走正常权限流程</span></span></code></pre></div><p><strong>第二步</strong>，给脚本加可执行权限（Mac/Linux 必须，否则 Claude 跑不了它）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +x</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .claude/hooks/block-dangerous.sh</span></span></code></pre></div><p><strong>第三步</strong>，在 <code>.claude/settings.json</code> 里注册它，挂到 <code>Bash</code> 工具的 <code>PreToolUse</code> 上：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PreToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$CLAUDE_PROJECT_DIR</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.claude/hooks/block-dangerous.sh&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>这里那个 <code>$CLAUDE_PROJECT_DIR</code> 是 Claude Code 提供的环境变量，指向<strong>项目根目录</strong>——用它拼路径，钩子不管在哪个子目录跑都能找到脚本，比写死的相对路径稳。</p><blockquote><p>⚠️ 这一节回到第 21 篇的安全主线：<strong>钩子是用你的完整用户权限跑的 shell，能删你能删的任何文件</strong>。官方反复强调，加任何钩子前先审一遍它的命令，尤其别从来路不明的地方抄整段脚本就往设置里塞。</p></blockquote><h3 id="例子三-它需要你输入时-发条桌面通知-notification" tabindex="-1">例子三：它需要你输入时，发条桌面通知（<code>Notification</code>） <a class="header-anchor" href="#例子三-它需要你输入时-发条桌面通知-notification" aria-label="Permalink to &quot;例子三：它需要你输入时，发条桌面通知（\`Notification\`）&quot;">​</a></h3><p>Claude 干到一半要你批准、或者答完等你下一句时，你可能早切去刷别的了。挂个通知钩子，它就主动喊你。这个用 <code>Notification</code> 事件（Claude 发通知时触发）。</p><p><strong>macOS</strong> 写进 <code>~/.claude/settings.json</code>（这种「叫我」的钩子是你个人偏好，放全局）：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;Notification&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;osascript -e &#39;display notification </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Claude Code 在等你</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> with title </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Claude Code</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>平台差异</strong>（官方给的原生命令，照抄即可）：</p><table tabindex="0"><thead><tr><th>平台</th><th>通知命令（填进 <code>command</code>）</th></tr></thead><tbody><tr><td><strong>macOS</strong></td><td><code>osascript -e &#39;display notification &quot;...&quot; with title &quot;Claude Code&quot;&#39;</code></td></tr><tr><td><strong>Linux</strong></td><td><code>notify-send &#39;Claude Code&#39; &#39;...&#39;</code></td></tr><tr><td><strong>Windows</strong></td><td>用 PowerShell 的 <code>MessageBox</code>（官方文档有完整片段）</td></tr></tbody></table><p>macOS 上如果通知没弹出来，多半是 Script Editor 没拿到通知权限——去「系统设置 → 通知」里找到 Script Editor 把开关打开。第一次配很容易死活没声响，折腾半天才发现是这个权限没给。</p><blockquote><p>💡 一句话总结：三个钩子按风险递增——<strong>格式化（<code>PostToolUse</code>，零风险，建议人人配）、拦命令（<code>PreToolUse</code>+脚本，记得 <code>chmod +x</code> 和 <code>exit 2</code>）、发通知（<code>Notification</code>，平台命令不同）</strong>；脚本路径用 <code>$CLAUDE_PROJECT_DIR</code> 拼最稳。</p></blockquote><hr><h2 id="_06-动手-5-分钟配一个钩子并亲眼看它触发" tabindex="-1">06 动手：5 分钟配一个钩子并亲眼看它触发 <a class="header-anchor" href="#_06-动手-5-分钟配一个钩子并亲眼看它触发" aria-label="Permalink to &quot;06 动手：5 分钟配一个钩子并亲眼看它触发&quot;">​</a></h2><p>光看不练记不住。下面带你配一个<strong>最安全、最容易看到效果</strong>的钩子——<strong>每次 Claude 跑完 Bash 命令，就把这条命令记进一个日志文件</strong>。全程不动你任何代码，纯加一段配置，跑完能亲眼验证。</p><p>这个练习用到 <code>jq</code>。没装的话：Mac 跑 <code>brew install jq</code>，Ubuntu 跑 <code>sudo apt-get install jq</code>。装不装得上不需要魔法上网。</p><p><strong>第一步：找一个练手目录，打开它的项目设置文件</strong></p><p>随便找个空目录（别在重要项目里练），在里头建 <code>.claude/settings.json</code>。如果文件已存在且有别的内容，把 <code>hooks</code> 这块作为新键加进去，别整个覆盖。文件内容：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;PostToolUse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;matcher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;hooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            &quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jq -r &#39;.tool_input.command&#39; &gt;&gt; ~/claude-bash-log.txt&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>这条钩子：每次 <code>Bash</code> 工具跑完（<code>PostToolUse</code> + <code>matcher: &quot;Bash&quot;</code>）→ 从 stdin 的 JSON 里抠出命令 → 用 <code>&gt;&gt;</code> 追加写进主目录的 <code>claude-bash-log.txt</code>。</p><p><strong>第二步：在这个目录里启动 Claude，确认钩子已注册</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span></span></code></pre></div><p>进去后输入 <code>/hooks</code>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/hooks</span></span></code></pre></div><p><strong>预期</strong>：弹出一个<strong>只读</strong>的钩子浏览器，列出所有事件。找到 <code>PostToolUse</code>，旁边应该显示有 1 个钩子。选中它能看到详情：事件、matcher（<code>Bash</code>）、来源文件（<code>Project</code>，即项目的 <code>.claude/settings.json</code>）、还有那条命令。<strong>看到它在列表里 = 钩子注册成功了。</strong></p><p><code>/hooks</code> 菜单是<strong>只读</strong>的——它只能让你查、不能加和改。要改钩子，直接编辑 <code>settings.json</code>，或者直接让 Claude 帮你改。</p><p><strong>第三步：让 Claude 跑条 Bash 命令，触发钩子</strong></p><p>按 <code>Esc</code> 回到对话，让它跑个无害的命令：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>帮我用 ls 看一下当前目录有哪些文件</span></span></code></pre></div><p>它会调用 <code>Bash</code> 工具跑 <code>ls</code>。<strong>这一跑，<code>PostToolUse</code> 钩子就该触发了。</strong>（钩子成功执行时是「静默」的，终端不会特意提示——这是正常的。）</p><p><strong>第四步：验证钩子真跑了——查日志文件</strong></p><p>新开一个终端，看日志：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/claude-bash-log.txt</span></span></code></pre></div><p><strong>预期</strong>：文件里出现了刚才那条 <code>ls</code> 命令（以及这个会话里 Claude 跑过的其他 Bash 命令）。<strong>看到命令被记下来了 = 钩子真的在每次 Bash 后自动触发了</strong>，你这条「自动化规则」立起来了。</p><p><strong>第五步：清理（可选）</strong></p><p>练完拆掉很简单——把 <code>.claude/settings.json</code> 里那段 <code>hooks</code> 删掉就行（钩子没有单独的「删除命令」，从配置里移除条目即可）。日志文件 <code>rm ~/claude-bash-log.txt</code> 删掉。</p><p>跑通这五步，你就把「<strong>写配置 → <code>/hooks</code> 确认注册 → 触发事件 → 验证副作用</strong>」这条完整链路亲手走了一遍。<strong>以后配任何钩子，本质都是这套流程</strong>，无非换个事件、换个 matcher、换条命令。</p><blockquote><p>💡 一句话总结：练手就配「记录 Bash 命令」这种零风险钩子最稳——<strong>写进 <code>.claude/settings.json</code>、用 <code>/hooks</code> 看它注册、让 Claude 跑命令触发、查日志文件验证</strong>；亲眼看到副作用发生，比记十条定义都顶用。</p></blockquote><hr><h2 id="_07-钩子不触发-报错了-怎么查" tabindex="-1">07 钩子不触发 / 报错了，怎么查 <a class="header-anchor" href="#_07-钩子不触发-报错了-怎么查" aria-label="Permalink to &quot;07 钩子不触发 / 报错了，怎么查&quot;">​</a></h2><p>Hook 配好了不灵，是新手最常见的卡点。别瞎猜，按下面这个顺序查，基本都能定位。我把官方的排查清单整理成了「症状 → 怎么查」：</p><table tabindex="0"><thead><tr><th>症状</th><th>最可能的原因 / 怎么查</th></tr></thead><tbody><tr><td><strong>钩子压根不触发</strong></td><td>① 跑 <code>/hooks</code> 看它到底注册了没；② <code>matcher</code> 区分大小写，<code>edit</code> 匹配不上 <code>Edit</code>；③ 事件挂错了（想拦操作要用 <code>PreToolUse</code>，不是 <code>PostToolUse</code>）</td></tr><tr><td><strong><code>/hooks</code> 里根本没有我配的钩子</strong></td><td>① JSON 格式错了（<strong>JSON 不允许尾逗号、不允许注释</strong>）；② 文件位置错了（项目钩子在 <code>.claude/settings.json</code>，全局在 <code>~/.claude/settings.json</code>）；③ 改完没生效就重启一次会话</td></tr><tr><td><strong>终端报 <code>hook error</code></strong></td><td>脚本意外非零退出。手动测一下（见下方命令）；报 <code>command not found</code> 多半是脚本路径不对，用绝对路径或 <code>$CLAUDE_PROJECT_DIR</code>；报 <code>jq: command not found</code> 就是没装 <code>jq</code></td></tr><tr><td><strong>脚本没跑起来</strong></td><td>Mac/Linux 上脚本忘了加可执行权限，补 <code>chmod +x</code></td></tr><tr><td><strong>想拦却没拦住</strong></td><td>八成是写了 <code>exit 1</code>，改成 <code>exit 2</code>（见第 04 节那个坑）</td></tr></tbody></table><p>两个<strong>最实用的排查手段</strong>单独拎出来：</p><p><strong>① 手动喂假数据测脚本</strong>。不用真在 Claude 里触发，自己造一段 JSON 管道喂给脚本，看它退出码对不对：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;tool_name&quot;:&quot;Bash&quot;,&quot;tool_input&quot;:{&quot;command&quot;:&quot;rm -rf /tmp/x&quot;}}&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ./block-dangerous.sh</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> $?</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 看退出码：拦截脚本这里应该输出 2</span></span></code></pre></div><p>这是调拦截钩子的标准动作——<strong>先把脚本单独喂熟了，再挂到 Claude 上</strong>，省得在真实会话里反复试。</p><p><strong>② 开调试日志看细节</strong>。想看「到底哪些钩子匹配了、退出码多少、stdout/stderr 是啥」，用 <code>--debug</code> 启动，日志会写进 <code>~/.claude/debug/&lt;会话id&gt;.txt</code>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --debug</span></span></code></pre></div><p>或者会话里直接敲 <code>/debug</code> 也能开。日志里会有类似这样的行，一眼看出钩子有没有跑、跑成啥样：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[DEBUG] Executing hooks for PostToolUse:Bash</span></span>
<span class="line"><span>[DEBUG] Hook command completed with status 0</span></span></code></pre></div><p>还有个一键开关值得知道：<strong>想临时关掉所有钩子</strong>（比如怀疑某个钩子在捣乱），在设置文件里加一句 <code>&quot;disableAllHooks&quot;: true</code> 就行，不用一个个删。</p><blockquote><p>💡 一句话总结：钩子不灵别瞎猜，按「<strong><code>/hooks</code> 看注册 → 查 matcher 大小写和事件选对没 → 手动喂 JSON 测脚本 → <code>--debug</code> 看日志</strong>」的顺序查；想拦没拦住先看是不是写了 <code>exit 1</code>。</p></blockquote><hr><h2 id="_08-小结" tabindex="-1">08 小结 <a class="header-anchor" href="#_08-小结" aria-label="Permalink to &quot;08 小结&quot;">​</a></h2><p>这一篇我们把 Hook 从「是什么」一路讲到「怎么配、怎么调」——<strong>它是给 Claude Code 装的「自动化规则」：某个事件一触发，就雷打不动替你跑一段动作</strong>。</p><p>把核心串起来回顾：</p><table tabindex="0"><thead><tr><th>你想干的事</th><th>怎么落地</th><th>关键点</th></tr></thead><tbody><tr><td>理解 Hook 是什么</td><td>事件触发的自动 shell 命令</td><td>把「请求」变成「保证」，不靠 Claude 自觉</td></tr><tr><td>选对挂载时机</td><td>认生命周期事件</td><td><code>Pre</code> 能拦、<code>Post</code> 补刀、<code>Stop</code> 答完、<code>SessionStart</code> 开场</td></tr><tr><td>写一个钩子</td><td>配进 <code>settings.json</code></td><td>三层：事件 + <code>matcher</code>（区分大小写）+ 动作</td></tr><tr><td>让钩子和 Claude 对话</td><td>stdin / 退出码 / stdout</td><td><strong><code>exit 2</code> 才拦得住</strong>，退出码和 JSON 别混用</td></tr><tr><td>拦危险操作</td><td><code>PreToolUse</code> + 脚本</td><td>记得 <code>chmod +x</code>，Hook 只能收紧不能放松权限</td></tr><tr><td>钩子不灵了</td><td>按顺序排查</td><td><code>/hooks</code> 看注册、手动喂 JSON 测、<code>--debug</code> 看日志</td></tr></tbody></table><p><strong>你现在应该能：</strong> 说清 Hook 和「写进 CLAUDE.md 的请求」差在哪个根本点（保证 vs 请求）；知道 <code>PreToolUse</code>/<code>PostToolUse</code>/<code>Stop</code>/<code>SessionStart</code> 各自挂在 Claude 干活流程的哪一步；照着模板往 <code>settings.json</code> 里写一个带 <code>matcher</code> 的钩子；看懂钩子靠 stdin/退出码/stdout 跟 Claude 对话，并记死「<code>exit 2</code> 才能拦」；钩子不触发时知道从 <code>/hooks</code> 和 <code>--debug</code> 入手查。<strong>开头那 23 次手敲 prettier 的苦差，到这儿你已经有能力用一行配置永久解决了。</strong></p><p>Hook 是「系统配置与优化」这一组里很硬核的一块——它让你对 Claude 的行为有了<strong>确定性</strong>的掌控，而不只是「拜托它」。</p><hr><p>下一篇 <strong>34「CLI 参考手册：命令与全部标志」</strong>——这一路你敲了不少 <code>claude</code> 开头的命令、用了 <code>--debug</code>、<code>--dangerously-skip-permissions</code> 这些标志，但它们其实只是冰山一角。下一篇把 <code>claude</code> 命令行的<strong>全部命令和标志</strong>系统梳一遍，当成一本随手能翻的「字典」。想想看：你现在能脱口而出的 <code>claude</code> 标志有几个？翻完那篇，你会发现自己之前<strong>至少漏用了一半能省事的开关</strong>。</p>`,154)])])}const E=t(n,[["render",d]]);export{g as __pageData,E as default};
