import{_ as s,o as i,c as e,a2 as n}from"./chunks/framework.BWuWLRhz.js";const o="/assets/12-init-flow@2x.Ch1J56N5.png",k=JSON.parse('{"title":"12 · 项目初始化：用 /init 一键生成 CLAUDE.md","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"claude-code/12-project-init.md","filePath":"claude-code/12-project-init.md"}'),a={name:"claude-code/12-project-init.md"};function d(p,t,l,r,c,h){return i(),e("div",null,[...t[0]||(t[0]=[n(`<h1 id="_12-·-项目初始化-用-init-一键生成-claude-md" tabindex="-1">12 · 项目初始化：用 /init 一键生成 CLAUDE.md <a class="header-anchor" href="#_12-·-项目初始化-用-init-一键生成-claude-md" aria-label="Permalink to &quot;12 · 项目初始化：用 /init 一键生成 CLAUDE.md&quot;">​</a></h1><blockquote><p>📚 <strong>系列导航</strong>：上一篇 <a href="./11-web-and-cloud">11 网页版与云端</a> 带你跳出终端，在浏览器和云端环境里用上了 Claude Code。这一篇回到本地、回到一个最该养成的习惯——进新项目第一件事，敲 <code>/init</code>，让 Claude 自己扫一遍代码库、把项目说明书写出来。下一篇：<a href="./13-project-structure">13 项目结构</a> 。</p></blockquote><p>兄弟们，先讲个常见的蠢操作。</p><p>刚上手 Claude Code 那阵，接手一个之前同事留下的 Node 后端项目，没跑 <code>/init</code> 就直接开干。第一次问「测试怎么跑」，它去翻了一圈 <code>package.json</code> 才回你；过了二十分钟开个新会话，<strong>同样的问题它又从头翻了一遍</strong>；下午换个任务，它第三次问「这个项目用的是 npm 还是 pnpm」。</p><p>到这一刻就真被它看烦了——<strong>不是它笨，是没给它一份「项目说明书」，它每次都得从零摸索一遍</strong>。等学会进项目先敲一个 <code>/init</code>，这些重复全没了：它一次性把项目摸清、写成一份 CLAUDE.md 存下来，<strong>之后每次会话开局就自带这份上下文，再也不用反复解释</strong>。</p><p>说白了，<code>/init</code> 就干一件事：<strong>把「Claude 每次重新认识你的项目」变成「认识一次，长期记住」</strong>。这一篇就讲清楚它怎么用、生成出来长啥样、到底帮你省了什么。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>搞懂 <code>/init</code> 解决的核心痛点：从「每次重新摸索」到「一次生成长期记忆」</li><li>一套照着敲就行的操作：在项目根目录启动 <code>claude</code>，敲 <code>/init</code></li><li>知道它背后做了什么：扫描结构、识别技术栈、生成 CLAUDE.md 草稿</li><li>看懂生成出来的 CLAUDE.md 大致长什么样、每一块是干嘛的</li><li>一个关键认知：<code>/init</code> 只是起点，草稿得人工补一刀（怎么补，留给第 18 篇）</li></ul><hr><h2 id="_01-为什么要有-init-claude-的「失忆」问题" tabindex="-1">01 为什么要有 /init：Claude 的「失忆」问题 <a class="header-anchor" href="#_01-为什么要有-init-claude-的「失忆」问题" aria-label="Permalink to &quot;01 为什么要有 /init：Claude 的「失忆」问题&quot;">​</a></h2><p>先说结论：<strong>Claude Code 每次开新会话，都是「失忆」状态——它不记得你上次跟它说过什么</strong>。</p><p>这不是 bug，是它的设计。官方文档里写得明明白白：</p><blockquote><p>每个 Claude Code 会话都从一个全新的上下文窗口开始。</p></blockquote><p><strong>类比：每天来上班都换一个新实习生。</strong> 这实习生本事不小，但有个毛病——<strong>今天教会他的事，明天来的是另一个人，全得重教</strong>。你跟他讲「我们用 pnpm 不用 npm」「测试在 <code>tests/</code> 目录」「提交前要跑 lint」，第二天新的一个来了，又得从头讲一遍。</p><p>这就是没有 CLAUDE.md 时的真实状态。前面那个 Node 项目踩的坑就是这么来的——<strong>不是 Claude 记性差，是它压根没有跨会话的记忆载体</strong>。</p><p>那怎么办？官方给了两套跨会话传递知识的机制，这一篇只讲第一套：</p><table tabindex="0"><thead><tr><th>机制</th><th>谁来写</th><th>装的是什么</th></tr></thead><tbody><tr><td><strong>CLAUDE.md 文件</strong></td><td><strong>你</strong>（可让 /init 起草）</td><td>指令和规则：项目架构、构建命令、约定</td></tr><tr><td>自动记忆（Auto Memory）</td><td>Claude 自己</td><td>它工作中攒下的经验、调试心得</td></tr></tbody></table><p>注意自动记忆有个硬限制：<strong>每个会话只加载前 200 行或 25KB，超出部分不进上下文</strong>——所以靠自动记忆塞太多东西是靠不住的，CLAUDE.md 才是主力。</p><p>CLAUDE.md（项目记忆文件）就是那份<strong>写给「每天换人的实习生」看的入职手册</strong>——你提前写好放在项目里，<strong>Claude 每次开局先读一遍，立刻就「记起」这个项目是怎么回事</strong>。</p><p>而 <code>/init</code>，就是帮你把这份手册的初稿一键生成出来的命令。</p><blockquote><p>💡 <strong>一句话总结</strong>：Claude 每次开会话都「失忆」，CLAUDE.md 是它跨会话的记忆载体——<code>/init</code> 帮你把这份记忆的初稿一键写出来。</p></blockquote><hr><h2 id="_02-init-是什么-让-claude-自己写自己的说明书" tabindex="-1">02 /init 是什么：让 Claude 自己写自己的说明书 <a class="header-anchor" href="#_02-init-是什么-让-claude-自己写自己的说明书" aria-label="Permalink to &quot;02 /init 是什么：让 Claude 自己写自己的说明书&quot;">​</a></h2><p><code>/init</code> 是 Claude Code 内置的一个斜杠命令（slash command，输入框里以 <code>/</code> 开头的指令）。</p><p>它干的事，用一句话概括：<strong>让 Claude 自己扫描你的项目，生成一份专属于这个项目的 CLAUDE.md 草稿</strong>。</p><p>官方文档对它的描述很精准：</p><blockquote><p>运行 <code>/init</code> 自动生成起始 CLAUDE.md。Claude 分析你的代码库并创建一个包含构建命令、测试指令和它发现的项目约定的文件。</p></blockquote><p><strong>类比：让新员工自己写一份入职手册。</strong> 一般入职手册是老员工写好给新人看的。但 <code>/init</code> 反过来——<strong>它让这个「新来的」先把整个项目自己读一遍，然后把读到的东西（用什么技术、目录怎么分、命令怎么跑）整理成一份手册</strong>。读得对不对，你回头审一眼就知道。</p><p>这么设计的好处很实在：<strong>项目里的客观事实（技术栈、目录结构、有哪些脚本命令），Claude 自己扫一遍就能扒出来，根本不用你一个字一个字敲</strong>。你省下的力气，留着去补那些它扫不出来的东西（比如团队的分支命名规范），就够了。</p><p>什么时候该用它？一般有这几个触发时机：</p><ul><li><strong>接手一个别人留下的项目</strong>——你自己都没摸熟，正好让它先扫一遍给你打底</li><li><strong>自己的项目还没有 CLAUDE.md</strong>——一直在裸用，是时候补上了</li><li><strong>clone 下来一个开源项目想改改</strong>——先 <code>/init</code> 拿到项目地图，再动手</li></ul><blockquote><p>💡 <strong>一句话总结</strong>：<code>/init</code> 让 Claude 把项目自己读一遍，把客观事实整理成 CLAUDE.md 草稿——你省下扒事实的力气，专心补它扫不出的东西。</p></blockquote><hr><h2 id="_03-怎么跑-进目录、启动、敲命令" tabindex="-1">03 怎么跑：进目录、启动、敲命令 <a class="header-anchor" href="#_03-怎么跑-进目录、启动、敲命令" aria-label="Permalink to &quot;03 怎么跑：进目录、启动、敲命令&quot;">​</a></h2><p>操作简单到有点反高潮——<strong>启动 <code>claude</code> 后，在输入框里敲 <code>/init</code> 就完事</strong>。</p><p><strong>第一步：在项目根目录启动 Claude Code。</strong></p><p>这一步是关键，<strong>一定要 <code>cd</code> 进项目的根目录再启动</strong>，不能在桌面或主目录裸启。第 07 篇就强调过：你在哪启动，Claude 就把哪儿当工作区、读哪儿的文件。在主目录跑 <code>/init</code>，它扫的是你一堆乱七八糟的个人文件，扫了个寂寞。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /path/to/your-project</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span></span></code></pre></div><p><strong>第二步：在输入框里敲 <code>/init</code>，回车。</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/init</span></span></code></pre></div><p>就这样。接下来你<strong>什么都不用做</strong>，剩下全是 Claude 自己跑——整个过程无需手动操作，它会自行分析、自动输出。</p><p>那它在背后到底干了啥？看它跑过很多次，大致是这么个流程：</p><p><img src="`+o+`" alt="/init 五步初始化流程"></p><p>整件事串成一条线就是：<strong>在项目目录里启动 <code>claude</code> → 敲 <code>/init</code> → Claude 扫描项目结构、识别技术栈 → 落盘一份 CLAUDE.md 草稿 → 你再人工微调补全</strong>。前四步基本是自动的，最后那步「人工微调」才是把草稿变成好手册的关键，这一篇先点到，第 18 篇专门讲。</p><p>官方对这一步的说法是「分析你的代码库」。具体扫的是哪些文件，落到实处大致是这几类（跑下来它基本都会翻）：</p><ul><li><strong>依赖清单</strong>：<code>package.json</code>（Node）、<code>requirements.txt</code>（Python）、<code>pom.xml</code>（Java）这类——用来判断<strong>技术栈和能跑的命令</strong></li><li><strong>现有文档</strong>：<code>README</code> 之类——理解<strong>项目是干什么的</strong></li><li><strong>配置文件 + 代码结构</strong>：摸清<strong>目录怎么分、入口在哪</strong></li></ul><blockquote><p>⚠️ <strong>一个容易忽略的细节</strong>：如果项目里<strong>已经有</strong> CLAUDE.md，<code>/init</code> <strong>不会</strong>粗暴覆盖它。官方文档明确写了——这时它会<strong>建议改进</strong>而不是覆盖。在一个已经写好 CLAUDE.md 的项目里再敲一次 <code>/init</code>，本来还担心白写了，结果它老老实实列了几条「这里可以补充」的建议，原文一行没动。这点设计相当贴心。</p></blockquote><blockquote><p>💡 <strong>一句话总结</strong>：<code>cd</code> 进项目根目录、启动 <code>claude</code>、敲 <code>/init</code>，剩下交给它自动扫——已有 CLAUDE.md 时它只建议改进、不覆盖。</p></blockquote><hr><h2 id="_04-生成出来长什么样-claude-md-草稿拆解" tabindex="-1">04 生成出来长什么样：CLAUDE.md 草稿拆解 <a class="header-anchor" href="#_04-生成出来长什么样-claude-md-草稿拆解" aria-label="Permalink to &quot;04 生成出来长什么样：CLAUDE.md 草稿拆解&quot;">​</a></h2><p>跑完 <code>/init</code>，项目根目录下就多了一个 <code>CLAUDE.md</code> 文件。打开它，你会看到一份结构清晰的 Markdown。</p><p><strong>类比：一份标准的项目说明书。</strong> 它不是流水账，而是分好块的——项目是干嘛的、用了什么技术、目录怎么分、命令怎么敲、有什么约定。一个新人（包括下一次会话「失忆」后的 Claude）扫一眼这几块，就知道这项目怎么回事了。</p><p>生成的内容通常覆盖这么几块（具体字段 Claude 会按你项目实际情况来，下面是个典型样子）：</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># 项目名称</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 项目概述</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">简述这个项目是做什么的、核心功能是什么。</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 技术栈</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Frontend: React + TypeScript</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Backend: Node.js + Express</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Database: PostgreSQL</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 目录结构</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`src/components/\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> - React 组件</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`src/api/\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - API 层</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`tests/\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          - 测试文件</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 常用命令</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 启动开发服务器：</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`pnpm dev\`</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 运行测试：</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`pnpm test\`</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 代码检查：</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`pnpm lint\`</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 开发规范</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 使用 TypeScript strict 模式</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 提交前先跑 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`pnpm test\`</span></span></code></pre></div><p>逐块说一下它各自的价值——这正是 Claude 以前要反复问你的那些东西：</p><table tabindex="0"><thead><tr><th>这一块</th><th>装的是什么</th><th>它替你省掉的重复</th></tr></thead><tbody><tr><td><strong>项目概述</strong></td><td>项目目的、核心功能</td><td>不用每次解释「这项目是干嘛的」</td></tr><tr><td><strong>技术栈</strong></td><td>用了什么框架、语言、数据库</td><td>不用每次问「这是 React 还是 Vue」</td></tr><tr><td><strong>目录结构</strong></td><td>关键目录各放什么、入口在哪</td><td>不用每次找「API 代码在哪个文件夹」</td></tr><tr><td><strong>常用命令</strong></td><td>启动、测试、检查怎么敲</td><td>不用每次翻 <code>package.json</code> 找命令</td></tr><tr><td><strong>开发规范</strong></td><td>项目约定（如 strict 模式）</td><td>不用每次叮嘱「记得开严格模式」</td></tr></tbody></table><p>看明白没？<strong>前面那个 Node 项目里被问烂的「测试怎么跑」「用 npm 还是 pnpm」，全被「常用命令」「技术栈」这两块一次性吃掉了</strong>。手册写好往那一放，Claude 开局自己读，这些重复对话直接清零。</p><p>至于这个文件该放哪——官方给的位置是项目根目录的 <code>./CLAUDE.md</code>（或者 <code>./.claude/CLAUDE.md</code>）。<code>/init</code> 默认就帮你放对地方了，你<strong>不用操心路径</strong>。更细的层级关系（用户级、项目级怎么叠加），留到第 18 篇展开。</p><blockquote><p>💡 <strong>一句话总结</strong>：生成的 CLAUDE.md 分「概述 / 技术栈 / 目录 / 命令 / 规范」几块，每一块都精准对掉一类以前反复问的重复——文件放哪 <code>/init</code> 帮你搞定。</p></blockquote><hr><h2 id="_05-关键认知-init-是起点-不是终点" tabindex="-1">05 关键认知：/init 是起点，不是终点 <a class="header-anchor" href="#_05-关键认知-init-是起点-不是终点" aria-label="Permalink to &quot;05 关键认知：/init 是起点，不是终点&quot;">​</a></h2><p>这是这一篇最想让你记住的一句话：<strong><code>/init</code> 生成的是草稿，不是终稿</strong>。</p><p>为什么必须人工补一刀？因为有些东西，<strong>Claude 扫遍整个代码库也扫不出来</strong>——它们根本不在代码里，只在你和团队脑子里。</p><p>举几个 Claude 自己绝对推断不出来的例子：</p><ul><li><strong>分支命名规范</strong>：你们约定 <code>feature/xxx</code>、<code>fix/xxx</code>，这写在哪个文件里？没有。Claude 扫不到。</li><li><strong>部署流程</strong>：合并到 <code>main</code> 自动触发部署、还是要手动点一下？项目代码里看不出来。</li><li><strong>Code Review 要求</strong>：「PR 必须两人 approve」「核心模块改动先在 Plan Mode 出方案」——这是团队的隐性约定。</li><li><strong>业务背景</strong>：为什么这块要这么设计、哪个模块碰了容易出事——这些「为什么」藏在你脑子里。</li></ul><p>正确的姿势是<strong>迭代优化，而非一次写死</strong>。</p><p>前面那个 Node 项目就是反面教材的对照组：<code>/init</code> 完就以为万事大吉，没补。结果它生成的「常用命令」里漏了一条<strong>自己写的部署脚本</strong>（因为那脚本藏在 <code>scripts/</code> 里没在 <code>package.json</code> 暴露），Claude 自然没扫到。手动往 CLAUDE.md 里加一行「部署用 <code>./scripts/deploy.sh</code>」，这才算补全。<strong>所以正确的习惯是固定的：<code>/init</code> 跑完，立刻通读一遍，把它扫不出的硬约束手动补进去</strong>。</p><p>所以正确的姿势是这样的对比：</p><table tabindex="0"><thead><tr><th>❌ 错误用法</th><th>✅ 正确用法</th></tr></thead><tbody><tr><td><code>/init</code> 跑完就不管，当终稿用</td><td><code>/init</code> 跑完通读一遍，当草稿审</td></tr><tr><td>指望它把团队所有约定都扫出来</td><td>把它扫不出的硬约束（分支、部署、Review）手动补上</td></tr><tr><td>生成完一次写死，从此不动</td><td>随项目演进持续迭代、删掉过时的</td></tr></tbody></table><p><strong>记住这个分工就行：<code>/init</code> 负责把客观事实快速扒出来打底（这块它强），你负责把主观约定和业务背景补进去（这块只有你知道）</strong>。两边配合，才是一份真正好用的 CLAUDE.md。</p><p>至于「怎么把这份草稿改成一份精炼又好用的手册」——比如层级怎么设计、怎么用引用拆分、怎么长期维护——那是第 18 篇「CLAUDE.md 使用指南」的活儿。<strong>这一篇你只要做到「有了它」，下一步才谈「写好它」</strong>。</p><blockquote><p>💡 <strong>一句话总结</strong>：<code>/init</code> 只把客观事实扒出来打底，团队约定、部署流程这些它扫不到的硬货，必须你人工补——草稿审完才算数。</p></blockquote><hr><h2 id="_06-动手-给一个最小项目跑通-init" tabindex="-1">06 动手：给一个最小项目跑通 /init <a class="header-anchor" href="#_06-动手-给一个最小项目跑通-init" aria-label="Permalink to &quot;06 动手：给一个最小项目跑通 /init&quot;">​</a></h2><p>光说不练假把式。下面用一个<strong>两三个文件的最小项目</strong>，带你把 <code>/init</code> 完整跑一遍，验证它确实生成了 CLAUDE.md。不依赖任何复杂环境，照着敲就行。</p><p><strong>第一步：建一个最小项目</strong>（Mac / Linux）</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init-demo</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init-demo</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;name&quot;: &quot;init-demo&quot;, &quot;scripts&quot;: {&quot;test&quot;: &quot;echo test ok&quot;}}&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package.json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;console.log(&quot;hello from init-demo&quot;);&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> index.js</span></span></code></pre></div><p>Windows PowerShell 用户：<code>mkdir init-demo</code>、<code>cd init-demo</code> 照敲，<code>package.json</code> 和 <code>index.js</code> 这两个文件用记事本新建、把上面单引号里的内容贴进去存好即可。</p><p><strong>预期</strong>：<code>init-demo</code> 文件夹里有 <code>package.json</code> 和 <code>index.js</code> 两个文件。敲 <code>ls</code>（Windows 用 <code>dir</code>）能看到它俩。</p><p><strong>第二步：在项目目录里启动 Claude Code</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span></span></code></pre></div><p><strong>预期</strong>：出现欢迎屏幕，底部有输入框。确认终端当前就在 <code>init-demo</code> 目录下。</p><p><strong>第三步：跑 /init</strong></p><p>在输入框里敲：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/init</span></span></code></pre></div><p><strong>预期</strong>：Claude 开始滚屏——你会看到它在读 <code>package.json</code>、<code>index.js</code>，分析项目，然后写文件。跑完它会告诉你 CLAUDE.md 已生成。<strong>因为这项目只有两个文件，整个过程很快，几秒到十几秒</strong>。</p><p><strong>第四步：确认 CLAUDE.md 生成了</strong></p><p>退出 Claude（敲 <code>exit</code> 或按 <code>Ctrl+D</code>），回终端看：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CLAUDE.md</span></span></code></pre></div><p>（Windows PowerShell 用 <code>type CLAUDE.md</code>）</p><p><strong>预期</strong>：终端打印出一份 Markdown，里面<strong>至少能看到项目名 <code>init-demo</code>、技术栈是 Node.js / JavaScript、以及那条 <code>test</code> 命令（<code>echo test ok</code>）被识别成了「运行测试」</strong>。看到这些 = <code>/init</code> 跑通了，它确实把你这个小项目读懂并记了下来。</p><p><strong>第五步（可选）：验证它「记住」了</strong></p><p>重新进 <code>claude</code>，问一句：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>这个项目的测试命令是什么？</span></span></code></pre></div><p><strong>预期</strong>：它会<strong>直接告诉你测试命令</strong>，而不用再去翻 <code>package.json</code>——因为答案已经写在它开局就读过的 CLAUDE.md 里了。<strong>这一下，你就亲眼看到「失忆」被治好了</strong>。</p><blockquote><p>⚠️ 小提醒：真实项目里 <code>/init</code> 扫的文件多、生成内容也丰富得多，跑起来会比这个 demo 慢一些，耐心等它扫完即可；扫完别忘了第 05 节说的——<strong>通读一遍，把它扫不出的硬约束补进去</strong>。</p></blockquote><blockquote><p>💡 <strong>一句话总结</strong>：建个两文件的最小项目、<code>claude</code> 里敲 <code>/init</code>、退出后 <code>cat CLAUDE.md</code>，亲眼看它把项目读懂——再问一句测试命令，验证「失忆」被治好了。</p></blockquote><hr><h2 id="_07-小结" tabindex="-1">07 小结 <a class="header-anchor" href="#_07-小结" aria-label="Permalink to &quot;07 小结&quot;">​</a></h2><p>这一篇就讲透了一个动作：<strong>进新项目第一件事，敲 <code>/init</code>，让 Claude 自己把项目说明书的初稿写出来</strong>。</p><p>把核心要点串一遍：</p><table tabindex="0"><thead><tr><th>维度</th><th>结论</th></tr></thead><tbody><tr><td><strong>解决什么</strong></td><td>Claude 每次会话「失忆」，CLAUDE.md 是跨会话记忆载体</td></tr><tr><td><strong>/init 是什么</strong></td><td>让 Claude 扫项目、自动生成 CLAUDE.md 草稿的命令</td></tr><tr><td><strong>怎么跑</strong></td><td><code>cd</code> 进项目根目录 → 启动 <code>claude</code> → 敲 <code>/init</code></td></tr><tr><td><strong>它做了什么</strong></td><td>扫描结构、识别技术栈、扒命令，落盘成草稿</td></tr><tr><td><strong>长什么样</strong></td><td>概述 / 技术栈 / 目录 / 命令 / 规范几块</td></tr><tr><td><strong>关键认知</strong></td><td>草稿不是终稿——团队约定得人工补</td></tr></tbody></table><p><strong>你现在应该能：</strong> 进任何一个项目，在根目录启动 <code>claude</code> 后敲 <code>/init</code>，让它自动生成一份 CLAUDE.md，看懂生成内容里每一块是什么，并且明白这只是起点——客观事实它帮你打底，主观约定要你亲手补。<strong>从此 Claude 不再「每天换个失忆的新人」，而是开局就读过你项目说明书的老手</strong>。</p><hr><p>下一篇 <strong>13「项目结构」</strong>——你跑完 <code>/init</code>，CLAUDE.md 里那份「目录结构」是 Claude 扫出来的。但一个项目除了 CLAUDE.md，还有 <code>.claude/</code> 目录、<code>settings.json</code>、自定义命令、MCP 配置等一堆「Claude Code 专属文件」各司其职。它们分别放什么、长什么样？下一篇带你把这套项目结构一次理清。</p>`,105)])])}const u=s(a,[["render",d]]);export{k as __pageData,u as default};
