import{_ as e,o,c as t,a2 as d}from"./chunks/framework.BWuWLRhz.js";const n="/assets/11-agents-discovery@2x.Gm8QHqC9.png",i="/assets/11-discovery-chain@2x.Cnm087lC.png",m=JSON.parse('{"title":"11 · 项目说明书 AGENTS.md：把规矩焊进 Codex 的开工流程","description":"","frontmatter":{"head":[["meta",{"property":"og:site_name","content":"David Studio AI 编程指南"}]]},"headers":[],"relativePath":"codex/11-agents-md.md","filePath":"codex/11-agents-md.md"}'),a={name:"codex/11-agents-md.md"};function r(c,s,p,l,g,h){return o(),t("div",null,[...s[0]||(s[0]=[d('<h1 id="_11-·-项目说明书-agents-md-把规矩焊进-codex-的开工流程" tabindex="-1">11 · 项目说明书 AGENTS.md：把规矩焊进 Codex 的开工流程 <a class="header-anchor" href="#_11-·-项目说明书-agents-md-把规矩焊进-codex-的开工流程" aria-label="Permalink to &quot;11 · 项目说明书 AGENTS.md：把规矩焊进 Codex 的开工流程&quot;">​</a></h1><blockquote><p>📚 <strong>系列导航</strong>：上一篇 <a href="./10-cloud">10 · 云端 Codex Cloud</a> 把「让 Codex 在远端跑、回头收 PR」这条线讲透了。这一篇拉回本地，聊一份<strong>每个项目都该有、但九成新手写废了</strong>的文件——<code>AGENTS.md</code>，Codex 每次开工前都要先读一遍的项目说明书。</p></blockquote><p>说个我刚上手 Codex 时干的蠢事。</p><p>那是去年我把一个 Node 项目交给 Codex，开宗明义在根目录写了份 <code>AGENTS.md</code>，第一行赫然写着「<strong>本项目用 pnpm，禁用 npm</strong>」。结果它转头给我来了一句 <code>npm install</code>。我当场以为它没读到，气得把那条规矩复制了三遍、加粗、还打了感叹号。还是不听。</p><p>折腾半天才发现——<strong>它读到了，只是那条规矩被我埋在了第 140 行</strong>。前面我塞了公司简介、产品路线图、技术选型的来龙去脉，洋洋洒洒一百多行。等 Codex 翻到「禁用 npm」那句，注意力早被前面那堆废话稀释没了。<strong>问题从来不是它不听话，是我把唯一有用的那条焊进了一坨没用的里</strong>。</p><p><code>AGENTS.md</code> 之于 Codex，就是 <code>CLAUDE.md</code> 之于 Claude Code，<strong>同一个概念换了个文件名</strong>。但 Codex 这边的发现机制、覆写规则、字节上限自成一套，有几处容易踩的坑。这篇就把它们连同「该写什么、不该写什么」一次讲清。</p><p><strong>看完这一篇，你会拿到：</strong></p><ul><li>Codex 发现 <code>AGENTS.md</code> 的完整链条（全局层 → 项目层 → 合并顺序），以及谁冲突时说了算</li><li><code>AGENTS.override.md</code> 这个 Claude Code 没有的「临时盖章」机制，什么时候用</li><li>一张「该写 vs 不该写」清单，避开我当年那个「140 行没人听」的坑</li><li>改文件名（<code>project_doc_fallback_filenames</code>）、调字节上限（<code>project_doc_max_bytes</code>）两个旋钮的官方用法</li><li>一套照着敲就能验证「Codex 到底读没读到」的动手流程</li></ul><p><strong>注意：</strong> 本篇只聚焦 <code>AGENTS.md</code> 这一个文件怎么写好、怎么被加载。和它容易混的「记忆（Memories）」机制（那是本地自动回忆层、默认关着）在 [02 核心概念] 里已经点过，<strong>记住一句话：真正必须每次生效的团队规矩，写进 <code>AGENTS.md</code>，别指望记忆</strong>。</p><hr><h2 id="_01-先搞懂-agents-md-是-codex-开工前的「交接清单」" tabindex="-1">01 先搞懂：AGENTS.md 是 Codex 开工前的「交接清单」 <a class="header-anchor" href="#_01-先搞懂-agents-md-是-codex-开工前的「交接清单」" aria-label="Permalink to &quot;01 先搞懂：AGENTS.md 是 Codex 开工前的「交接清单」&quot;">​</a></h2><p>先给结论：<strong><code>AGENTS.md</code> 是你写给 Codex 的一份持久指令，它每次启动、动手干活之前都先读一遍，当成这个项目的背景装进脑子。</strong></p><p>为什么需要它？因为 Codex 每开一轮（每轮（per run）——官方术语，在 TUI 里通常是每启动一个会话算一轮）都从一张白纸开始。上回你苦口婆心交代的「用 pnpm、别碰 <code>legacy/</code>、测试这么跑」，这回它一概不记得。没有 <code>AGENTS.md</code>，你就得每次重新解释一遍。</p><p><strong>类比：交接班的工作清单。</strong> 工厂三班倒，上一班下班前会在墙上那块板写清楚——这台机器有点毛病别硬开、这批料要先质检、紧急联系人是谁。下一班接手，照着板上干就行，不用把上一班的人薅回来口述。<code>AGENTS.md</code> 就是 Codex 的那块交接板——<strong>而且是它每次接班前都会先扫一眼的那块</strong>。</p><p>（注意我这里没用「入职手册」——那个比喻 [02] 已经用过了。交接清单更贴切的地方在于：它强调「每一轮开工都重读」，不是「入职那天看一次」。）</p><p>什么时候该往里加内容？几个特别实在的信号：</p><ul><li><strong>Codex 第二次犯同一个错</strong> —— 这条得固化进去，别再口头纠正第三遍</li><li>你这轮又敲了一遍上轮敲过的那句更正</li><li>代码审查时发现，它本该早就知道这个代码库的某个约定</li><li>新队友（或者三个月后的你自己）需要同样的背景才能快速上手</li></ul><p>我自己最爱的用法，[02] 里提过、这里再强调一次——<strong>把它当反馈回路</strong>：Codex 对你代码库做了错误假设，别光在对话里纠正（那是一次性的，下轮它又忘），直接让它把这条修正写进 <code>AGENTS.md</code>。我给一个 Python 项目调了两周，那份 <code>AGENTS.md</code> 从空白长到二十来行，全是它踩过、被我逮住、然后自己记下来的坑，现在新会话基本不犯重复错误了。</p><blockquote><p>💡 一句话总结：<code>AGENTS.md</code> 是 Codex 每轮开工必读的交接清单，<strong>犯一次错就记一条，越用越顺手</strong>；它跟 Claude Code 的 <code>CLAUDE.md</code> 是同一个概念，换了个名字。</p></blockquote><hr><h2 id="_02-发现链条-codex-是怎么找到这些文件的" tabindex="-1">02 发现链条：Codex 是怎么找到这些文件的 <a class="header-anchor" href="#_02-发现链条-codex-是怎么找到这些文件的" aria-label="Permalink to &quot;02 发现链条：Codex 是怎么找到这些文件的&quot;">​</a></h2><p><code>AGENTS.md</code> 不止一份，它能放在好几个地方，<strong>作用范围从大到小</strong>。Codex 启动时会把它们串成一条「指令链」。这一节就把这条链拆开——这也是跟 Claude Code 差异最大的一块，看仔细。</p><p>按官方文档，Codex 构建指令链分两步走（一轮一构建，TUI 里通常是每启动一个会话构建一次）：</p><p><strong>第一步，全局层（Global scope）。</strong> 在你的 Codex 主目录（默认 <code>~/.codex</code> ，除非你设了 <code>CODEX_HOME</code> 环境变量）里，Codex <strong>先看有没有 <code>AGENTS.override.md</code> ，有就用它；没有才读 <code>AGENTS.md</code></strong> 。这一层<strong>只取第一个非空文件</strong>，不会两个都读。</p><p><strong>第二步，项目层（Project scope）。</strong> 从项目根目录（通常是 Git 根）开始，<strong>一路往下走到你当前所在的目录</strong>。沿途每一个目录里，Codex 按这个顺序挑：先看 <code>AGENTS.override.md</code> ，再看 <code>AGENTS.md</code> ，最后看 <code>project_doc_fallback_filenames</code> 里你自定义的备选文件名。<strong>每个目录最多只收一个文件</strong>。</p><p><strong>第三步，合并（Merge order）。</strong> Codex 把找到的这些文件<strong>从根到叶依次拼接</strong>，中间用空行隔开。<strong>越靠近你当前目录的文件，因为排在拼接结果的越后面，优先级越高、能盖过前面的</strong>。</p><p>光说有点绕，上张图：</p><p><img src="'+n+'" alt="AGENTS.md 三层发现链：机器级 ~/.codex/（override 优先）→ 项目层 Git 根逐级累加 → 拼成指令链"></p><p>这张图把整条链走了一遍：先在全局层二选一，再到项目层逐级各挑一个，最后从根到叶拼起来——<strong>离你越近的，说话越晚、越管用</strong>。</p><p>这里有两个要点，都来自官方，记牢：</p><p><strong>第一，是「拼接」不是「覆盖」。</strong> 全局那份和项目那份<strong>同时生效</strong>，不存在「写了项目级，全局级就失效」。它们全都进上下文，后面的不会把前面的整个顶掉，只是<strong>冲突的条目以靠后（更近）的为准</strong>。</p><p><strong>第二，更近的赢。</strong> 比如全局 <code>AGENTS.md</code> 说「字符串用单引号」、项目根 <code>AGENTS.md</code> 说「用双引号」——项目根离当前目录更近、排得更靠后，所以<strong>双引号胜出</strong>。说白了，<strong>项目规矩能盖过你的个人偏好，这正是团队协作想要的</strong>。</p><p><strong>类比：导航地图的三层缩放。</strong> 全国地图给你大方向（全局层），城市地图给你城区路线（项目根），小区门口的指示牌告诉你最后那几步怎么走（子目录）。三层同时有效、信息互不删除；可一旦小区门口的牌子和大地图对不上，<strong>以现场最近那块牌子为准</strong>——它最贴你眼前这一步。<code>AGENTS.md</code> 这条链就是这套「越靠近当前位置越具体、冲突时越优先」的逻辑。</p><p><img src="'+i+`" alt="AGENTS.md 三层发现链、就近优先"></p><p>这张图把三层从上到下叠了出来——全局层（<code>~/.codex/</code>）、仓库根、子目录依次拼接，三层同时生效；右侧那根「优先级」轴在提醒你：<strong>越靠近当前目录的那一层，排在拼接结果越靠后、冲突时越管用</strong>，这就是「就近覆盖」。</p><blockquote><p>💡 一句话总结：Codex 发现链 = 全局层（<code>~/.codex/</code>，override 优先、只取一个非空文件）→ 项目层（Git 根逐级到当前目录、每目录挑一个）→ <strong>从根到叶拼接，越近优先级越高</strong>；是拼接不是覆盖。</p></blockquote><hr><h2 id="_03-agents-override-md-claude-code-没有的「临时盖章」" tabindex="-1">03 AGENTS.override.md：Claude Code 没有的「临时盖章」 <a class="header-anchor" href="#_03-agents-override-md-claude-code-没有的「临时盖章」" aria-label="Permalink to &quot;03 AGENTS.override.md：Claude Code 没有的「临时盖章」&quot;">​</a></h2><p>上一节反复出现一个名字——<code>AGENTS.override.md</code> 。这玩意儿 Claude Code 那边<strong>没有对应物</strong>，是 Codex 独有的设计，单独拎出来讲。</p><p>先说它解决什么问题。设想你 <code>~/.codex/AGENTS.md</code> 里写了一套团队共用的全局约定，平时挺好。但今天你接了个临时活，需要<strong>整段换掉</strong>这套全局指导，又不想把原文件删了（删了回头还得重写）。怎么办？</p><p><strong>类比：在文件上盖一张「以此为准」的便签。</strong> 原合同还在抽屉里，你没撕；只是临时贴了张便签条「本月按这个新条款执行」。事儿办完，便签一揭，原合同自动生效。<code>AGENTS.override.md</code> 就是这张便签——<strong>它在时，同级的 <code>AGENTS.md</code> 被整个跳过；把它删了，原来那份立刻回来</strong>。</p><p>官方给的两个典型场景：</p><ul><li><strong>全局临时覆写</strong>：在 <code>~/.codex/AGENTS.override.md</code> 写一套临时全局指导，原 <code>~/.codex/AGENTS.md</code> 原封不动。活干完删掉 override，共享的那份就恢复了。</li><li><strong>子目录特殊规则</strong>：某个子目录的团队需要一套跟外面完全不同的规矩。比如支付服务目录 <code>services/payments/</code> ，里头放一个 <code>AGENTS.override.md</code> ：</li></ul><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># services/payments/AGENTS.override.md</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 支付服务规则</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 用 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`make test-payments\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 替代 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`npm test\`</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 轮换 API Key 前必须先通知安全频道</span></span></code></pre></div><p>放了这个 override，<strong>这个目录里同级的 <code>AGENTS.md</code> 就被跳过</strong>（如果有的话），Codex 在这个目录干活时认 override 这份。</p><p>这里要拎清一个新手最容易混的点——<strong>override 跟「覆盖整条链」不是一回事</strong>：</p><table tabindex="0"><thead><tr><th>范围</th><th><code>AGENTS.override.md</code> 的作用</th></tr></thead><tbody><tr><td><strong>同一个目录内</strong></td><td>它存在 → 同级的 <code>AGENTS.md</code>（和备选文件名）被跳过，<strong>这一层只认 override</strong></td></tr><tr><td><strong>跨目录（整条链）</strong></td><td>它<strong>不会</strong>清掉别的目录的指令；上层指令照常拼进来，只是冲突时被更近的覆盖</td></tr></tbody></table><p>说白了：<strong>override 是「这一层用我，别用旁边的 <code>AGENTS.md</code>」，不是「全链路只听我一个」</strong>。整条链的拼接、就近优先规则照旧。我第一次用的时候就理解错了，以为在子目录放个 override 能把全局那份屏蔽掉，结果全局约定照样生效——后来翻官方文档才搞明白，<strong>它只在自己那一格里二选一</strong>。</p><p>还有个排查神器级的用法：万一 Codex 蹦出来一条你压根没写的奇怪指令，<strong>第一件事就是顺着目录树往上、连同 <code>~/.codex</code> ，找有没有谁藏了个 <code>AGENTS.override.md</code></strong>。把它改名或删掉，就回退到常规 <code>AGENTS.md</code> 了。官方排查清单里专门列了这条。</p><blockquote><p>💡 一句话总结：<code>AGENTS.override.md</code> 是 Codex 独有的「临时盖章」——<strong>它在的那一层，同级 <code>AGENTS.md</code> 被跳过；它不影响别的目录的指令拼接</strong>；想临时换指导又不删原文件，用它最省事；遇到莫名其妙的指令，先去揪有没有谁藏了个 override。</p></blockquote><hr><h2 id="_04-该写什么-vs-不该写什么" tabindex="-1">04 该写什么 vs 不该写什么 <a class="header-anchor" href="#_04-该写什么-vs-不该写什么" aria-label="Permalink to &quot;04 该写什么 vs 不该写什么&quot;">​</a></h2><p>这一节是全篇的命门，也是我开头那个「140 行没人听」的解药。<strong><code>AGENTS.md</code> 写不好，九成栽在「该写的没写清，不该写的塞一堆」。</strong></p><p>先说该写的——一句话：<strong>写「Codex 在每一轮里都该保持的事实」</strong>。翻成清单就是这五类：</p><table tabindex="0"><thead><tr><th>类别</th><th>具体写什么</th><th>例子</th></tr></thead><tbody><tr><td><strong>项目概述</strong></td><td>一句话说清这是个啥</td><td>「基于 FastAPI 的订单管理后端」</td></tr><tr><td><strong>技术栈</strong></td><td>语言、框架、数据库、关键工具</td><td>「Python 3.11 / PostgreSQL / pytest」</td></tr><tr><td><strong>常用命令</strong></td><td>测试、构建、检查 / lint 怎么跑</td><td><code>npm run lint</code> 、<code>make test-payments</code></td></tr><tr><td><strong>代码约定</strong></td><td>风格、命名、必须遵守的写法</td><td>「函数必须有类型注解」「字符串用双引号」</td></tr><tr><td><strong>明确的「不要做」</strong></td><td>雷区、禁改文件、必须先确认的操作</td><td>「禁改 <code>migrations/</code> 已有文件」「新增生产依赖前先确认」</td></tr></tbody></table><p>其中<strong>常用命令是被参考最频繁的</strong>——Codex 跑测试、提 PR 前会先来这儿翻命令，省得它瞎猜或者用错。官方那份示例 <code>AGENTS.md</code> 里头第一条就是命令：「提 PR 前运行 <code>npm run lint</code> 」。<strong>禁止清单则是防它「聪明但闯祸」的护栏</strong>：哪些目录是遗留代码只读不改、哪个操作改之前必须先问你。</p><p>再说<strong>不该写的</strong>，这才是新手翻车重灾区，也是我亲身踩过的坑：</p><ul><li>❌ <strong>长篇大论的背景</strong>：公司介绍、产品愿景、技术选型的历史渊源——Codex 写代码用不上，纯占上下文，还把真正有用的规矩稀释了（参见我开头那 140 行）。</li><li>❌ <strong>过时信息</strong>：换了包管理器却没更新 <code>AGENTS.md</code> ，里头还写着 npm，结果反过来误导它。</li><li>❌ <strong>看代码就知道的东西</strong>：别去复述目录结构里每个文件干啥、别把 ESLint / Prettier 已经定义好的代码风格再抄一遍。<strong>Codex 自己会读代码，复述等于白占空间。</strong></li></ul><p>官方对「别写太大」给了硬性约束，但它的红线跟 Claude Code 不太一样——<strong>Claude Code 是「按行数」（建议 200 行内），Codex 是「按字节」</strong>：</p><p>Codex 跳过空文件，一旦拼接后的总大小达到 <code>project_doc_max_bytes</code> 限制（默认 32 KiB），就停止继续加文件。</p><p>注意这话有两层意思：一是<strong>默认 32 KiB 封顶</strong>，超了就截断（甚至整个文件被拦在外面）；二是这个上限<strong>算的是合并后的总和</strong>，全局 + 项目 + 子目录加一块儿。所以你一份文件写太肥，可能把后面更重要的那份顶出窗口。<strong>官方建议：撞上限了，要么调高 <code>project_doc_max_bytes</code> ，要么把指令拆到多级子目录分散放</strong>（这正好呼应 [04 计费] 里讲的「<code>AGENTS.md</code> 瘦身 + 按子目录分层」省 token）。</p><p>有个好用的土办法，我现在写每一条之前都先自问一句：<strong>「这条 Codex 看代码能不能自己推出来？能，就删。」</strong> 就靠这一刀，能把一份接手项目的 <code>AGENTS.md</code> 从一百多行砍到几十行，留下的全是它推不出来的硬约束——砍完之后，它用错包管理器的次数肉眼可见地降下去。</p><blockquote><p>💡 一句话总结：写「每轮都该记住的事实」（概述 / 技术栈 / 命令 / 约定 / 禁区），<strong>删一切 Codex 看代码能自证的东西</strong>；红线是<strong>字节</strong>——合并后默认 32 KiB（<code>project_doc_max_bytes</code>），撑爆了就调高上限或按子目录拆。</p></blockquote><hr><h2 id="_05-两个旋钮-改文件名、调字节上限" tabindex="-1">05 两个旋钮：改文件名、调字节上限 <a class="header-anchor" href="#_05-两个旋钮-改文件名、调字节上限" aria-label="Permalink to &quot;05 两个旋钮：改文件名、调字节上限&quot;">​</a></h2><p>大多数人用默认的 <code>AGENTS.md</code> 就够了。但有两种情况你得动配置——这一节讲两个写在 <code>~/.codex/config.toml</code>（Codex 的用户级配置文件）里的旋钮。</p><h3 id="旋钮一-让-codex-认你已有的文件名" tabindex="-1">旋钮一：让 Codex 认你已有的文件名 <a class="header-anchor" href="#旋钮一-让-codex-认你已有的文件名" aria-label="Permalink to &quot;旋钮一：让 Codex 认你已有的文件名&quot;">​</a></h3><p>设想你仓库里早就有一份 <code>TEAM_GUIDE.md</code> ，全队一直拿它当规范文档。你不想再造一个 <code>AGENTS.md</code> 重复一遍，<strong>想让 Codex 直接把 <code>TEAM_GUIDE.md</code> 当指令文件读</strong>。这时候就用 <code>project_doc_fallback_filenames</code>（备选文件名）：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.codex/config.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">project_doc_fallback_filenames = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;TEAM_GUIDE.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.agents.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>加了这条之后，Codex 在每个目录里的挑选顺序变成：<code>AGENTS.override.md</code> → <code>AGENTS.md</code> → <code>TEAM_GUIDE.md</code> → <code>.agents.md</code> ，<strong>取第一个存在（且非空）的</strong>。</p><blockquote><p><strong>不在这个列表里的文件名，Codex 在指令发现这一步一律忽略。</strong> 想让某个自定义文件被当成项目说明书，就必须把它的名字加进 <code>project_doc_fallback_filenames</code> ——别指望 Codex「猜」。</p></blockquote><h3 id="旋钮二-合并内容被截断了-调高上限" tabindex="-1">旋钮二：合并内容被截断了，调高上限 <a class="header-anchor" href="#旋钮二-合并内容被截断了-调高上限" aria-label="Permalink to &quot;旋钮二：合并内容被截断了，调高上限&quot;">​</a></h3><p>上一节说过默认 32 KiB 封顶。如果你的指令确实超了、又暂时不想拆文件，可以把 <code>project_doc_max_bytes</code> 调大：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.codex/config.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">project_doc_max_bytes = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">65536</span></span></code></pre></div><p>这就把上限抬到了 64 KiB，能塞下更多合并指导才触顶。不过我得劝一句——<strong>这是治标</strong>。内容真有用，调上限；但更多时候，撞上 32 KiB 是个信号，提醒你「该精简了」或「该按子目录拆了」，而不是「该把上限往上拱」。我自己的偏好是<strong>优先拆、其次删，实在拆不动才抬上限</strong>。</p><p>两个旋钮的适用场景对照一下：</p><table tabindex="0"><thead><tr><th>你的处境</th><th>该动哪个旋钮</th></tr></thead><tbody><tr><td>仓库已有 <code>TEAM_GUIDE.md</code> 想直接当说明书用</td><td><code>project_doc_fallback_filenames</code> 加上它的名字</td></tr><tr><td>合并后的指导超了 32 KiB 被截断</td><td>先想拆子目录 / 精简；非调不可才抬 <code>project_doc_max_bytes</code></td></tr><tr><td>想换一套独立配置 profile（如自动化用户）</td><td>设 <code>CODEX_HOME</code> 指到别的目录（见下节动手）</td></tr></tbody></table><blockquote><p>⚠️ 改完 <code>config.toml</code> 记得<strong>重启 Codex</strong> ——这两个配置在启动时读，不重启不生效。这点官方排查清单里专门强调过（「回退文件名不生效？确认拼写无误，重启 Codex」）。</p></blockquote><blockquote><p>💡 一句话总结：<code>project_doc_fallback_filenames</code> 让 Codex 认你自定义的文件名（不在列表里的一律忽略）；<code>project_doc_max_bytes</code> 抬高合并上限（默认 32 KiB），但撞上限<strong>优先拆 / 删而非硬抬</strong>；改完都得重启。</p></blockquote><hr><h2 id="_06-动手-给玩具项目配一份合格的-agents-md-并验证加载" tabindex="-1">06 动手：给玩具项目配一份合格的 AGENTS.md 并验证加载 <a class="header-anchor" href="#_06-动手-给玩具项目配一份合格的-agents-md-并验证加载" aria-label="Permalink to &quot;06 动手：给玩具项目配一份合格的 AGENTS.md 并验证加载&quot;">​</a></h2><p>光说不练没用。下面用一个最小项目，走一遍「建文件 → 写规矩 → 验证 Codex 真读到了」的完整流程。跟着敲，几分钟搞定。</p><blockquote><p>平台差异先说清：下面建项目的命令 <code>mkdir</code> / <code>git init</code> 在 <strong>Mac / Linux</strong> 直接用；<strong>Windows</strong> 下建议在 Git Bash 或 WSL 里敲，或用资源管理器手动建文件夹。路径里的 <code>~</code> 指用户主目录，Windows 对应 <code>C:\\Users\\你的用户名\\</code> 。</p></blockquote><p><strong>第一步：建个玩具项目并初始化 git</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> agents-md-demo</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> agents-md-demo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span></span></code></pre></div><p><strong>预期</strong>：<code>agents-md-demo</code> 目录里出现一个 <code>.git</code> 目录。git 初始化是为了让 Codex 能把这里识别成「项目根」（它通常以 Git 根为起点往下扫），也为了这份 <code>AGENTS.md</code> 能进版本控制、团队共享。</p><p><strong>第二步：手写一份精简的项目级 AGENTS.md</strong></p><p>用你顺手的编辑器，在项目根目录新建 <code>AGENTS.md</code> ，贴入下面这份（<strong>注意它一共没几行——这就是好 <code>AGENTS.md</code> 该有的样子</strong>）：</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># agents-md-demo — 一个演示用的最小项目</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">只有用来演示 AGENTS.md 怎么写，没有真实业务逻辑。</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 常用命令</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \`npm test\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> —— 运行测试</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 编程约定</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 所有函数必须有类型注解</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 字符串一律用双引号</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 注意事项</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 不要新增任何生产依赖，需要时先问我</span></span></code></pre></div><p><strong>预期</strong>：项目根目录下出现 <code>AGENTS.md</code> ，内容就是上面这几节。<strong>全文十几行——记住这个篇幅感，真实项目也别失控膨胀。</strong></p><p><strong>第三步：让 Codex 复述它读到的指令</strong></p><p>官方给了一个特别直接的验证法——<strong>让 Codex 把当前生效的指令总结一遍</strong>。在项目目录里跑：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ask-for-approval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> never</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Summarize the current instructions.&quot;</span></span></code></pre></div><p><strong>预期</strong>：Codex 在动手提方案之前，会<strong>回显你刚写的那几条</strong>（命令、类型注解、双引号、别加依赖）。只要它复述出来了，就说明这份 <code>AGENTS.md</code> 这一轮确实被装进了它的上下文。</p><blockquote><p>ℹ️ 这里的 <code>--ask-for-approval never</code> 只是为了让它别在演示时停下来问你审批、好让输出干净，<strong>不是说平时干活也建议这么用</strong>——日常该用什么审批模式，看 [02 核心概念] 和后面权限相关的篇章，别在不熟的项目里随手关审批。</p></blockquote><p><strong>第四步（进阶）：验证子目录覆写真的「就近优先」</strong></p><p>想亲眼看到「越近越管用」，再做一步。在项目里建个子目录，放一份只覆盖一条命令的 override：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> services/payments</span></span></code></pre></div><p>在 <code>services/payments/AGENTS.override.md</code> 里写：</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># services/payments/AGENTS.override.md</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## 支付服务规则</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 用 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`make test-payments\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 替代 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\`npm test\`</span></span></code></pre></div><p>然后<strong>从这个子目录的视角</strong>启动 Codex，让它报告加载了哪些指令来源：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> services/payments</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ask-for-approval</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> never</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;List the instruction sources you loaded.&quot;</span></span></code></pre></div><p><strong>预期</strong>：按官方描述，Codex 会<strong>依次列出</strong>——先全局文件（如果你 <code>~/.codex</code> 下有的话）、再仓库根的 <code>AGENTS.md</code> 、最后是支付服务这份 override；而且这一层的测试命令会以 override 的 <code>make test-payments</code> 为准，<strong>根目录那条 <code>npm test</code> 在这个子目录里被盖掉了</strong>。这就是「就近优先」的活样本。</p><blockquote><p>⚠️ 万一发现 Codex 没按 <code>AGENTS.md</code> 来：① 先用上面的「Summarize the current instructions」确认它到底读到没；② 跑 <code>codex status</code> 看它认的工作区根目录是不是你以为的那个；③ 顺着目录树往上找有没有谁藏了 <code>AGENTS.override.md</code> 把你的规矩盖了；④ 文件别是空的（Codex 跳过空文件）。这几步是官方排查清单的顺序，照着走基本能定位。</p></blockquote><blockquote><p>💡 一句话总结：照着「建文件 → 写精简规矩 → 用『Summarize the current instructions』让 Codex 复述」走一遍，亲眼确认它真读到了你的 <code>AGENTS.md</code>；再加一层子目录 override，就能看清「越近越优先」是怎么把根目录那条命令盖掉的。</p></blockquote><hr><h2 id="_07-小结" tabindex="-1">07 小结 <a class="header-anchor" href="#_07-小结" aria-label="Permalink to &quot;07 小结&quot;">​</a></h2><p>这一篇你把 <code>AGENTS.md</code> 这个「Codex 的项目说明书」从发现机制到落地写法摸清了：</p><table tabindex="0"><thead><tr><th>维度</th><th>关键结论</th></tr></thead><tbody><tr><td>是什么</td><td>每轮开工必读的交接清单，<strong>就是 <code>CLAUDE.md</code> 换了个名</strong></td></tr><tr><td>发现链</td><td>全局层（override 优先、取一个非空）→ 项目层（Git 根逐级到当前目录、每目录挑一个）→ <strong>从根到叶拼接</strong></td></tr><tr><td>谁说了算</td><td>拼接不覆盖，<strong>越靠近当前目录的越晚拼、冲突时占优</strong></td></tr><tr><td>override</td><td>Codex 独有的「临时盖章」：<strong>那一层跳过同级 <code>AGENTS.md</code></strong>，但不清掉别处指令</td></tr><tr><td>写什么</td><td>概述 / 技术栈 / 命令 / 约定 / 禁区，<strong>删一切代码能自证的</strong></td></tr><tr><td>大小红线</td><td><strong>按字节</strong>算，合并默认 32 KiB（<code>project_doc_max_bytes</code>），撑爆优先拆 / 删</td></tr><tr><td>两个旋钮</td><td><code>project_doc_fallback_filenames</code> 改文件名、<code>project_doc_max_bytes</code> 调上限，<strong>改完重启</strong></td></tr></tbody></table><p><strong>你现在应该能：</strong> 判断一条信息该不该进 <code>AGENTS.md</code> 、该放哪一层；看懂多层文件冲突时谁赢；用 <code>AGENTS.override.md</code> 临时换指导、又不删原文件；撞上字节上限知道是拆还是抬；用「Summarize the current instructions」一句话验证 Codex 到底读没读到。<strong>一句话——你能写出一份 Codex 真愿意听、而不是像我当年那样写了 140 行没人理的 <code>AGENTS.md</code> 了。</strong></p><hr><p>下一篇 <strong>12「斜杠命令与快捷键」</strong>——这篇里你已经在命令行里敲了好几次 <code>codex ...</code> ，那进了会话之后呢？<code>/</code> 开头的斜杠命令能让你当场切模式、清上下文、看状态，再配上几个顺手的快捷键，操作效率能翻倍。下一篇就把会话里这套「快捷操作面板」一次摊开。留个小思考：这一篇让 Codex「记住规矩」靠的是写文件，那要是想在会话<strong>当场</strong>改它的行为、又不想动 <code>AGENTS.md</code> ，你猜该用什么？</p>`,112)])])}const E=e(a,[["render",r]]);export{m as __pageData,E as default};
